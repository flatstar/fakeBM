---
phase: 03-wait-proof
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app/api/blob/upload/route.ts
  - lib/downscale.ts
  - tests/api/blob-upload.test.ts
autonomous: false
requirements: [PROOF-02]
user_setup:
  - service: vercel-blob
    why: "듀얼 사진(시킨 척 음식 / 실제 식단) public 저장 — 피드(Phase 4)·OG(Phase 6) 공개 URL 요구"
    env_vars:
      - name: BLOB_READ_WRITE_TOKEN
        source: "Vercel Dashboard → Storage → Create Blob Store → .env.local (vercel env pull). server-only, NEVER NEXT_PUBLIC_"
    dashboard_config:
      - task: "Create Blob store"
        location: "Vercel Dashboard → Storage → Blob"
must_haves:
  truths:
    - "@vercel/blob 2.4.0이 설치되어 클라 직접 업로드(upload/handleUpload)가 가능하다 (D-12)"
    - "POST /api/blob/upload가 onBeforeGenerateToken에서 세션 게이트 + MIME 화이트리스트 + size 제한 + addRandomSuffix를 강제한다 (PROOF-02 보안)"
    - "lib/downscale가 EXIF 회전 정규화 + 긴 변 1080~1440px + q0.8 리사이즈를 수행한다 (D-12)"
    - "Vercel Blob store가 프로비저닝되고 BLOB_READ_WRITE_TOKEN이 server-only로 주입된다 (D-13 public 저장)"
  artifacts:
    - path: "app/api/blob/upload/route.ts"
      provides: "handleUpload 토큰 브로커 (세션 게이트)"
      exports: ["POST"]
    - path: "lib/downscale.ts"
      provides: "canvas 다운스케일 + EXIF 정규화 헬퍼"
    - path: "tests/api/blob-upload.test.ts"
      provides: "토큰 라우트 세션 게이트 + MIME 강제 단언 (offline mock)"
  key_links:
    - from: "blob/upload route onBeforeGenerateToken"
      to: "requireSession()"
      via: "세션 없으면 throw (익명 업로드 차단)"
      pattern: "onBeforeGenerateToken"
    - from: "lib/downscale"
      to: "@vercel/blob/client upload (04에서 소비)"
      via: "scaled Blob 반환"
      pattern: "toBlob|createImageBitmap"
---

<objective>
사진 업로드 인프라(PROOF-02 plumbing): @vercel/blob 2.4.0 설치 + POST /api/blob/upload handleUpload 토큰 라우트(세션 게이트·MIME·size·랜덤 suffix) + lib/downscale(canvas EXIF 정규화) + 토큰 라우트 offline 테스트 + Vercel Blob store 프로비저닝 체크포인트.

Purpose: 듀얼 사진이 서비스 정체성(시킨 척 vs 실제 식단). 04의 PostClient/PhotoUploadSlot이 이 라우트·헬퍼·토큰을 소비한다. 인프라를 인증 슬라이스와 분리해 병렬·검증 분리.
Decisions covered: D-12(클라 직접 업로드 + canvas 다운스케일), D-13(Blob public 접근 — 피드/OG 공개 URL).
Output: 패키지 설치, blob/upload 라우트, downscale 헬퍼, 테스트, Blob store 프로비저닝(human).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wait-proof/03-CONTEXT.md
@.planning/phases/03-wait-proof/03-RESEARCH.md
@.planning/phases/03-wait-proof/03-PATTERNS.md
@app/api/orders/route.ts
@tests/api/orders/route.test.ts
@lib/auth.ts
</context>

## Phase Goal

**As a** 식비절감·다이어트 사용자, **I want to** "시킨 척한 음식"과 "실제 내 식단" 사진을 각각 업로드하는 것, **so that** 인증 포스트에 듀얼 사진이 박제되어 피드·공유로 이어진다.

> 이 인프라 슬라이스 완료 후: 인증된 사용자가 (04의 PhotoUploadSlot 경유) Blob에 직접 업로드할 수 있는 토큰을 서버가 안전하게 발급한다. 가시 듀얼 사진 UI는 04가 완성.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: @vercel/blob 2.4.0 설치 + Vercel Blob store 프로비저닝</name>
  <files>package.json, package-lock.json, .env.local (토큰 — 비공개)</files>
  <action>[BLOCKING checkpoint] `npm install @vercel/blob@2.4.0` 실행 후 사용자가 Vercel Dashboard에서 Blob store를 생성하고 BLOB_READ_WRITE_TOKEN을 .env.local(server-only)에 주입한다. 라이브 Vercel 프로비저닝은 코드 자동화 불가 — 사람만 가능. RESEARCH Package Legitimacy Audit: @vercel/blob는 Approved(Vercel 1st-party github.com/vercel/storage, npm 라이브 ~3.99M/주 교차검증). how-to-verify 단계를 따른다.</action>
  <what-built>D-12는 @vercel/blob 클라 직접 업로드를 필수로 못박는다(현재 package.json 미설치). 실 업로드 토큰 발급에는 Vercel Blob store + BLOB_READ_WRITE_TOKEN(코드 자동화 불가, RESEARCH Environment Availability)이 필요하다.</what-built>
  <how-to-verify>
    1. `npm install @vercel/blob@2.4.0` 실행 → `npm ls @vercel/blob`로 2.4.0 확정.
    2. Vercel Dashboard → Storage → Blob → Create Store (프로젝트 연결).
    3. `vercel env pull .env.local` (또는 수동) 로 BLOB_READ_WRITE_TOKEN 주입. **절대 NEXT_PUBLIC_ 접두 금지**(BOT_TOKEN과 동일 server-only 규칙).
    4. `.env.local`에 BLOB_READ_WRITE_TOKEN 존재 확인(값 비공개).
  </how-to-verify>
  <verify>
    <automated>npm ls @vercel/blob 2>/dev/null | grep -q "@vercel/blob@2.4.0" && echo BLOB_INSTALLED</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @vercel/blob`가 2.4.0 표시
    - package.json dependencies에 `@vercel/blob` 추가됨
    - Vercel Blob store 생성 + BLOB_READ_WRITE_TOKEN이 .env.local에 server-only로 존재(NEXT_PUBLIC_ 아님)
  </acceptance_criteria>
  <done>@vercel/blob@2.4.0 설치 + Blob store 생성 + BLOB_READ_WRITE_TOKEN server-only 주입 완료.</done>
  <resume-signal>설치 + store 생성 + 토큰 주입 후 "approved", 또는 이슈 기술. (라이브 Vercel 프로비저닝 — 사람만 가능. 자동 승인 금지.)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: POST /api/blob/upload handleUpload 토큰 라우트 + lib/downscale + offline 테스트</name>
  <files>app/api/blob/upload/route.ts, lib/downscale.ts, tests/api/blob-upload.test.ts</files>
  <read_first>
    - app/api/orders/route.ts (PATTERNS analog — auth-gate-first + try/catch → generic error shape; role-match)
    - lib/auth.ts (requireSession — 세션 게이트)
    - tests/api/orders/route.test.ts (PATTERNS analog — `@vitest-environment node` + vi.hoisted + vi.mock)
    - .planning/phases/03-wait-proof/03-PATTERNS.md §"blob/upload/route.ts" + §"PhotoUploadSlot" (downscale 메커닉)
    - .planning/phases/03-wait-proof/03-RESEARCH.md §"Pattern 3" (handleUpload + onBeforeGenerateToken + onUploadCompleted localhost no-op + Pitfall 3 EXIF)
  </read_first>
  <action>
    blob/upload/route.ts: `import { handleUpload, type HandleUploadBody } from '@vercel/blob/client'`. POST(request): body = await request.json() as HandleUploadBody. try { handleUpload({ body, request, onBeforeGenerateToken: async () => { **const tgId = await requireSession(); if (!tgId) throw new Error('Not authenticated')** (세션 게이트 — 익명 업로드 차단); return { allowedContentTypes: ['image/jpeg','image/png','image/webp'], addRandomSuffix: true, maximumSizeInBytes: 8*1024*1024, tokenPayload: JSON.stringify({tgId}) }; }, onUploadCompleted: async () => { /* no-op: localhost 콜백 미도달 — URL은 04의 POST /api/posts 본문으로 영속, Pitfall 2 */ } }); return Response.json(json); } catch → Response.json({error}, {status:400}). PATTERNS.md §"blob/upload/route.ts" 코드 그대로. Node 런타임(Edge 강제 금지).
    lib/downscale.ts: 클라 사용 헬퍼 downscale(file, maxEdge=1440, quality=0.8): Blob. **createImageBitmap(file, { imageOrientation: 'from-image' })**로 EXIF 회전 정규화(Pitfall 3) → canvas(긴 변 ≤ maxEdge 비율 유지) → canvas.toBlob('image/webp', quality). 04의 PhotoUploadSlot이 import.
    blob-upload.test.ts: orders 테스트 하베스 복제(`@vitest-environment node`). vi.mock('@vercel/blob/client', handleUpload) + vi.mock('@/lib/auth', requireSession). 케이스: 세션 없음 → onBeforeGenerateToken throw('Not authenticated'); 인증됨 → allowedContentTypes(이미지)·maximumSizeInBytes·addRandomSuffix 반환. handleUpload mock이 onBeforeGenerateToken 콜백을 호출하도록 구성.
  </action>
  <verify>
    <automated>npm test -- tests/api/blob-upload.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/api/blob-upload.test.ts` exits 0
    - 세션 없을 때 onBeforeGenerateToken throw 단언 통과; 인증 시 MIME 화이트리스트(image/*만) + addRandomSuffix + maximumSizeInBytes 반환 단언 통과
    - route가 `onUploadCompleted`에서 URL 영속 안 함(no-op) — localhost 콜백 의존 0
    - lib/downscale가 `createImageBitmap` EXIF 정규화 + `toBlob` 사용 (grep: `imageOrientation`)
    - `npm run build` clean
  </acceptance_criteria>
  <done>blob-upload 테스트 green; upload 라우트가 세션 게이트 + MIME/size 강제; downscale가 EXIF 정규화; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/blob/upload (토큰 요청) | 익명/타 사용자 업로드 토큰 탈취 시도 |
| client file → Vercel Blob (직접 PUT) | 거대/악성 파일 업로드 |
| BLOB_READ_WRITE_TOKEN | server-only secret — 노출 시 무제한 업로드 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-08 | Spoofing/Elevation | 익명 Blob 업로드 토큰 발급 | mitigate | onBeforeGenerateToken에서 requireSession 게이트 — 세션 없으면 throw (Pitfall 11) |
| T-3-09 | DoS | 거대/비이미지 파일 업로드 | mitigate | allowedContentTypes 이미지 화이트리스트 + maximumSizeInBytes(8MB) |
| T-3-10 | Info Disclosure | BLOB_READ_WRITE_TOKEN 클라 노출 | mitigate | server-only env, NEXT_PUBLIC_ 금지(BOT_TOKEN 규칙); RW 토큰은 라우트 안에서만 |
| T-3-11 | Tampering | 추측 가능 pathname 충돌/덮어쓰기 | mitigate | addRandomSuffix: true (Pitfall 11) |
| T-3-SC | Tampering | npm 설치(@vercel/blob 2.4.0) | mitigate | RESEARCH Package Legitimacy Audit: Approved(Vercel 1st-party, npm 라이브 교차검증); Task 1 human-verify 게이트 + `npm ls`로 버전 확정 |
</threat_model>

<verification>
- `npm test -- tests/api/blob-upload.test.ts` green
- `npm ls @vercel/blob` = 2.4.0
- `npm run build` + `npx tsc --noEmit` clean
- Manual(VALIDATION.md): 실 store + 토큰 후 실기기 듀얼 사진 업로드 → public URL 반환 + EXIF 방향 정상(04 슬라이스 통합 시)
</verification>

<success_criteria>
@vercel/blob 설치 + Blob store 프로비저닝 + 세션 게이트된 handleUpload 토큰 라우트 + EXIF-정규화 다운스케일 헬퍼가 04 인증 슬라이스에 제공된다.
</success_criteria>

<output>
Create `.planning/phases/03-wait-proof/03-03-SUMMARY.md` when done
</output>

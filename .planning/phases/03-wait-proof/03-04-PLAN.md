---
phase: 03-wait-proof
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - app/api/posts/route.ts
  - app/(mini)/post/[id]/page.tsx
  - app/(mini)/post/[id]/_components/PostClient.tsx
  - app/(mini)/post/[id]/_components/PhotoUploadSlot.tsx
  - tests/api/posts/route.test.ts
  - tests/ui/post-receipt.test.tsx
autonomous: true
requirements: [PROOF-01, PROOF-02, PROOF-03, PROOF-04]
must_haves:
  truths:
    - "인증 화면에 '실제 결제 ₩0 · 가상 주문'이 명시된 가짜 영수증이 orders 스냅샷에서 파생 렌더된다 (PROOF-01)"
    - "사용자가 '시킨 척한 음식'·'실제 내 식단' 사진을 각각 Blob에 업로드한다 (둘 다 필수, PROOF-02)"
    - "사용자가 실제 식단 텍스트 + 한마디(캡션)를 입력하고 zod 검증을 통과한다 (PROOF-03)"
    - "인증 저장 시 사진 URL·캡션·식단·아낀 돈·kcal·연속일·endured가 posts에 박제된다 (PROOF-04)"
    - "POST /api/posts가 소유자·도착·1회(멱등)를 서버 검증하고 endured/streak를 서버 계산한다 (D-09/10/16)"
  artifacts:
    - path: "app/api/posts/route.ts"
      provides: "인증 저장 서버 권위 트랜잭션 (소유자·도착·멱등·재스냅샷·KST 스트릭)"
      exports: ["POST"]
    - path: "app/(mini)/post/[id]/page.tsx"
      provides: "도착+미인증 가드 SC 셸 + 영수증 props (orders 스냅샷)"
    - path: "app/(mini)/post/[id]/_components/PostClient.tsx"
      provides: "영수증 + 듀얼 업로드 + diet/caption + 제출 CC island"
    - path: "app/(mini)/post/[id]/_components/PhotoUploadSlot.tsx"
      provides: "canvas 다운스케일 → @vercel/blob/client upload CC"
    - path: "tests/api/posts/route.test.ts"
      provides: "소유자·도착·멱등·재스냅샷·zod·듀얼URL·세션 단언"
  key_links:
    - from: "posts route"
      to: "orders (id AND tgId)"
      via: "owner-scoped SELECT (IDOR)"
      pattern: "and\\(eq\\(orders\\.id"
    - from: "posts insert"
      to: "posts.orderId UNIQUE"
      via: "onConflictDoNothing({ target: posts.orderId })"
      pattern: "onConflictDoNothing"
    - from: "PhotoUploadSlot"
      to: "/api/blob/upload"
      via: "upload(name, scaled, { handleUploadUrl })"
      pattern: "handleUploadUrl"
    - from: "posts route"
      to: "lib/streak nextStreak"
      via: "computeStreak (직전 endured posts 조회 + 순수함수)"
      pattern: "nextStreak|computeStreak"
---

<objective>
인증 수직 슬라이스(PROOF-01~04): /post/[id] SC 셸(도착+미인증 가드) + PostClient CC(가짜 영수증 + 듀얼 Blob 업로드 + diet/caption) + POST /api/posts 서버 권위 트랜잭션(소유자·도착·1회 멱등 + KST 스트릭 + 재스냅샷 박제) + API/UI 테스트.

Purpose: 코어 루프의 "인증" 절반 — 사용자가 가짜 영수증·듀얼 사진·식단으로 posts에 박제한다. D-09, D-10, D-15, D-16, D-18 서버 권위·멱등·재스냅샷·endured 박제이 Phase 4 피드·5 통계·6 공유의 진실원천이 된다.
Output: posts 라우트 + SC 페이지 + 2 CC 컴포넌트 + posts API 테스트 + receipt RTL 테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wait-proof/03-CONTEXT.md
@.planning/phases/03-wait-proof/03-RESEARCH.md
@.planning/phases/03-wait-proof/03-PATTERNS.md
@app/api/orders/route.ts
@app/(mini)/order/[id]/page.tsx
@app/(mini)/store/[id]/_components/StoreMenu.tsx
@design-reference/screens-flow.jsx
@tests/api/orders/route.test.ts
@tests/ui/cart-payoff.test.tsx
@components/Money.tsx
@components/StatBadge.tsx
@db/schema.ts
@lib/streak.ts
@lib/downscale.ts
@app/api/blob/upload/route.ts
---
NOTE: db/schema.ts posts 테이블 + lib/streak는 03-01에서, lib/downscale + /api/blob/upload는 03-03에서 생성됨 — 그 SUMMARY를 신뢰하고 재구현 금지.
</context>

## Phase Goal

**As a** 식비절감·다이어트 사용자, **I want to** 가짜 영수증(₩0) + 듀얼 사진(시킨 척/실제 식단) + 식단/한마디로 인증을 올리는 것, **so that** 절제 기록이 서버에 박제되어 명예의 전당·통계·공유로 이어진다.

> 이 슬라이스 완료 후: 도착한 주문에서 사용자가 /post/[id]로 들어가 영수증을 확인하고, 듀얼 사진을 업로드하고, 식단/캡션을 입력해 "피드에 올리기"로 posts에 1회 박제한다. (코어 루프 주문→대기→인증이 닫힌다.)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/posts 서버 권위 트랜잭션(소유자·도착·멱등·스트릭·재스냅샷) + 테스트</name>
  <files>app/api/posts/route.ts, tests/api/posts/route.test.ts</files>
  <read_first>
    - app/api/orders/route.ts (PATTERNS analog — exact: auth gate first, authError/badRequest, zod try/catch generic 400, 서버 파생 persisted 값만)
    - app/(mini)/order/[id]/page.tsx (PATTERNS analog — owner-scope and(eq id, eq tgId), line 49)
    - tests/api/orders/route.test.ts (PATTERNS analog — exact 하베스: node env, vi.hoisted, vi.mock db/auth, postJson, insert values 캡처)
    - lib/streak.ts (nextStreak/kstDateKey — computeStreak 래퍼는 여기 라우트가 소유)
    - db/schema.ts (posts 컬럼 + orders.arrivedAt/endured)
    - .planning/phases/03-wait-proof/03-PATTERNS.md §"app/api/posts/route.ts" (bodySchema + 게이트 + 멱등 insert 코드)
    - .planning/phases/03-wait-proof/03-RESEARCH.md §"Pattern 4" + §"Pattern 5" + §"posts body zod"
  </read_first>
  <behavior>
    - posts Test: 비소유 주문(tgId 불일치) → 404, no insert
    - posts Test: order.arrivedAt 없음(미도착) → 거부(400), no insert (D-09 도착 게이트)
    - posts Test: 이미 인증된 주문(onConflictDoNothing 빈 반환) → 409 already_posted (D-10)
    - posts Test: insert values의 restName/items/total/kcal/savedAmount/endured가 **order 행에서 재스냅샷**(본문 아님, D-15)
    - posts Test: diet/caption zod(min1/max) + 듀얼 URL required + Blob 호스트 prefix → 위반 시 400 generic
    - posts Test: 세션 없음 → 401 {error:'auth'}
    - posts Test: streakDay가 서버 계산값(nextStreak) — 본문에서 streak 미수신
  </behavior>
  <action>
    posts/route.ts: orders 라우트 스켈레톤 복제(authError/badRequest 헬퍼). bodySchema = z.object({ orderId int positive, foodPhotoUrl url().regex(BLOB_HOST), dietPhotoUrl url().regex(BLOB_HOST), diet min1 max120, caption min1 max200 }) — BLOB_HOST = /^https:\/\/[a-z0-9-]+\.public\.blob\.vercel-storage\.com\// (A1 — install 후 실 URL로 완화 가능). requireSession 게이트 → try/catch parse → generic 400. **owner-scope SELECT** and(eq(orders.id,orderId),eq(orders.tgId,tgId)); !o → notFound JSON(404). **!o.arrivedAt → badRequest**(도착 게이트, 클라 주장 불신 D-09). computeStreak(tgId, o.endured) 래퍼: db에서 직전 endured=true posts 1건(created_at DESC, tgId) 조회 → lib/streak.nextStreak 적용(D-16/17). insert posts.values({ orderId:o.id, tgId, restName:o.restName, items:o.items, total:o.total, kcal:o.kcal, savedAmount:o.savedAmount, foodPhotoUrl, dietPhotoUrl, diet, caption, streakDay, endured:o.endured! }) — **재스냅샷은 order 행에서(본문 money/kcal 절대 신뢰 금지, T-02 정신)**. .onConflictDoNothing({ target: posts.orderId }).returning({id}). !inserted → 409 already_posted(D-10). PATTERNS.md §"posts/route.ts" 코드 그대로.
    posts/route.test.ts: orders 테스트 하베스 복제. db.select(orders) + db.select(직전 posts) + db.insert(posts).onConflictDoNothing().returning() mock(vi.hoisted). 위 behavior 케이스 전부. insert values 캡처해 재스냅샷=order값, streakDay=서버계산 단언.
  </action>
  <verify>
    <automated>npm test -- tests/api/posts/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/api/posts/route.test.ts` exits 0
    - posts route가 owner-scope `and(eq(orders.id` + `!o.arrivedAt` 도착 게이트 + `onConflictDoNothing({ target: posts.orderId })`
    - insert values의 restName/items/total/kcal/savedAmount/endured가 order 행 파생(본문 아님) 단언 통과
    - 비소유 404 / 미도착 거부 / 중복 409 / 세션 401 / zod 400 단언 통과
    - streakDay가 nextStreak 서버 계산 (본문에서 미수신)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>posts 테스트 green; posts API가 소유자·도착·1회 멱등 + 서버 스트릭 + order 재스냅샷; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: /post/[id] SC 셸 + PostClient/PhotoUploadSlot CC(영수증+듀얼업로드+diet/caption) + receipt RTL 테스트</name>
  <files>app/(mini)/post/[id]/page.tsx, app/(mini)/post/[id]/_components/PostClient.tsx, app/(mini)/post/[id]/_components/PhotoUploadSlot.tsx, tests/ui/post-receipt.test.tsx</files>
  <read_first>
    - app/(mini)/order/[id]/page.tsx (PATTERNS analog — exact: SC 셸, 영수증을 orders 스냅샷에서 렌더 lines 67-127, owner-scope 가드)
    - app/(mini)/store/[id]/_components/StoreMenu.tsx (PATTERNS analog — CC 구조: 'use client', submit + child overlay, TgMainButton, Won/Num)
    - design-reference/screens-flow.jsx (1차 이식 소스 — PostScreen L151-211: 영수증 L162-184, 듀얼 PhotoSlot L186-191, diet/caption L193-199, payoff StatBadge L202-205)
    - tests/ui/cart-payoff.test.tsx (PATTERNS analog — RTL jsdom, next/navigation mock, Won/Num 한글 카피)
    - components/Money.tsx, components/StatBadge.tsx, lib/downscale.ts, app/api/blob/upload/route.ts
    - .planning/phases/03-wait-proof/03-PATTERNS.md §"PostClient" + §"PhotoUploadSlot"
  </read_first>
  <action>
    page.tsx (SC 셸): order/[id]/page.tsx 스캐폴드 복제 — params await, requireSession null→notFound, Number.isInteger, owner-scope SELECT and(eq id, eq tgId). **진입 가드(D-08/10)**: order.arrivedAt 없으면 notFound/redirect(/wait/[id]); 이미 인증된 주문(posts에 orderId 존재)이면 결과/피드로 안내(중복 방지). 영수증 source props를 **orders 스냅샷에서**(D-14 — order/[id] 영수증과 동일, ALL_MENU 재조회 금지) PostClient에 전달(restName, orderNo, createdAt, items, total, savedAmount, kcal). <SubBar title="인증 올리기"/>.
    PostClient.tsx ('use client'): screens-flow PostScreen 이식 — 가짜 영수증(L162-184: zigzag 하단 엣지 + dashed 구분선 + 카피 "＊＊ 안 먹음 인증 영수증 ＊＊"/"강철 절제력"/"실제 결제 ₩0"/"＊ 본 주문은 시키지 않았습니다 ＊") + 듀얼 PhotoUploadSlot ×2(라벨 "시킨 척한 음식 🤤"/"실제 내 식단 🥗", D-11 둘 다 필수) + diet input + caption textarea(inStyle) + payoff StatBadge. **이탈(Pitfall 7)**: 모든 fmtWon/fmtNum inline → <Won>/<Num>; 영수증 값은 SC props(orders 스냅샷)에서. **둘 다 필수 가드(Pitfall 4)**: 두 사진 URL 모두 존재해야 제출 버튼 활성. 제출 → POST /api/posts { orderId, foodPhotoUrl, dietPhotoUrl, diet, caption } → 성공 시 결과/피드 안내. TgMainButton "피드에 올리기".
    PhotoUploadSlot.tsx ('use client'): import { upload, type PutBlobResult } from '@vercel/blob/client' + lib/downscale. 파일 선택 → downscale(file,1440,0.8) → upload(`proof/${crypto.randomUUID()}.webp`, scaled, { access:'public', handleUploadUrl:'/api/blob/upload', contentType:'image/webp' }) → result.url을 부모 PostClient state로 lift. 진행/에러 UI(재량). screens-flow PhotoSlot의 web-component(image-slot)는 사용 금지(D-11 대체).
    post-receipt.test.tsx: cart-payoff 패턴(jsdom, next/navigation mock). 영수증 "실제 결제 ₩0" + 스냅샷 파생 항목이 Won 경유(예: ₩23,000) 렌더 + 듀얼 슬롯 둘 다 없으면 제출 비활성 단언.
  </action>
  <verify>
    <automated>npm test -- tests/ui/post-receipt.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/ui/post-receipt.test.tsx` exits 0
    - 영수증에 "실제 결제 ₩0" + "＊ 본 주문은 시키지 않았습니다 ＊" 카피 존재
    - 영수증/payoff ₩·kcal이 `<Won`/`<Num` 경유 (design inline `fmtWon(` 부재)
    - PhotoUploadSlot이 `handleUploadUrl: '/api/blob/upload'` + lib/downscale 사용 (web-component `image-slot` 부재)
    - 듀얼 사진 둘 다 없으면 제출 비활성 단언 통과(Pitfall 4)
    - page.tsx 도착 가드(arrivedAt) + 중복 인증 안내(D-08/10) + owner-scope SELECT
    - `npm run build` clean
  </acceptance_criteria>
  <done>post-receipt 테스트 green; /post/[id]가 orders-스냅샷 영수증(₩0) + 듀얼 Blob 업로드(둘 다 필수) + diet/caption + /api/posts 제출; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/posts | 본문(URL/diet/caption) 신뢰 경계 밖; money/streak/endured는 본문에서 받지 않음 |
| route id (sequential) → orders/posts SELECT | IDOR 면 |
| client-supplied photo URL | 임의 URL 주입 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-12 | Tampering/IDOR | 남의 주문에 인증 작성 | mitigate | posts API owner-scope SELECT and(eq(orders.id),eq(orders.tgId)) → 404 |
| T-3-13 | Tampering | 도착 안 한 주문 인증 위조 | mitigate | 서버 arrived_at 게이트(!o.arrivedAt → 400) — 클라 주장 불신 (D-09) |
| T-3-14 | Tampering | 중복 인증(스트릭/통계 부풀리기) | mitigate | order_id UNIQUE + onConflictDoNothing → 409 (D-10) |
| T-3-15 | Tampering | 임의 URL 주입(posts 사진) | mitigate | zod .url() + Blob 호스트 prefix regex (A1 — install 후 확정) |
| T-3-16 | Tampering | 클라가 보낸 streak/endured/money 신뢰 | mitigate | streak/endured/money 전부 서버 계산·order 재스냅샷(본문에서 미수신, D-15/16) |
| T-3-17 | Spoofing | 익명 사진 업로드(소비 경로) | mitigate | 03-03 /api/blob/upload 세션 게이트 재사용 |
| T-3-SC | Tampering | npm 설치 | accept | 신규 패키지 없음(03-03이 @vercel/blob 게이트) |
</threat_model>

<verification>
- `npm test -- tests/api/posts/route.test.ts tests/ui/post-receipt.test.tsx` green
- `npm test` 전체 green (phase gate)
- `npm run build` + `npx tsc --noEmit` clean
- Manual(VALIDATION.md): 실 store + 토큰 후 실기기 듀얼 사진 업로드 → public URL + posts 저장 + 피드(Phase 4) 방향 정상
</verification>

<success_criteria>
사용자가 도착한 주문에서 /post/[id] 가짜 영수증(₩0)을 보고 듀얼 사진(둘 다 필수)·식단·캡션을 올려 posts에 1회 박제하며, 서버가 소유자·도착·멱등·스트릭·재스냅샷을 권위 처리한다.
</success_criteria>

<output>
Create `.planning/phases/03-wait-proof/03-04-SUMMARY.md` when done
</output>

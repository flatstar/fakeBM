---
phase: 03-wait-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema.ts
  - lib/streak.ts
  - lib/wait.ts
  - tests/lib/streak.test.ts
  - tests/db/posts-schema.test.ts
autonomous: false
requirements: [WAIT-01, WAIT-02, WAIT-03, WAIT-04, PROOF-01, PROOF-02, PROOF-03, PROOF-04]
must_haves:
  truths:
    - "posts 테이블이 order_id UNIQUE 제약으로 주문당 1회 인증을 DB가 보장한다 (D-10)"
    - "orders가 wait_started_at·wait_deadline·arrived_at·endured 컬럼을 보유한다 (D-03/05)"
    - "KST 자정 경계 스트릭 순수함수가 연속(+1)·끊김(2일+→1)·같은날(유지)·미완주(0)를 정확 계산한다 (D-17)"
    - "라이브 Neon에 DDL이 push되어 posts/orders 신규 스키마가 존재한다"
  artifacts:
    - path: "db/schema.ts"
      provides: "orders 4 nullable 대기/도착 컬럼 + posts 테이블 + Post/NewPost 타입"
      contains: "export const posts"
    - path: "lib/streak.ts"
      provides: "kstDateKey + nextStreak 순수 KST 스트릭 함수"
      exports: ["kstDateKey", "nextStreak"]
    - path: "lib/wait.ts"
      provides: "대기 시간 상수 + deadline 계산 헬퍼 (15~30분)"
    - path: "tests/lib/streak.test.ts"
      provides: "KST 경계/연속/끊김/미완주 스트릭 단언"
    - path: "tests/db/posts-schema.test.ts"
      provides: "orders 4컬럼 + posts 제약 shape-lock"
  key_links:
    - from: "db/schema.ts posts.orderId"
      to: "orders.id"
      via: ".references(() => orders.id).unique()"
      pattern: "references\\(\\(\\) => orders\\.id\\)\\.unique"
    - from: "db/schema.ts posts.items"
      to: "OrderItemSnapshot"
      via: "재사용 (재정의 금지)"
      pattern: "\\$type<OrderItemSnapshot\\[\\]>"
---

<objective>
Phase 3의 공유 테스트 기반(Wave 0): orders에 서버 권위 대기/도착 컬럼 4개를 추가하고, posts 인증 테이블을 신규 생성하며(D-10 order_id UNIQUE 멱등), KST 스트릭 순수함수(D-17)와 대기 상수 헬퍼를 만든다. 두 슬라이스(대기·인증)가 모두 이 테스트된 스키마/함수 위에 빌드되도록 한다.

Purpose: 인증 저장(posts)·도착 판정(orders 컬럼)·스트릭 계산은 두 후속 슬라이스가 공유하는 substrate다. 먼저 shape-lock + 순수함수 테스트로 박아두면 슬라이스 API/UI가 안정 기반 위에서 병렬 진행된다.
Output: db/schema.ts(orders +4, posts new), lib/streak.ts, lib/wait.ts, 두 테스트 파일, 라이브 Neon push.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wait-proof/03-CONTEXT.md
@.planning/phases/03-wait-proof/03-RESEARCH.md
@.planning/phases/03-wait-proof/03-PATTERNS.md
@db/schema.ts
@tests/db/orders-schema.test.ts
@tests/lib/order.test.ts
</context>

## Phase Goal

**As a** 식비절감·다이어트 사용자, **I want to** 가짜 배달 대기를 견뎌 "참기 성공"에 도달하고 가짜 영수증 + 듀얼 사진으로 인증 포스트를 남기는 것, **so that** 절약/칼로리 절제 기록이 서버에 박제되어 이후 피드·통계·공유로 이어진다.

> 이 Wave 0 플랜은 위 user story의 데이터/계산 기반만 깐다(사용자 가시 슬라이스는 02·04). MVP 예외: 검증 계약(VALIDATION.md)이 명시적으로 Wave 0 스키마/순수함수 선행을 허용한다.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: orders 대기 컬럼 + posts 테이블 + lib/streak + lib/wait (테스트 우선)</name>
  <files>db/schema.ts, lib/streak.ts, lib/wait.ts, tests/db/posts-schema.test.ts, tests/lib/streak.test.ts</files>
  <read_first>
    - db/schema.ts (수정 대상: orders 객체 lines 60-85에 컬럼 추가, OrderItemSnapshot lines 36-43 재사용, posts 신규)
    - tests/db/orders-schema.test.ts (PATTERNS analog — exact: 라이브 DB 없이 column .name/.notNull/.primary/.hasDefault 단언)
    - tests/lib/order.test.ts + lib/order.ts (PATTERNS analog — 순수 공유 모듈 + 헤더 주석 컨벤션, role-match)
    - .planning/phases/03-wait-proof/03-PATTERNS.md §"db/schema.ts" + §"lib/streak.ts" (정확한 컬럼/타입/인덱스 코드 + KST +09:00 규칙)
    - .planning/phases/03-wait-proof/03-RESEARCH.md §"Pattern 5: KST 스트릭" + §"Code Examples"
  </read_first>
  <behavior>
    - streak Test: nextStreak(오늘, null, true) === 1 (첫 완주)
    - streak Test: 직전 완주 KST 날짜가 어제 → +1; 2일+ 전 또는 없음 → 1
    - streak Test: 같은 KST 날짜 재인증 → 직전 streakDay 유지
    - streak Test: thisEndured=false → 0 (미완주는 스트릭 끊김, D-17)
    - streak Test: KST 자정 경계 — UTC 15:00(=KST 익일 00:00) 직전/직후가 다른 날짜키 (DST 없음, +09:00 고정)
    - schema Test: orders에 wait_started_at·wait_deadline·arrived_at(nullable timestamptz) + endured(nullable boolean) 존재
    - schema Test: posts.order_id NOT NULL + unique 제약, 모든 재스냅샷/콘텐츠/박제 컬럼 NOT NULL, created_at hasDefault
  </behavior>
  <action>
    db/schema.ts: orders 객체에 4 nullable 컬럼 추가 — waitStartedAt('wait_started_at', timestamptz), waitDeadline('wait_deadline', timestamptz), arrivedAt('arrived_at', timestamptz), endured(boolean('endured')). drizzle-orm/pg-core import 블록에 boolean 추가. posts 테이블 신규 생성: id generatedAlwaysAsIdentity PK, orderId integer NOT NULL .references(()=>orders.id).unique() (D-10 멱등 타겟), tgId bigint mode:number .references(()=>users.tgId), restName text, items jsonb $type<OrderItemSnapshot[]> (lines 36-43 타입 재사용 — 재정의 금지), total/kcal/savedAmount integer, foodPhotoUrl/dietPhotoUrl/caption/diet text, streakDay integer, endured boolean, createdAt timestamptz defaultNow — restName 이후 전부 notNull. 인덱스 posts_created_idx(createdAt), posts_tg_created_idx(tgId, createdAt). Post/NewPost 타입 export. PATTERNS.md §"db/schema.ts"의 코드 블록을 그대로 따른다.
    lib/streak.ts: 순수 의존성 0 모듈(lib/order.ts 헤더 주석 컨벤션 모방). export kstDateKey(d: Date): string — d + 9h 후 toISOString().slice(0,10) (KST 'YYYY-MM-DD'). export nextStreak(today, prevEnduredPost: {createdAt; streakDay}|null, thisEndured): number — RESEARCH Pattern 5 로직(미완주 0, 없음 1, 같은날 유지, diff 1 → +1, else 1). DB 조회 래퍼(computeStreak)는 여기 두지 말 것 — posts API(04)가 소유.
    lib/wait.ts: 대기 시간 상수 export (WAIT_MINUTES 또는 WAIT_MS, 15~30 범위 내 — 20분 권장 A4) + deadline 계산 헬퍼. start 라우트/SC가 interval '20 minutes' SQL 또는 이 상수를 소비.
  </action>
  <verify>
    <automated>npm test -- tests/lib/streak.test.ts tests/db/posts-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/lib/streak.test.ts tests/db/posts-schema.test.ts` exits 0
    - db/schema.ts에 `export const posts` 존재 + `posts.orderId`가 `.references(() => orders.id).unique()` (grep: `references\(\(\) => orders\.id\)\.unique`)
    - posts.items가 `$type<OrderItemSnapshot[]>` (OrderItemSnapshot 재정의 없음 — 단일 정의)
    - orders 객체에 wait_started_at·wait_deadline·arrived_at·endured 컬럼 string 존재
    - lib/streak.ts가 kstDateKey·nextStreak export, 외부 import(date-fns 등) 0
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>streak/posts-schema 테스트 green; orders 4컬럼 + posts(order_id unique) 정의; lib/streak·lib/wait 존재; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] drizzle-kit push to live Neon (DDL 적용)</name>
  <files>db/schema.ts (DDL source — 편집 없음; push만)</files>
  <action>[BLOCKING checkpoint — 자동 코드 변경 없음] Task 1의 db/schema.ts 변경을 라이브 Neon에 `npm run db:push`(drizzle-kit push, DIRECT_URL)로 적용한다. drizzle.config.ts 변경 금지(Pitfall 6 pooled URL 금지). Neon 라이브 credential이 필요하므로 Phase 1/2처럼 사람이 실행·승인하는 게이트다. how-to-verify 단계를 따른다. build/tsc는 push 없이도 통과하므로(타입은 스키마 config에서 옴) 이 push를 건너뛰면 false-positive 검증 상태가 된다.</action>
  <what-built>db/schema.ts에 orders 4 nullable 컬럼 + posts 테이블(order_id UNIQUE + 2 인덱스)을 정의했다. 라이브 Neon에 DDL을 적용해야 후속 API(arrive·posts)가 실제 컬럼/테이블에 쓸 수 있다.</what-built>
  <how-to-verify>
    1. `.env.local`에 DIRECT_URL(non-pooled)이 있는지 확인 (02-03에서 프로비저닝됨).
    2. `npm run db:push` 실행 (drizzle-kit push, DIRECT_URL — drizzle.config.ts 변경 금지, Pitfall 6 pooled URL 금지).
    3. push 출력에 posts 테이블 생성 + orders 4 컬럼 추가가 보이는지 확인. order_id UNIQUE 제약·posts 인덱스 생성 확인.
    4. 기존 orders 행은 nullable 추가라 백필 불필요(RESEARCH A5).
  </how-to-verify>
  <verify>
    <automated>npm run db:push 2>&1 | grep -qiE "(posts|changes applied|no changes)" && echo PUSH_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:push`가 에러 없이 완료 (DDL hang/충돌 없음)
    - posts 테이블 + orders 4 컬럼이 라이브 Neon에 존재
  </acceptance_criteria>
  <done>posts 테이블 + orders 4 컬럼이 라이브 Neon에 push 완료; order_id UNIQUE + 인덱스 생성 확인.</done>
  <resume-signal>push 성공 후 "approved" 또는 push 출력/이슈 기술. (Neon 라이브 credential 필요 — Phase 1/2 처럼 사람 게이트.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema → live DB (DDL) | drizzle push가 라이브 Neon 구조 변경 — credential 경유 |
| (이 플랜은 런타임 요청 경계 없음 — 스키마/순수함수만) | 후속 02/04가 요청 경계 도입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | posts 중복 인증(스트릭/통계 부풀리기) | mitigate | posts.order_id `.unique()` 제약 — DB 레벨 멱등 substrate (04의 onConflictDoNothing 타겟) |
| T-3-02 | Tampering | KST 경계 오계산으로 스트릭 위조/오류 | mitigate | lib/streak +09:00 고정 순수함수 + 자정 경계 테스트(DST 없음) |
| T-3-03 | Tampering | DDL을 pooled URL로 실행해 PgBouncer 충돌 | mitigate | drizzle.config.ts DIRECT_URL 유지(Pitfall 6) — 변경 금지 |
| T-3-SC | Tampering | npm 설치(이 플랜 신규 패키지 없음) | accept | 이 플랜은 install 없음; @vercel/blob은 03-03이 게이트 |
</threat_model>

<verification>
- `npm test -- tests/lib/streak.test.ts tests/db/posts-schema.test.ts` green
- `npm run db:push` 적용 완료 (Task 2)
- `npx tsc --noEmit` + `npm run build` clean
</verification>

<success_criteria>
posts 테이블(order_id UNIQUE) + orders 4 대기/도착 컬럼이 정의·테스트·라이브 push 되고, KST 스트릭 순수함수가 경계/끊김/미완주를 정확 계산한다.
</success_criteria>

<output>
Create `.planning/phases/03-wait-proof/03-01-SUMMARY.md` when done
</output>

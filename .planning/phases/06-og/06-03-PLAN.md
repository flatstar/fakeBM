---
phase: 06-og
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - components/ShareCard.tsx
  - app/share/[id]/page.tsx
  - app/share/[id]/opengraph-image.tsx
  - lib/share.ts
  - tests/api/share-page.test.ts
  - tests/api/og-image.test.ts
  - tests/auth/public-open.test.ts
autonomous: true
requirements: [SHARE-02, SHARE-03]
user_setup: []

must_haves:
  truths:
    - "GET /share/[id] for a valid id renders the card SSR with NO session cookie (no redirect to reauth)"
    - "GET /share/[id] for an unknown id returns notFound (404, no data leak)"
    - "generateMetadata emits openGraph.images (og:image) for a valid id"
    - "app/share/[id]/opengraph-image returns content-type image/png"
    - "The public card shows only the wordmark — no real name/handle"
  artifacts:
    - path: "components/ShareCard.tsx"
      provides: "DOM card body (dark gradient, <Won>/<Num>, BM/Pretendard) reused by S3 page + S2 sheet"
      contains: "ShareCard"
    - path: "app/share/[id]/page.tsx"
      provides: "public SSR card page + generateMetadata (no (mini) shell, no auth)"
      exports: ["default", "generateMetadata"]
    - path: "app/share/[id]/opengraph-image.tsx"
      provides: "next/og Satori PNG (runtime nodejs, subset fonts, flex-only)"
      exports: ["default", "runtime", "size", "contentType", "alt"]
    - path: "lib/share.ts"
      provides: "getShare(id) snapshot reader + monthLabel/glyph helpers"
  key_links:
    - from: "app/share/[id]/page.tsx generateMetadata"
      to: "/share/[id]/opengraph-image"
      via: "openGraph.images"
      pattern: "openGraph"
    - from: "app/share/[id]/opengraph-image.tsx"
      to: "assets/og/*-ogsubset.ttf"
      via: "fs.readFile(join(process.cwd(), ...))"
      pattern: "readFile\\(join\\(process\\.cwd\\(\\)"
    - from: "app/share/[id]/page.tsx"
      to: "shares table"
      via: "getShare(id) → SELECT ... WHERE id"
      pattern: "notFound"
---

<objective>
Render the share card on its two public surfaces: the **S1 OG PNG** (`opengraph-image.tsx`, Satori, Node runtime, embedded subset fonts, flex-only) and the **S3 public SSR page** (`/share/[id]/page.tsx`, no auth, no Mini App shell, `generateMetadata` for crawler preview). Both read the FROZEN snapshot from `shares` (no live re-aggregation, D-01). A shared `ShareCard` DOM component keeps S3 (and S2 later) pixel-identical.

Purpose: SHARE-02 (server OG with un-broken Korean) + SHARE-03 (public link opens outside Telegram with a crawler preview). The opaque-id public read is enumeration-safe (T-06-01); the snapshot is self-contained so no name/handle leaks (T-06-02/D-09).
Output: `ShareCard.tsx`, `/share/[id]/page.tsx` (+ generateMetadata), `/share/[id]/opengraph-image.tsx`, `lib/share.ts`, Wave-0 tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-og/06-RESEARCH.md
@.planning/phases/06-og/06-PATTERNS.md
@.planning/phases/06-og/06-UI-SPEC.md
@app/share/page.tsx
@design-reference/screens-social.jsx
</context>

<artifacts_this_phase_produces>
- `components/ShareCard.tsx` (DOM card body — reused by S3 page + S2 ShareSheet in 06-04)
- `app/share/[id]/page.tsx` (public SSR card + generateMetadata og:image)
- `app/share/[id]/opengraph-image.tsx` (Satori PNG, subset fonts)
- `lib/share.ts` (getShare reader + KST monthLabel helper)
Consumes 06-01 (shares table, subset fonts) + 06-02 (created ids). The opengraph-image is reached via the og:image meta and via the 저장 download in 06-04.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/share.ts (getShare reader) + ShareCard DOM body</name>
  <files>lib/share.ts, components/ShareCard.tsx</files>
  <read_first>
    - design-reference/screens-social.jsx §ShareCard L184–208 (pixel card: dark gradient container radius 24, lead line, coral hero ₩, kcal line, 7 weekly mini-bars, 3-stat top-bordered flex row, footer credit, 🔥 watermark).
    - .planning/phases/06-og/06-UI-SPEC.md §"Component Inventory" (ShareCard contract) + §Typography (4-step DOM scale {44,18,14,11}) + §Color (card-local dark palette, coral accents reserved list) + §Copywriting Contract (exact copy strings).
    - components/Money.tsx `<Won>`/`<Num>` + lib/format.ts (₩ HARD RULE) — app/(mini)/stats/page.tsx ~L95–106 for `<Won>` usage.
    - db/schema.ts `Share` type (from 06-01); lib/db.ts for the db handle; lib/stats.ts kstMonthBounds for the monthLabel helper.
  </read_first>
  <behavior>
    - getShare(id) returns the frozen Share row or null (SELECT shares WHERE id, Drizzle eq, parameterized) — null for unknown id.
    - ShareCard renders ALL money/kcal/counts through `<Won>`/`<Num>` (DOM HARD RULE) — never routes ₩ through a BM font.
    - ShareCard renders ONLY the wordmark — no firstName/handle anywhere (D-09).
    - Weekly mini-bars: 7 bars, max-day bar is coral, the rest translucent white; bar height = `Math.max(5, (v/maxDay)*56)`, `maxDay = Math.max(...byDay, 1)`.
    - topMenu === null → the "최다 적" caption is suppressed or "—", never blank/NaN.
  </behavior>
  <action>Create lib/share.ts: a pure-ish `getShare(id: string)` that SELECTs the shares row by id (Drizzle `eq(shares.id, id)`, parameterized — V5 input validation; returns null on miss), plus a KST monthLabel formatter (reuse kstMonthBounds, `YYYY.MM`) and the fixed Korean glyph-set constant (so 06-01's font task and this stay in sync). Keep DB read here; keep the formatter pure. Then create components/ShareCard.tsx — the DOM card body reused by S3 (this plan) and S2 (06-04). Port design-reference §ShareCard L184–208 (the D-06 pixel-canonical card) as a real component using `var(--color-*)` tokens, BMHanna/BMDohyeon/Pretendard fonts per the UI-SPEC font role table, and `<Won>`/`<Num>` for EVERY numeric (HARD RULE). Container: radius 24, dark gradient `linear-gradient(160deg,#2a1d14,#3d2a1c 60%,#52331f)`, shadow `0 24px 50px rgba(0,0,0,.5)`, padding 24. Compose: wordmark "배달의 만족" + "{monthLabel} 리포트" → lead "이번 달, 시켜놓고 참아서" → coral hero `<Won value={savedMonth}/>` → "아끼고 <Num value={kcalTotal}/>kcal 덜 먹었어요" → 7 weekly mini-bars (max-day coral, rest `rgba(255,255,255,.22)`, height `Math.max(5,(v/maxDay)*56)`, maxDay `Math.max(...byDay,1)`, radius 5) → top-bordered 3-stat flex row (🔥{streak}일 연속 / ✋{resisted}번 참음 / 🏆{topMenu} 최다 적; topMenu null → "—") → footer "＠배달의_만족 · 참아서 만든 기록" → 🔥 watermark. NO real name/handle (D-09). Accept the frozen Share snapshot as props. Use the exact copy from the UI-SPEC Copywriting Contract.</action>
  <verify>
    <automated>npx vitest run tests/api/share-page.test.ts -t card</automated>
  </verify>
  <acceptance_criteria>
    - getShare returns the Share row for a known id and null for an unknown id.
    - ShareCard renders the wordmark and no firstName/handle (assert no PII text path).
    - All numerics render via `<Won>`/`<Num>` (no raw `₩` through a BM-font span).
    - topMenu null renders "—" (not blank/NaN); `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>lib/share.ts getShare reader + a reusable wordmark-only ShareCard DOM body with HARD-RULE money rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: public /share/[id] SSR page + generateMetadata + public-open test</name>
  <files>app/share/[id]/page.tsx, tests/api/share-page.test.ts, tests/auth/public-open.test.ts</files>
  <read_first>
    - app/share/page.tsx (FULL — the no-auth public boundary stub; this route extends it).
    - proxy.ts L29 matcher (already excludes `share(?:/|$)` — NO proxy change).
    - app/(mini)/stats/page.tsx ~L43–44 — the `(mini)` guard pattern that MUST NOT be copied here (this route is intentionally public, D-08).
    - .planning/phases/06-og/06-RESEARCH.md L191–219 (Pattern 2: generateMetadata + page) + Pitfall 3 (params is a Promise in Next 16).
    - .planning/phases/06-og/06-PATTERNS.md §"app/share/[id]/page.tsx".
    - tests/auth/public-open.test.ts (existing — extend it for /share/[id]).
    - components/ShareCard.tsx + lib/share.ts (Task 1).
  </read_first>
  <behavior>
    - GET /share/[id] with a valid id and NO session cookie → 200 SSR card (NOT a redirect to /?reauth=1).
    - GET /share/[id] with an unknown id → notFound() (404), no data leak.
    - generateMetadata for a valid id returns openGraph.images containing the og:image URL (`share.ogUrl ?? '/share/[id]/opengraph-image'`) + the locked title.
    - The page renders NO (mini) shell — no TgHeader, no BottomNav.
    - params is awaited (Next 16 Promise).
  </behavior>
  <action>Create app/share/[id]/page.tsx — public SSR, NO auth. Do NOT call requireSession/redirect (the `(mini)` guard from stats/page.tsx must NOT be copied — D-08; proxy already excludes share). `export default async function SharePage({ params }: { params: Promise<{ id: string }> })`: `const { id } = await params;` (Pitfall 3) → `const share = await getShare(id);` → `if (!share) notFound();` (import from next/navigation) → render `<ShareCard {...share} />` centered on a neutral standalone page (NO TgHeader/BottomNav/(mini) shell — it must open in any browser outside Telegram). Add `export async function generateMetadata({ params })`: await params, getShare(id); if missing return `{}`; else return `{ title: '배달의 만족 · 참아서 만든 기록', openGraph: { title: '배달의 만족 리포트', images: [share.ogUrl ?? \`/share/${id}/opengraph-image\`] } }` (the colocated opengraph-image also auto-emits og:image; this explicit set is belt-and-braces + lets a Blob-cached ogUrl win). Then write tests/api/share-page.test.ts (`// @vitest-environment node`): mock getShare → (a) valid id: generateMetadata returns openGraph.images non-empty (`-t metadata`); (b) unknown id: page calls notFound (`-t notfound`). Extend tests/auth/public-open.test.ts with a `-t share-id` case asserting /share/[id] is reachable with no cookie (no reauth redirect) — mirror the existing /share open-reachability assertion.</action>
  <verify>
    <automated>npx vitest run tests/api/share-page.test.ts tests/auth/public-open.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - generateMetadata emits a non-empty openGraph.images for a valid id; returns `{}` for unknown.
    - Page calls notFound() for an unknown id.
    - public-open test: /share/[id] reachable with no session (no /?reauth=1 redirect).
    - No requireSession/(mini) shell import in page.tsx; params awaited; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Public no-auth SSR /share/[id] page with crawler-preview metadata; unknown id → 404; reachable outside Telegram.</done>
</task>

<task type="auto">
  <name>Task 3: opengraph-image.tsx (next/og Satori PNG) + png/font-size test</name>
  <files>app/share/[id]/opengraph-image.tsx, tests/api/og-image.test.ts</files>
  <read_first>
    - .planning/phases/06-og/06-RESEARCH.md §Pattern 1 L148–189 (the verbatim OG skeleton) + Pitfalls 1 (500KB cap), 2 (flexbox-only / NO grid), 3 (params Promise), 4 (₩ via Pretendard span), 5 (process.cwd() font path).
    - .planning/phases/06-og/06-UI-SPEC.md §"Component Inventory" (OGImage contract) + §Typography S1 derivation {88,36,28,22} + §Color "S1 OG accent literal" (hardcode coral #FF5A33 + gradient literal — no var()).
    - design-reference/screens-social.jsx §ShareCard L186–219 (the flex layout to port; it is ALREADY flex).
    - app/api/blob/upload/route.ts ~L20–22 (Node-runtime rationale; do NOT force Edge).
    - app/fonts.ts ~L8–19 (BM font source paths) + assets/og/*-ogsubset.ttf (from 06-01).
    - lib/share.ts getShare (Task 1).
    - NO repo analog for next/og — use RESEARCH Pattern 1 as the skeleton.
  </read_first>
  <behavior>
    - The default export returns an ImageResponse → content-type image/png, 1200×630.
    - runtime === 'nodejs'; size === {width:1200,height:630}; contentType === 'image/png'; alt === '배달의 만족 리포트'.
    - Fonts are read from assets/og/*-ogsubset.ttf via fs.readFile(join(process.cwd(), ...)) and embedded.
    - Layout uses display:flex throughout — NO display:grid (Satori unsupported).
    - The ₩ hero amount renders in a span with fontFamily 'Pretendard' (the subset with ₩); Korean headline/labels in 'BMDisplay'.
    - No name/handle (D-09); coral + gradient are literal hex (Satori can't resolve var()).
    - Unknown id snapshot → graceful (the S3 page already 404s; the OG may return a minimal/empty response without throwing).
  </behavior>
  <action>Create app/share/[id]/opengraph-image.tsx per RESEARCH Pattern 1. Exports: `export const runtime = 'nodejs'` (D-04/CLAUDE.md — NEVER Edge; fs.readFile needs Node), `export const alt = '배달의 만족 리포트'`, `export const size = { width: 1200, height: 630 }`, `export const contentType = 'image/png'`. Default `async function Image({ params }: { params: Promise<{ id: string }> })`: `const { id } = await params;` (Pitfall 3) → `getShare(id)` (the frozen snapshot). Load both fonts: `const display = await readFile(join(process.cwd(), 'assets/og/BMDohyeon-ogsubset.ttf'))` and `const num = await readFile(join(process.cwd(), 'assets/og/Pretendard-ogsubset.ttf'))` (Pitfall 5 — always process.cwd()). Return `new ImageResponse(<flex JSX>, { ...size, fonts: [{ name:'BMDisplay', data:display, weight:800, style:'normal' }, { name:'Pretendard', data:num, weight:700, style:'normal' }] })`. JSX is the ShareCard composition at the S1 ~2× type scale {88,36,28,22}, FLEX-ONLY (port the design's flex rows L190/L204/L207 — do NOT use gridTemplateColumns like stats/page.tsx tiles, Pitfall 2): dark gradient literal `linear-gradient(160deg,#2a1d14,#3d2a1c 60%,#52331f)`, padding 64, coral `#FF5A33` literal for the hero amount + max bar (Satori can't read var(), UI-SPEC "S1 OG accent literal"). The ₩ amount text MUST be in a nested `<span style={{ fontFamily:'Pretendard', fontVariantNumeric:'tabular-nums' }}>` using raw `fmtWon(savedMonth)` (NOT `<Won>` — Satori can't host it; ₩ stays in the Pretendard subset, Pitfall 4); likewise wrap kcal digits + the stat numerics in Pretendard spans. Korean headline/wordmark/labels use fontFamily 'BMDisplay'. NO name/handle (D-09). Blob cache (D-05) is OPTIONAL/deferred per RESEARCH O-3 — ship on-demand + Next's own caching first; leave shares.ogUrl null (the column exists for a later add with no migration). Then write tests/api/og-image.test.ts (`// @vitest-environment node`): mock getShare + node:fs/promises readFile (return a small buffer); invoke the default export and assert the returned Response/ImageResponse has content-type 'image/png' (`-t png`); add a `-t fontsize` case asserting both assets/og/*-ogsubset.ttf real file sizes are < 500KB (the cap guard). NOTE the manual-only visual check (한글/₩ correctness) belongs to /gsd-verify-work after a real Vercel deploy — it is NOT automatable here.</action>
  <verify>
    <automated>npx vitest run tests/api/og-image.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The default export yields content-type image/png (1200×630).
    - runtime/size/contentType/alt exports match the contract.
    - Fonts loaded via `readFile(join(process.cwd(), 'assets/og/...'))`; both files asserted < 500KB.
    - No `display: grid` in the OG JSX; ₩ amount in a Pretendard-font span; coral/gradient are literal hex.
    - `npx tsc --noEmit` and `npm run build` clean.
  </acceptance_criteria>
  <done>next/og Satori PNG route: Node runtime, embedded subset fonts, flex-only layout, ₩ via Pretendard; png/200 + font-size tested. Visual 한글/₩ correctness flagged manual-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public crawler/browser → /share/[id] + /opengraph-image | Un-authed public read; the only key is the opaque shares.id |
| [id] route param → DB | Used solely as a parameterized Drizzle lookup key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | /share/[id] enumeration | mitigate | id is the opaque randomUUID from 06-01/06-02 — not sequential; an unknown id → notFound() (no existence oracle beyond 404) |
| T-06-02 | Information Disclosure | PII leak on the public card/page | mitigate | The snapshot is self-contained (no users join); ShareCard + page render the wordmark only, no firstName/handle (D-09); Task 1 acceptance asserts no PII path |
| T-06-07 | Tampering | untrusted host in the share/og URL | mitigate | og:image is same-origin `/share/[id]/opengraph-image`; the PNG is fully server-generated; no client-supplied host is used |
| T-06-08 | Information Disclosure | [id] param injection | mitigate | [id] used only as a parameterized Drizzle `eq` lookup (V5); never interpolated into SQL or a filesystem path |
| T-06-09 | Elevation | public route accidentally inside the auth guard / leaking private data | mitigate | proxy matcher already excludes `share`; page.tsx does NOT import requireSession or the (mini) shell; the read joins no live posts/users |
</threat_model>

<verification>
- `npx vitest run tests/api/share-page.test.ts tests/auth/public-open.test.ts tests/api/og-image.test.ts` green.
- /share/[id] reachable with no session; unknown id → notFound; generateMetadata emits og:image.
- OG route returns image/png; both subset fonts < 500KB; no grid in OG JSX.
- `npx tsc --noEmit` and `npm run build` clean.
- MANUAL (deferred to /gsd-verify-work after a real Vercel deploy): the OG PNG renders Korean + ₩ with no 깨짐; /share/[id] opens outside Telegram with a working crawler card preview.
</verification>

<success_criteria>
- SHARE-02: server OG PNG generated with embedded subset fonts (visual correctness manual-verified post-deploy).
- SHARE-03: public /share/[id] opens SSR outside Telegram with a crawler-preview og:image; unknown id 404s; no PII.
- S3 page + S1 OG read the same frozen snapshot (D-01); the DOM ShareCard is reusable by 06-04's S2 sheet.
</success_criteria>

<output>
Create `.planning/phases/06-og/06-03-SUMMARY.md` when done.
</output>

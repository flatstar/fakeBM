---
phase: 01-db
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - vitest.config.ts
  - app/layout.tsx
  - app/globals.css
  - app/fonts.ts
  - app/fonts/BMHannaPro.ttf
  - app/fonts/BMDohyeon.ttf
  - app/fonts/BMJua.ttf
  - db/schema.ts
  - drizzle.config.ts
  - lib/db.ts
  - tests/fixtures/initdata.ts
  - tests/db/schema.test.ts
  - .env.local.example
autonomous: false
requirements: [AUTH-01]
user_setup:
  - service: neon
    why: "Postgres for users table read/write (AUTH-01, D-06 theme persistence)"
    env_vars:
      - name: DATABASE_URL
        source: "Neon (Vercel Marketplace) → connection string → POOLED (-pooler) — runtime queries"
      - name: DIRECT_URL
        source: "Neon → connection string → DIRECT (non-pooled) — drizzle-kit migrations/push only"
  - service: telegram-botfather
    why: "BOT_TOKEN for initData HMAC validation (consumed in plan 02; provision now)"
    env_vars:
      - name: BOT_TOKEN
        source: "Telegram @BotFather → /newbot → API token (server-only, never NEXT_PUBLIC_)"
      - name: SESSION_SECRET
        source: "Generate locally: openssl rand -base64 32 (server-only jose signing key)"

must_haves:
  truths:
    - "Project builds (npx next build) and lint passes on a Next 16 App Router scaffold"
    - "npx vitest run executes against a real vitest.config.ts (test infra live)"
    - "The users table exists in Neon with tg_id PK, theme enum (coral|mint, default coral), created_at"
    - "Coral design tokens and BM/Pretendard fonts resolve as Tailwind v4 @theme utilities"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest + @vitejs/plugin-react + jsdom test runner config"
      contains: "jsdom"
    - path: "db/schema.ts"
      provides: "Drizzle users table (D-06 theme column)"
      contains: "pgTable('users'"
    - path: "lib/db.ts"
      provides: "Neon HTTP driver + drizzle client + upsertUser DAL"
      exports: ["db", "upsertUser"]
    - path: "app/globals.css"
      provides: "Tailwind v4 @theme coral tokens + @theme inline font chain + [data-theme=mint] swap + keyframes"
      contains: "@theme"
    - path: "app/fonts.ts"
      provides: "next/font/local BM font variables"
      contains: "localFont"
    - path: "tests/fixtures/initdata.ts"
      provides: "signed + forged + expired initData fixtures (no live Telegram client)"
      exports: ["validInitData", "forgedInitData", "expiredInitData", "FIXTURE_BOT_TOKEN"]
  key_links:
    - from: "app/layout.tsx"
      to: "app/fonts.ts"
      via: "html className with font variables"
      pattern: "bmHanna\\.variable"
    - from: "lib/db.ts"
      to: "db/schema.ts"
      via: "drizzle query against users table"
      pattern: "users"
    - from: "app/globals.css"
      to: "app/fonts.ts"
      via: "@theme inline --font-* chain"
      pattern: "@theme inline"
---

<objective>
Stand up the walking-skeleton foundation: scaffold Next 16 App Router + Tailwind v4, install the locked stack, wire Vitest test infra + initData fixtures (Wave 0), port the coral design tokens and BM/Pretendard fonts, define the Drizzle `users` schema + Neon HTTP client, and push the schema to Neon so a live `users` table exists.

Purpose: Every later slice (auth, shell, deploy) sits on this. The DB smoke test in plan 02 (users upsert, AUTH-01) requires a live `users` table — this plan creates it and proves the test runner works.
Output: A building, lint-clean Next 16 project; `vitest.config.ts` + `tests/fixtures/initdata.ts`; coral `@theme` tokens + self-hosted BM fonts; `db/schema.ts` + `lib/db.ts` + `drizzle.config.ts`; a pushed `users` table in Neon.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db/01-RESEARCH.md
@.planning/phases/01-db/01-PATTERNS.md
@.planning/phases/01-db/01-UI-SPEC.md
@.planning/phases/01-db/01-VALIDATION.md
@.planning/phases/01-db/01-SKELETON.md
@design-reference/배달의 만족.html
@design-reference/fonts/
</context>

<artifacts_this_phase_produces>
This plan creates (exclude from drift verification): `db/schema.ts` (`users` table: `tgId`/`tg_id` bigint PK, `username`, `firstName`/`first_name`, `theme` enum coral|mint default coral, `createdAt`/`created_at`), `lib/db.ts` (`db`, `upsertUser`), `app/fonts.ts` (`bmHanna`, `bmDohyeon`, `bmJua`), `app/globals.css` (`@theme` coral tokens, `@theme inline` font chain, `[data-theme="mint"]`, `fadeUp`/`fadeIn`/`confFall` keyframes, `.app-scroll`), `app/layout.tsx`, `drizzle.config.ts`, `vitest.config.ts`, `tests/fixtures/initdata.ts` (`validInitData`, `forgedInitData`, `expiredInitData`, `FIXTURE_BOT_TOKEN`), `tests/db/schema.test.ts`. Env var names: `DATABASE_URL`, `DIRECT_URL`, `BOT_TOKEN`, `SESSION_SECRET`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next 16 App Router + Tailwind v4, install locked stack, wire Vitest</name>
  <read_first>
    - .planning/phases/01-db/01-RESEARCH.md §Standard Stack (locked versions + install block), §Validation Architecture (Wave 0 framework install), §Package Legitimacy Audit
    - .planning/phases/01-db/01-VALIDATION.md §Test Infrastructure, §Wave 0 Requirements
  </read_first>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, vitest.config.ts, .env.local.example</files>
  <action>
    Scaffold a Next 16 App Router + TypeScript + Tailwind v4 project at the repo root with `npx create-next-app@latest . --typescript --tailwind --app --eslint --no-src-dir --import-alias "@/*"` (do not create a `src/` dir; route group + lib layout per RESEARCH Recommended Project Structure). Pin the locked versions from RESEARCH §Standard Stack: `next@16.2.7`, `react@19.2.7`, `react-dom@19.2.7`, `tailwindcss@4.3.0`, `@tailwindcss/postcss@4.3.0`. Install runtime deps `drizzle-orm@0.45.2`, `@neondatabase/serverless@1.1.0`, `@telegram-apps/sdk-react@3.3.9`, `@telegram-apps/init-data-node@2.0.10`, `jose@6.2.3`; dev deps `drizzle-kit@0.31.10`, `vitest`, `@vitejs/plugin-react`, `@testing-library/react`, `jsdom`. For zod: pin `zod@^3.24` (lock v3 per RESEARCH Open Question 1 — lower migration friction, STACK-aligned) and `drizzle-zod` compatible with zod v3; record the v3-vs-v4 decision in the SUMMARY. Create `vitest.config.ts` using `@vitejs/plugin-react` with `environment: 'jsdom'`, `globals: true`, and the `@/*` path alias mirroring tsconfig — NO watch flags. Create `.env.local.example` documenting server-only vars `DATABASE_URL` (pooled), `DIRECT_URL` (direct), `BOT_TOKEN`, `SESSION_SECRET` with a comment that NONE may carry a `NEXT_PUBLIC_` prefix. Confirm the installed `@telegram-apps/sdk-react@3.3.9` export surface (`useRawInitData`, `init`, `miniApp`, `viewport`, `themeParams`, `initData`, `mockTelegramEnv`) against `node_modules` types and note any drift in the SUMMARY (RESEARCH Assumption A2).
  </action>
  <verify>
    <automated>npx next build && npx eslint . && npx vitest run --reporter=basic; test -f vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `next@16.2.7`, `tailwindcss@4.3.0`, `drizzle-orm@0.45.2`, `@neondatabase/serverless@1.1.0`, `@telegram-apps/sdk-react@3.3.9`, `@telegram-apps/init-data-node@2.0.10`, `jose@6.2.3`, and devDependencies include `drizzle-kit@0.31.10`, `vitest`, `jsdom`
    - `vitest.config.ts` exists, sets `environment: 'jsdom'`, and `npx vitest run` exits 0 (zero or more tests, no config error)
    - `npx next build` exits 0 and `npx eslint .` exits 0
    - `.env.local.example` lists `DATABASE_URL`, `DIRECT_URL`, `BOT_TOKEN`, `SESSION_SECRET` and contains no `NEXT_PUBLIC_` secret
    - SUMMARY records the zod v3 lock decision and the confirmed `@telegram-apps/sdk-react@3.3.9` export names
  </acceptance_criteria>
  <done>Next 16 + Tailwind v4 project builds and lints; vitest runs against a real config; locked stack installed; env example documents server-only vars.</done>
</task>

<task type="auto">
  <name>Task 2: Port coral design tokens + BM/Pretendard fonts (Tailwind v4 @theme + next/font/local)</name>
  <read_first>
    - .planning/phases/01-db/01-RESEARCH.md §Pattern 5 (Tailwind v4 @theme / @theme inline font chain, app/fonts.ts excerpt, money/number HARD RULE)
    - .planning/phases/01-db/01-PATTERNS.md `app/globals.css` + `app/fonts.ts` + `app/layout.tsx` assignments (token block, @font-face weight ranges, keyframes, theme swap)
    - .planning/phases/01-db/01-UI-SPEC.md §Color (full LOCKED token table), §Font role contract, §Theme mechanism (mint palette LOCKED), §Animations
    - design-reference/배달의 만족.html (`:root` lines 17–28, `@font-face` lines 13–15, `@keyframes` lines 37–39, `.app-scroll` lines 34–35)
    - design-reference/fonts/ (BMHannaPro.ttf, BMDohyeon.ttf, BMJua.ttf — copy targets)
  </read_first>
  <files>app/globals.css, app/fonts.ts, app/fonts/BMHannaPro.ttf, app/fonts/BMDohyeon.ttf, app/fonts/BMJua.ttf, app/layout.tsx</files>
  <action>
    Copy `BMHannaPro.ttf`, `BMDohyeon.ttf`, `BMJua.ttf` from `design-reference/fonts/` into `app/fonts/` (next/font/local reads from the app tree). Create `app/fonts.ts` exporting `bmHanna` (`localFont({ src: './fonts/BMHannaPro.ttf', variable: '--font-bmhanna', weight: '400 800', display: 'swap' })`), `bmDohyeon` (`variable: '--font-bmdohyeon'`), `bmJua` (`variable: '--font-bmjua'` — load infra only per D-04, not surfaced). In `app/globals.css`: `@import "tailwindcss"`; port the LOCKED color token set 1:1 into `@theme` as `--color-*` keys (bg `#FFF7F1`, surface `#FFFFFF`, ink `#211A15`, ink2 `#6B5D52`, ink3 `#A1907F`, line `#F0E5DB`, primary `#FF5A33`, primary-soft `#FFE9E1`, primary-ink `#D8431F`, green `#15A24A`, green-soft `#E4F6EA`, amber `#F2A11E`, amber-ink `#B5760A`, amber-soft `#FFF1D6`, tg-header/tg-header-line/tg-title/tg-sub/tg-icon/tg-link per UI-SPEC) plus shadow recipe vars; add a `@theme inline` block defining `--font-body: var(--font-pretendard), 'BMHanna', -apple-system, sans-serif`, `--font-display: var(--font-bmhanna), var(--font-pretendard), sans-serif`, `--font-chunky: var(--font-bmdohyeon), var(--font-pretendard), sans-serif` (the `inline` is load-bearing — without it the var chain resolves to literal text); add the `[data-theme="mint"]` swap block (`--color-primary: #13C5B8`, `--color-primary-soft`/`--color-primary-ink` via `color-mix(in srgb, #13C5B8 13%/86%, white/black)` — mint palette LOCKED, mechanism only, NO toggle UI per D-05); port `fadeUp`/`fadeIn`/`confFall` `@keyframes` and the `.app-scroll` hidden-scrollbar utility verbatim. Pretendard via the dynamic-subset CDN `pretendard@v1.3.9` exposing `--font-pretendard` (Claude discretion D — CDN for app UI, self-host BM). In `app/layout.tsx`: root RSC with `<html lang="ko" data-theme="coral" className={\`${bmHanna.variable} ${bmDohyeon.variable} ${bmJua.variable}\`}>`, a `viewport-fit=cover` viewport export, `dvh`/`svh` height, `env(safe-area-inset-top)` for the status-bar area (NOT the prototype's hardcoded 54px), `--bg` background — do NOT port the one-giant-client-tree / iPhone frame (RESEARCH Anti-Patterns).
  </action>
  <verify>
    <automated>npx next build && grep -q "@theme inline" app/globals.css && grep -q "data-theme=\"mint\"" app/globals.css && grep -q "localFont" app/fonts.ts && test -f app/fonts/BMHannaPro.ttf</automated>
  </verify>
  <acceptance_criteria>
    - `app/fonts/BMHannaPro.ttf`, `app/fonts/BMDohyeon.ttf`, `app/fonts/BMJua.ttf` exist (copied from design-reference)
    - `app/fonts.ts` contains `export const bmHanna` with `variable: '--font-bmhanna'` and `weight: '400 800'`
    - `app/globals.css` contains `@theme` with `--color-primary` set to `#FF5A33`, an `@theme inline` block with `--font-body`/`--font-display`/`--font-chunky`, a `[data-theme="mint"]` block with `--color-primary` `#13C5B8`, and the `fadeUp`/`fadeIn`/`confFall` keyframes + `.app-scroll`
    - `app/layout.tsx` sets `lang="ko"`, applies the three BM font variable classes on `<html>`, exports a `viewport-fit=cover` viewport, and does NOT hardcode a 54px status bar
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Coral tokens + BM/Pretendard font roles + mint swap mechanism + keyframes resolve as Tailwind v4 utilities; root layout wires fonts and safe-area; no toggle UI shipped.</done>
</task>

<task type="auto">
  <name>Task 3: Define Drizzle users schema + Neon HTTP client + drizzle config + schema test</name>
  <read_first>
    - .planning/phases/01-db/01-RESEARCH.md §Pattern 4 (db/schema.ts users table, lib/db.ts neon-http + upsertUser, migration workflow DIRECT_URL vs pooled DATABASE_URL), §Runtime State Inventory
    - .planning/phases/01-db/01-PATTERNS.md `db/schema.ts` + `lib/db.ts` + `drizzle.config.ts` assignments (No Analog — follow Pattern 4 verbatim)
    - .planning/phases/01-db/01-CONTEXT.md D-06 (theme column coral|mint persisted on users)
    - .planning/phases/01-db/01-VALIDATION.md §Per-Task Verification Map (D-06 schema row, AUTH-01 users-upsert)
  </read_first>
  <files>db/schema.ts, lib/db.ts, drizzle.config.ts, tests/db/schema.test.ts</files>
  <action>
    Create `db/schema.ts` exporting `users = pgTable('users', { tgId: bigint('tg_id', { mode: 'number' }).primaryKey(), username: text('username'), firstName: text('first_name'), theme: text('theme', { enum: ['coral', 'mint'] }).notNull().default('coral'), createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow() })` (D-06 theme column). Create `lib/db.ts`: `const sql = neon(process.env.DATABASE_URL!)` (POOLED `-pooler` string), `export const db = drizzle({ client: sql })`, and `export async function upsertUser(u: { tgId: number; username?: string; firstName?: string })` doing `db.insert(users).values(u).onConflictDoUpdate({ target: users.tgId, set: { username: u.username, firstName: u.firstName } })` (AUTH-01 identity, idempotent — no signup). Create `drizzle.config.ts` with `dialect: 'postgresql'`, `schema: './db/schema.ts'`, `out: './drizzle'`, and `dbCredentials.url` reading `process.env.DIRECT_URL` (direct/non-pooled for DDL — never the pooled URL for migrations, Pitfall 5). Create `tests/db/schema.test.ts` asserting the Drizzle `users` table object exposes the `theme` column with enum `['coral','mint']` and default `'coral'` and `tgId` as the primary key (schema-shape assertions only — no live DB needed for this unit test).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/db/schema.test.ts && grep -q "process.env.DIRECT_URL" drizzle.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `db/schema.ts` contains `pgTable('users'` with `theme` enum `['coral', 'mint']` defaulting to `'coral'` and `tgId` as primary key
    - `lib/db.ts` exports `db` and `upsertUser`, uses `neon(process.env.DATABASE_URL!)` and `onConflictDoUpdate` on `users.tgId`
    - `drizzle.config.ts` sets `dialect: 'postgresql'` and reads `process.env.DIRECT_URL` for credentials (NOT `DATABASE_URL`)
    - `npx vitest run tests/db/schema.test.ts` exits 0 (D-06 default coral + mint accepted, tgId PK)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>users schema (with D-06 theme) defined; Neon HTTP DAL + upsert written; migration config uses DIRECT_URL; schema unit test green.</done>
</task>

<task type="auto">
  <name>Task 4: Build initData fixtures + [BLOCKING] push schema to Neon</name>
  <read_first>
    - .planning/phases/01-db/01-RESEARCH.md §Validation Architecture (fixture note — sign helper / data_check_string for signed+forged+expired), §Pattern 2 (validate/parse), §Pattern 4 migration workflow
    - .planning/phases/01-db/01-VALIDATION.md §Per-Task Verification Map (AUTH-02/03 need fixtures), §Wave 0 Requirements
    - .planning/phases/01-db/01-CONTEXT.md (blockers: Neon connection strategy, DIRECT_URL migration separation)
  </read_first>
  <files>tests/fixtures/initdata.ts</files>
  <action>
    Create `tests/fixtures/initdata.ts` exporting a deterministic test bot token `FIXTURE_BOT_TOKEN` and three raw initData strings generated without a live Telegram client: `validInitData` (correctly HMAC-signed with `FIXTURE_BOT_TOKEN` and a current `auth_date`), `forgedInitData` (a copy of `validInitData` with a tampered `hash` so signature verification fails — AUTH-02), and `expiredInitData` (correctly signed but with an `auth_date` far in the past so it fails an `expiresIn` window — AUTH-03). Generate the signed strings using `@telegram-apps/init-data-node`'s sign helper if exposed, otherwise construct the `data_check_string` and HMAC manually (sorted keys, `\n` join, HMAC-SHA256 keyed by `HMAC-SHA256("WebAppData", botToken)`) — these fixtures must let `validate()`-based unit tests run offline. Then, AFTER all schema-relevant files from Task 3 are committed, run the [BLOCKING] push: `npx drizzle-kit push` against Neon using `DIRECT_URL` (add `--force` only if it prompts for a destructive op in non-TTY CI). This creates the live `users` table that plan 02's `users-upsert` DB smoke test (AUTH-01) depends on — the phase CANNOT pass verification without it (a build/type check passes from the schema file alone, masking a missing live table).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "export const validInitData" tests/fixtures/initdata.ts && grep -q "export const forgedInitData" tests/fixtures/initdata.ts && grep -q "export const expiredInitData" tests/fixtures/initdata.ts</automated>
    <human-check>npx drizzle-kit push reports the users table created/synced in Neon (verify via Neon dashboard or `drizzle-kit` output); confirm DATABASE_URL is the -pooler string and DIRECT_URL is the direct string</human-check>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/initdata.ts` exports `FIXTURE_BOT_TOKEN`, `validInitData`, `forgedInitData`, `expiredInitData`
    - `validInitData` verifies and `forgedInitData` fails signature verification under `validate(raw, FIXTURE_BOT_TOKEN)`; `expiredInitData` fails an `expiresIn` window (exercised by plan 02 tests)
    - `npx drizzle-kit push` completes and the `users` table exists in Neon (live table present, not just in the schema file)
    - `DATABASE_URL` is the pooled (`-pooler`) connection and `DIRECT_URL` is the direct connection (used for the push)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Offline-usable signed/forged/expired initData fixtures exist; the live users table is pushed to Neon so the AUTH-01 DB smoke test can run in plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Installing the locked stack — supply-chain surface |
| env → server bundle | `DATABASE_URL`/`DIRECT_URL`/`BOT_TOKEN`/`SESSION_SECRET` must stay server-only |
| migration tool → Neon DDL | `drizzle-kit push` over the direct connection mutates schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | env vars in client bundle | mitigate | `.env.local.example` documents all four as server-only; assert no `NEXT_PUBLIC_` prefix; secrets consumed only in `lib/db.ts` / drizzle.config (server) |
| T-01-02 | Tampering | Neon schema via pooled conn / per-build migration | mitigate | `drizzle-kit push` runs once over `DIRECT_URL` (direct); runtime queries use pooled `DATABASE_URL` (Pitfall 5) |
| T-01-SC | Tampering | npm installs (locked stack) | mitigate | All packages `[VERIFIED: npm registry]` in RESEARCH §Package Legitimacy Audit, no postinstall scripts; versions pinned exactly. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking checkpoint required (zod v4 drift avoided by locking v3) |
</threat_model>

<verification>
- `npx next build` and `npx eslint .` exit 0 (scaffold healthy)
- `npx vitest run` exits 0 (test infra live)
- `npx vitest run tests/db/schema.test.ts` green (D-06 theme default coral, mint accepted)
- `npx drizzle-kit push` created the live `users` table in Neon (DB read/write target for plan 02)
- `app/globals.css` has `@theme` coral tokens + `@theme inline` font chain + `[data-theme="mint"]`; fonts copied
</verification>

<success_criteria>
- Next 16 App Router + Tailwind v4 scaffold builds and lints
- Vitest runs; initData fixtures (signed/forged/expired) exist offline
- Coral tokens + BM/Pretendard font roles + mint mechanism live; no toggle UI
- `users` table (D-06 theme) defined AND pushed to Neon
- Server-only env contract documented; no `NEXT_PUBLIC_` secret
</success_criteria>

<output>
Create `.planning/phases/01-db/01-01-SUMMARY.md` when done.
</output>

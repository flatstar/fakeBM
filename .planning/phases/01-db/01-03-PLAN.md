---
phase: 01-db
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - lib/catalog.ts
  - lib/format.ts
  - components/Icon.tsx
  - components/Avatar.tsx
  - components/FoodTile.tsx
  - components/TgHeader.tsx
  - components/TgMainButton.tsx
  - components/Card.tsx
  - components/Body.tsx
  - components/SubBar.tsx
  - components/BottomNav.tsx
  - components/StatBadge.tsx
  - components/Burst.tsx
  - components/Money.tsx
  - app/(mini)/layout.tsx
  - app/(mini)/home/page.tsx
  - app/(mini)/_components/WelcomeIntro.tsx
  - tests/ui/home-shell.test.tsx
  - tests/lib/format.test.ts
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "On first visit a one-time welcome intro shows (localStorage flag), then the home shell (D-08)"
    - "The home shell at /home renders the TG header + coral header band + search pill + willpower hero + 5-slot bottom nav with a center 참기 FAB (D-10)"
    - "Every ₩/number/kcal/stat renders in Pretendard with tabular-nums (never a BM font); the 참기 glyph is ✋ U+270B (never 🫷)"
    - "Shell primitives (TgHeader/TgMainButton/Card/Body/SubBar/BottomNav/StatBadge/Burst/Icon/Avatar/FoodTile) are ported and usable by later phases"
  artifacts:
    - path: "components/BottomNav.tsx"
      provides: "5-slot nav (홈/피드/참기 FAB/통계/MY) with route-based active state + ✋ FAB"
      contains: "✋"
    - path: "components/Icon.tsx"
      provides: "inline SVG stroke set (30 names) ported verbatim"
      contains: "viewBox=\"0 0 24 24\""
    - path: "lib/catalog.ts"
      provides: "seed catalog CATEGORIES/RESTAURANTS/ALL_MENU/SEED_POSTS (immutable, typed)"
      exports: ["CATEGORIES", "RESTAURANTS", "ALL_MENU"]
    - path: "lib/format.ts"
      provides: "fmtWon / fmtNum (Pretendard-routed)"
      exports: ["fmtWon", "fmtNum"]
    - path: "app/(mini)/home/page.tsx"
      provides: "home shell placeholder at /home (D-10 payoff surface, behind the (mini) guard)"
    - path: "app/(mini)/_components/WelcomeIntro.tsx"
      provides: "one-time welcome intro (localStorage first-visit flag, D-08/09)"
      contains: "localStorage"
  key_links:
    - from: "app/(mini)/home/page.tsx"
      to: "components/BottomNav.tsx"
      via: "shell composition"
      pattern: "BottomNav"
    - from: "app/(mini)/home/page.tsx"
      to: "lib/format.ts"
      via: "fmtWon for the willpower hero amount in a Pretendard span"
      pattern: "fmtWon"
    - from: "components/BottomNav.tsx"
      to: "next/navigation"
      via: "usePathname route-based active tab"
      pattern: "usePathname"
---

<objective>
Port the locked coral design system and deliver the Phase-1 visible payoff: the shell primitives (TgHeader, TgMainButton, Card, Body, SubBar, BottomNav + 참기 FAB, StatBadge, Burst, Icon, Avatar, FoodTile), the seed catalog + formatters, the one-time welcome intro, and the home shell placeholder at `/home` — all rendered inside the authenticated `(mini)` boundary built in plan 02. (The session-establishing `SessionBoot` lives in the public `(boot)` surface from plan 02 and forwards here once the cookie is set.)

Purpose: D-10's "무가입으로 들어와 내 미니앱이 떠 있는" observable state. After this plan a real user opening the mini app boots on `/` (public bootstrap), gets a session, and is forwarded to `/home` where they see a faithful coral shell: welcome intro on first visit, then a home with TG header, coral band, search pill, willpower hero, and the bottom nav + ✋ FAB.
Output: 13 ported components, `lib/catalog.ts` + `lib/format.ts`, `WelcomeIntro`, the `/home` `page.tsx`, the TG shell wired into `(mini)/layout.tsx`, and home-shell + format tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db/01-PATTERNS.md
@.planning/phases/01-db/01-UI-SPEC.md
@.planning/phases/01-db/01-RESEARCH.md
@.planning/phases/01-db/01-SKELETON.md
@.planning/phases/01-db/01-02-SUMMARY.md
@design-reference/ui.jsx
@design-reference/app.jsx
@design-reference/data.jsx
@design-reference/screens-order.jsx
</context>

<artifacts_this_phase_produces>
This plan creates (exclude from drift verification): `lib/catalog.ts` (`CATEGORIES`, `RESTAURANTS`, `ALL_MENU`, `SEED_POSTS` + types), `lib/format.ts` (`fmtWon`, `fmtNum`), `components/`: `Icon`, `Avatar`, `FoodTile`, `TgHeader`, `TgMainButton`, `Card`, `Body`, `SubBar`, `BottomNav`, `StatBadge`, `Burst`, `Money` (Won/Num Pretendard wrapper), `app/(mini)/home/page.tsx` (home shell at `/home`), `app/(mini)/_components/WelcomeIntro.tsx`. Consumes from plans 01/02: design tokens (`@theme`), font variables, `(mini)/layout.tsx` guard, and the `(boot)` `SessionBoot` that forwards to `/home`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Port seed catalog + formatters + atomic primitives (Icon/Avatar/FoodTile/Money)</name>
  <read_first>
    - .planning/phases/01-db/01-PATTERNS.md `lib/catalog.ts` (verbatim port, ALL_MENU derivation), `lib/format.ts` (fmtWon/fmtNum + Money/Number Routing HARD RULE), `components/Icon.tsx` (30-name paths verbatim), `Avatar`/`FoodTile` assignments
    - design-reference/data.jsx (fmtWon/fmtNum lines 4–5; Icon lines 8–45; Avatar lines 48–63; FoodTile lines 66–88; CATEGORIES 90–96; RESTAURANTS 99–156; ALL_MENU 157–158; SEED_POSTS 161–186)
    - .planning/phases/01-db/01-UI-SPEC.md §Font role contract (money/number HARD RULE), Icon names list, §Design System (do NOT add lucide/heroicons)
  </read_first>
  <files>lib/catalog.ts, lib/format.ts, components/Icon.tsx, components/Avatar.tsx, components/FoodTile.tsx, components/Money.tsx, tests/lib/format.test.ts</files>
  <action>
    Port `lib/format.ts`: `fmtWon = (n) => '₩' + Math.round(n).toLocaleString('ko-KR')`, `fmtNum = (n) => Math.round(n).toLocaleString('ko-KR')` (verbatim). Create `components/Money.tsx` exporting a `Won`/`Num` wrapper that renders `fmtWon`/`fmtNum` output into a `--font-body` (Pretendard) span with `font-variant-numeric: tabular-nums` — the HARD RULE enforcement point so callers cannot route the ₩ glyph through a BM font (Pitfall 7). Port `lib/catalog.ts` verbatim from `data.jsx`: `CATEGORIES`, `RESTAURANTS`, the derived `ALL_MENU` map (preserve the `RESTAURANTS.forEach(... ALL_MENU[m.id] = { ...m, rest: r.name, cat: r.cat })` derivation), and `SEED_POSTS`, as immutable typed `export const`s (add TS types for restaurant/menu/post shapes; strip the `Object.assign(window, …)` export). Port `components/Icon.tsx` verbatim: the entire 30-name `paths` map (`home, feed, chart, user, search, heart, plus, minus, back, chevron, chevDown, clock, pin, receipt, share, fire, won, check, checkCircle, star, bag, rider, camera, sparkle, trophy, leaf, x, pencil, chat, bookmark`), `viewBox="0 0 24 24"`, `strokeLinecap/Linejoin: round`, default `strokeWidth 2`, `aria-hidden="true"` — do NOT substitute an icon library. Port `Avatar` (initials + deterministic warm gradient from `AV_COLORS`, hash logic verbatim) and `FoodTile` (`FOOD_BG` per-category gradient + emoji glyph). Write `tests/lib/format.test.ts` asserting `fmtWon(12000) === '₩12,000'` and `fmtNum(1500) === '1,500'`.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/format.test.ts && npx tsc --noEmit && grep -q "viewBox=\"0 0 24 24\"" components/Icon.tsx && grep -q "ALL_MENU" lib/catalog.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/format.ts` exports `fmtWon`/`fmtNum`; `tests/lib/format.test.ts` exits 0
    - `components/Money.tsx` renders money/number into a Pretendard (`--font-body`) span with `tabular-nums`
    - `lib/catalog.ts` exports `CATEGORIES`, `RESTAURANTS`, `ALL_MENU` (with the preserved derivation) and `SEED_POSTS` as typed immutable constants; no `Object.assign(window`
    - `components/Icon.tsx` contains all 30 names with `viewBox="0 0 24 24"` and no lucide/heroicons import
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Catalog, formatters (with Pretendard money wrapper), and atomic primitives ported verbatim; format test green.</done>
</task>

<task type="auto">
  <name>Task 2: Port shell chrome primitives (TgHeader/TgMainButton/Card/Body/SubBar/BottomNav+FAB/StatBadge/Burst)</name>
  <read_first>
    - .planning/phases/01-db/01-PATTERNS.md `TgHeader`/`TgMainButton`/`BottomNav`+FAB assignments + the Card/Body/SubBar/StatBadge/Burst table (source file:lines + port notes), §Theme Swap (do NOT port TweaksPanel/FONT_MAP)
    - design-reference/ui.jsx (TgHeader 4–34; TgMainButton 37–60; Body 63–69; SubBar 72–85; Card 87–94; StatBadge+TINT 97–113; Burst 116–132)
    - design-reference/app.jsx (BottomNav 147–166; 참기 FAB 152–156)
    - .planning/phases/01-db/01-UI-SPEC.md §Component Inventory (per-component contracts), §Spacing/Radii/Shadow LOCKED, §Color (accent reserved list), a11y aria-label recs
  </read_first>
  <files>components/TgHeader.tsx, components/TgMainButton.tsx, components/Card.tsx, components/Body.tsx, components/SubBar.tsx, components/BottomNav.tsx, components/StatBadge.tsx, components/Burst.tsx</files>
  <action>
    Port each primitive from inline styles → Tailwind v4 token utilities (the `@theme` color tokens + `--font-*` roles from plan 01). `TgHeader`: 50px, `--tg-header` bg, 1px `--tg-header-line` bottom border, centered title 16/700 + "mini app · bot" subtitle 11/400 `--tg-sub`, right minimize(`chevDown`)+close(`x`) 18px `--tg-icon`, optional left back ("뒤로" `--tg-link`); add `aria-label` on icon-only buttons (최소화/닫기/뒤로 — checker rec). `TgMainButton` (`'use client'`): full-width radius 16, min-height 54, coral fill + white label 17/800 `--font-display` + optional sub 12/500 Pretendard, disabled bg `#E3D8CE` no shadow, press `scale(.98)`, MainButton shadow, bottom padding `calc(14px + env(safe-area-inset-bottom))`, top gradient fade over `--bg`. `Card`: `--surface`, radius 18, `--shadow`, pointer when clickable. `Body`: `className="app-scroll"`, `flex:1; overflow-y:auto`. `SubBar`: 52px back + title 18/800 `--font-display`. `BottomNav` (`'use client'`): 5 slots `홈/피드/[FAB]/통계/MY`, `--surface` bg + 1px `--line` top border, active=coral icon+label 10.5/700 vs inactive `--ink3` 10.5/500, route-based active via `usePathname` (NOT the prototype `tab` state machine); center 참기 FAB = 58×58 coral circle, 4px `--surface` border, `top:-26` offset, FAB shadow, **✋ (U+270B)** glyph 20px + "참기" 10/800 — NEVER 🫷. `StatBadge`: pill radius 999 icon+label+value, tinted via the ported `TINT` map (save→green, kcal→primary-ink/primary-soft, streak→amber). `Burst`: 26 DOM confetti bits (colors `#FF5A33,#FFB454,#16A34A,#7AA7E0,#F59E0B,#E08BA9`) with `confFall` — primitive only. Do NOT port the iPhone frame, TweaksPanel, 5-color picker, title-font picker, or wait-time slider (D-04 removed).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build && grep -q "✋" components/BottomNav.tsx && ! grep -q "🫷" components/BottomNav.tsx && grep -q "usePathname" components/BottomNav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All eight primitive files exist and compile (`npx tsc --noEmit` exits 0)
    - `components/BottomNav.tsx` contains ✋ (U+270B), does NOT contain 🫷, and uses `usePathname` for route-based active state
    - `TgHeader` icon-only buttons carry `aria-label`; `TgMainButton` uses `calc(14px + env(safe-area-inset-bottom))`
    - No TweaksPanel / color-picker / font-picker / wait-time slider / iPhone frame ported (D-04)
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>All shell chrome primitives ported to Tailwind tokens with correct contracts, route-based nav, ✋ FAB, a11y labels; removed Tweaks not ported.</done>
</task>

<task type="auto">
  <name>Task 3: Welcome intro + home shell placeholder at /home wired into the (mini) shell (D-08/D-10)</name>
  <read_first>
    - .planning/phases/01-db/01-PATTERNS.md `app/(mini)/page.tsx` (HomeScreen chrome port — coral band, search pill, willpower hero; now mapped to `/home`), `WelcomeIntro` assignment (D-08/09, tone, TgMainButton), `app/(mini)/layout.tsx` (TgShell = TgHeader + BottomNav)
    - design-reference/screens-order.jsx (HomeScreen lines 6–36: coral header band 10–20, search pill 21–24, willpower hero 29–36)
    - .planning/phases/01-db/01-UI-SPEC.md §Phase-1 Visible Surfaces (welcome intro, home shell LOCKED elements, TG shell chrome), §Copywriting Contract (all locked copy + tone guardrails)
    - .planning/phases/01-db/01-CONTEXT.md D-08/09 (one-time intro, localStorage flag), D-10 (visible payoff)
    - app/(mini)/layout.tsx (from plan 02 — wrap shell inside the guard); app/(boot)/_components/SessionBoot.tsx (from plan 02 — it forwards to `/home`, so the home must live at `/home`)
  </read_first>
  <files>app/(mini)/layout.tsx, app/(mini)/home/page.tsx, app/(mini)/_components/WelcomeIntro.tsx, tests/ui/home-shell.test.tsx</files>
  <action>
    Update `app/(mini)/layout.tsx` to render the TG shell chrome around `{children}` (keep the plan-02 `requireSession()` guard; do NOT add `SessionBoot` here — it lives in the public `(boot)` surface from plan 02): `TgHeader` (title "배달의 만족" + "mini app · bot" subtitle) at top, `BottomNav` (5-slot + 참기 FAB) at bottom, content in a `Body` scroll container, with `env(safe-area-inset-*)` + `dvh`/`svh` (Pitfall 7) and the `data-theme` already set on `<html>` (plan 01). Create `app/(mini)/_components/WelcomeIntro.tsx` (`'use client'`): a one-time single-screen coral intro (tone "시켜놓고, 참는다", headline + `TgMainButton` "시작하기") gated by a localStorage first-visit flag (D-08/09) — shown only on first visit, then home; copy/visual Claude discretion within the locked tone. Create `app/(mini)/home/page.tsx` (served at `/home` — the forward target of the `(boot)` `SessionBoot`) as the home shell placeholder (D-10) porting the `HomeScreen` chrome ONLY: coral header band (`--primary` bg, white) with location "우리집" + chevDown (18px, `nowrap`) and a cart/bag icon (26px) top-right; white search pill (radius 14) with search icon + placeholder "오늘은 뭘 참아볼까? 🤤" (`--ink3`, 15/600, `nowrap`, static in Phase 1); willpower hero `Card` (dark gradient `linear-gradient(120deg,#231a14,#3a2a1d)`, 🔥 icon box, label "{streak}일째 참는 중 · 이번 달", hero amount in BMDohyeon 24/800 tabular-nums via the `Money`/`Won` wrapper + "아꼈어요" suffix, chevron) using seeded/zeroed values (DB-driven stats are Phase 5). Mount `WelcomeIntro` at the top of the home flow. Do NOT port the restaurant list / category grid interaction (Phase 2). Write `tests/ui/home-shell.test.tsx` (RTL) asserting the home shell renders the TG header title "배달의 만족", the search placeholder "오늘은 뭘 참아볼까? 🤤", and the bottom nav 참기 FAB (✋); render the page/shell with the welcome flag pre-set so the intro is dismissed.
  </action>
  <verify>
    <automated>npx vitest run tests/ui/home-shell.test.tsx && npx next build && grep -q "오늘은 뭘 참아볼까" "app/(mini)/home/page.tsx" && grep -q "localStorage" "app/(mini)/_components/WelcomeIntro.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `app/(mini)/layout.tsx` renders `TgHeader` + `BottomNav` around children AND retains the plan-02 `requireSession()` guard (and does NOT mount `SessionBoot` — that is in `(boot)`)
    - `app/(mini)/_components/WelcomeIntro.tsx` is `'use client'`, uses a localStorage first-visit flag (D-08/09), and renders `TgMainButton` "시작하기"
    - `app/(mini)/home/page.tsx` is served at `/home`, contains the location "우리집", search placeholder "오늘은 뭘 참아볼까? 🤤", and the willpower hero with the amount routed through the Pretendard `Money` wrapper (tabular-nums) — no restaurant-list interaction (Phase 2)
    - `tests/ui/home-shell.test.tsx` asserts header title + search placeholder + ✋ FAB present and exits 0
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>First-visit welcome intro + home shell placeholder at /home render inside the authenticated TG shell (the (boot) SessionBoot forwards here); D-10 visible payoff observable; home-shell smoke test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client localStorage → render | First-visit flag is client-only (D-09), no server trust value — low risk |
| ported prototype copy → render | Emoji/line-break/font-routing hazards (Pitfall 6/7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-UI1 | Tampering (display integrity) | BM font ₩→`~` glyph corruption | mitigate | `Money`/`Won` wrapper forces every ₩/number into a Pretendard `tabular-nums` span; never `--font-display`/`--font-chunky` for the ₩ symbol (Pitfall 7) |
| T-01-UI2 | (display integrity) | emoji tofu in Telegram WebView | mitigate | whitelist only; 참기 hand = ✋ U+270B, never 🫷 (verified in BottomNav grep) |
| T-01-UI3 | Information Disclosure | first-visit flag in localStorage | accept | client-only UX flag, no PII/auth value (D-09) — no server boundary crossed |
| T-01-SC | Tampering | npm installs | mitigate | no new packages this plan (ports only); inherits plan-01 locked stack |
</threat_model>

<verification>
- `npx vitest run tests/ui/home-shell.test.tsx` + `tests/lib/format.test.ts` green
- `npx next build` clean
- BottomNav contains ✋, not 🫷; uses `usePathname`
- Home shell at /home shows TG header, coral band, search pill, willpower hero (Pretendard money), 5-slot nav + FAB
- Welcome intro gated by localStorage first-visit flag; `(boot)` SessionBoot forwards to `/home`
</verification>

<success_criteria>
- D-10 visible payoff: authenticated user (booted via the public `(boot)` surface) is forwarded to `/home` and sees a faithful coral shell (welcome intro → home)
- All 13 primitives + catalog + formatters ported on the locked design contract
- Money/number HARD RULE enforced via the Pretendard wrapper; ✋ not 🫷
- Removed Tweaks (color/font picker, wait slider, iPhone frame) not ported
</success_criteria>

<output>
Create `.planning/phases/01-db/01-03-SUMMARY.md` when done.
</output>
</content>

---
phase: 01-db
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - vercel.json
  - README.md
autonomous: false
requirements: [AUTH-04, AUTH-05]
user_setup:
  - service: vercel
    why: "Deploy the walking skeleton to a dev environment so the real-device Telegram cookie check can run"
    env_vars:
      - name: BOT_TOKEN
        source: "Vercel project → Settings → Environment Variables (server-only)"
      - name: SESSION_SECRET
        source: "Vercel env (server-only)"
      - name: DATABASE_URL
        source: "Vercel env — Neon pooled (-pooler) string"
      - name: DIRECT_URL
        source: "Vercel env — Neon direct string"
    dashboard_config:
      - task: "Register the deployed mini app URL with a dev BotFather bot (Mini App → URL)"
        location: "Telegram @BotFather → /myapps (or /newapp) → set Web App URL to the Vercel deployment"

must_haves:
  truths:
    - "The walking skeleton is deployed to a dev environment (Vercel) and opens end-to-end: open → public (boot) bootstrap identifies (no signup) → forwarded to the protected /home shell"
    - "On a real Telegram device, the session persists across close/reopen (SameSite=None; Partitioned cookie survives the cross-site WebView) — AUTH-04 (resolves RESEARCH Open Question 2: SameSite real-device behavior — this is the device gate)"
    - "An unauthenticated/blocked path redirects to the public bootstrap (no redirect loop); share/* opens publicly on the deployed app — AUTH-05"
  artifacts:
    - path: "README.md"
      provides: "full-stack run + deploy + real-device verification instructions"
      contains: "vercel"
  key_links:
    - from: "deployed app"
      to: "Neon users table"
      via: "POST /api/session upsert on real open"
      pattern: "session"
---

<objective>
Deploy the walking skeleton to a dev environment and verify the one behavior that cannot be tested offline: the `SameSite=None; Partitioned` session cookie surviving a real Telegram iOS/Android WebView across close/reopen (AUTH-04, the project's MEDIUM-confidence Blocker and RESEARCH Open Question 2). This closes the walking skeleton: a real user, on a real device, opens the mini app with no signup — lands on the public `(boot)` bootstrap, is identified, and is forwarded to the coral `/home` shell with a persistent session.

Purpose: AUTH-04 device persistence and the deployed-skeleton verification are the only must-haves that automated tests cannot cover (cross-site iframe cookie behavior in Telegram's WebView has no authoritative source — RESEARCH A3 / Open Question 2). This plan provisions the deploy and runs the manual gate that resolves that open question.
Output: a deployed dev skeleton, `README.md` full-stack/deploy/verify instructions, and a confirmed (or fallback-triggered) real-device session result.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-db/01-RESEARCH.md
@.planning/phases/01-db/01-VALIDATION.md
@.planning/phases/01-db/01-SKELETON.md
@.planning/phases/01-db/01-02-SUMMARY.md
@.planning/phases/01-db/01-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates (exclude from drift verification): `vercel.json` (deploy config if needed), `README.md` (run/deploy/verify instructions). No new application symbols — this is deploy + manual verification of the slice built in plans 01–03.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Deploy walking skeleton to Vercel dev + document full-stack run/deploy</name>
  <read_first>
    - .planning/phases/01-db/01-RESEARCH.md §Environment Availability (Vercel project + env, BotFather bot), §Runtime State Inventory (server-only env vars + Vercel env pull)
    - .planning/phases/01-db/01-VALIDATION.md §Manual-Only Verifications (real-device SameSite check)
    - .planning/phases/01-db/01-SKELETON.md §Capability Proven End-to-End, §Stack Touched (deployment row)
    - .planning/phases/01-db/01-02-SUMMARY.md (cookie attributes + the (boot)/(mini) boundary + any header-token fallback notes), .planning/phases/01-db/01-03-SUMMARY.md (home shell at /home)
  </read_first>
  <files>vercel.json, README.md</files>
  <action>
    Deploy the skeleton to a Vercel dev/preview environment via the Vercel CLI (`vercel` for a preview deploy; `vercel link` if not yet linked). Ensure the four server-only env vars (`BOT_TOKEN`, `SESSION_SECRET`, `DATABASE_URL` pooled, `DIRECT_URL` direct) are set in the Vercel project (none `NEXT_PUBLIC_`); use `vercel env pull .env.local` to mirror locally. Add `vercel.json` only if a non-default setting is required (otherwise omit). Write `README.md` documenting: (a) local full-stack run (`npm run dev` + the `.env.local` vars), (b) the deploy command and required Vercel env vars, (c) the BotFather dev-bot Mini App URL registration step (the registered URL is the app root `/` — the public `(boot)` bootstrap surface that establishes the session then forwards to `/home`), (d) how to run the test suite (`npx vitest run`), and (e) the real-device verification checklist (next task). If the CLI deploy or env access returns an auth error, surface it as a checkpoint (the executor authenticates, then retries) rather than faking the deploy.
  </action>
  <verify>
    <automated>grep -qi "vercel" README.md && grep -q "vitest run" README.md && ! grep -q "NEXT_PUBLIC_BOT_TOKEN\|NEXT_PUBLIC_SESSION_SECRET" README.md</automated>
    <human-check>The Vercel deploy succeeds and the deployment URL opens the app (root / boots → home shell visible in a normal browser via the dev-mock path); the four env vars are set server-only in the Vercel project</human-check>
  </verify>
  <acceptance_criteria>
    - The skeleton is deployed to a Vercel dev/preview URL and opens (root `/` boots → `/home` shell renders)
    - `BOT_TOKEN`, `SESSION_SECRET`, `DATABASE_URL`, `DIRECT_URL` are set in Vercel as server-only (no `NEXT_PUBLIC_`)
    - `README.md` documents local full-stack run, deploy, BotFather URL registration (root `/`), test command, and the real-device checklist
  </acceptance_criteria>
  <done>Walking skeleton deployed to a dev environment; run/deploy/verify documented; server-only env confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-device Telegram session persistence verification (AUTH-04 / AUTH-05)</name>
  <files>(none — manual verification on a real Telegram device against the deployed app)</files>
  <action>
    Blocking human-verify checkpoint — NOT autonomous work. All automation (deploy, env, full offline test suite, the two HIGH security gates, the first-open-bootstrap no-loop test) completed in Task 1 and plans 01–03. The executor pauses here and presents the `<how-to-verify>` steps to the user, who runs them on a real Telegram iOS/Android device (the cross-site SameSite=None; Partitioned cookie behavior in Telegram's WebView is not automatable — RESEARCH A3 / Open Question 2, project Blocker). Do not auto-approve. Record the outcome (session persisted, or which fallback path was taken) in the SUMMARY.
  </action>
  <what-built>
    The deployed walking skeleton: Telegram mini app where the public `(boot)` surface at `/` identifies a user with no signup (SDK boot → raw initData → `POST /api/session` server validate → users upsert → `__session` cookie `HttpOnly; Secure; SameSite=None; Partitioned`) then forwards to the protected `/home`; an authoritative `(mini)` guard, a public `share/*` boundary, and the coral home shell (welcome intro on first visit). All offline AUTH-01/02/03/05 + the two HIGH security gates + the first-open-bootstrap no-loop test already pass in CI.
  </what-built>
  <how-to-verify>
    On a real Telegram device (iOS AND/OR Android in-app WebView — the cross-site cookie behavior is platform-specific and not automatable):
    1. Open the mini app via the dev BotFather bot (the root `/` URL registered in Task 1). Confirm the brief `(boot)` splash does NOT loop/redirect endlessly (the first-open no-loop fix) and you are forwarded into the app.
    2. First open: confirm the one-time welcome intro shows ("시켜놓고, 참는다" tone), tap "시작하기" → land on the coral `/home` shell (TG header "배달의 만족", search pill "오늘은 뭘 참아볼까? 🤤", willpower hero with ₩ in Pretendard, 5-slot nav + ✋ FAB). Confirm ✋ renders (not 🫷 tofu) and ₩/numbers are not corrupted to `~`.
    3. Confirm no signup prompt appeared (AUTH-01 identity).
    4. Close the mini app fully, then reopen it: confirm the session persists (NO re-auth / no welcome intro again, lands straight on `/home`) — this is the AUTH-04 SameSite=None; Partitioned device gate (resolves Open Question 2).
    5. (AUTH-05) Open a `share/*` URL in an external browser (outside Telegram): confirm it opens with no auth.
    6. If step 4 FAILS (session lost on reopen): record it and trigger the documented fallback (per-reopen re-auth via D-03, or carry the session token via an `Authorization` header instead of the cookie — RESEARCH Pattern 2 / Pitfall 2). Note the fallback path taken in the SUMMARY.
  </how-to-verify>
  <verify>
    <human-check>Session persists across reopen on a real Telegram device (AUTH-04), or the documented fallback is confirmed working; the (boot) surface does not loop on first open; share/* opens with no auth (AUTH-05)</human-check>
  </verify>
  <done>Real-device AUTH-04 session persistence confirmed (or fallback path confirmed working), first-open no-loop confirmed, and AUTH-05 public share boundary confirmed on the deployed app.</done>
  <resume-signal>Type "approved" if the session persists across reopen on a real device (or the fallback is confirmed working) and the first open does not loop, or describe what failed (which platform, which step).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram WebView (cross-site iframe) → cookie store | The deployed cookie must survive a real device reopen — the only unverifiable-offline boundary |
| Vercel env → server bundle | Deployed secrets must stay server-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-CHIPS | (auth availability) | cross-site cookie blocked in TG WebView | mitigate | real-device verification gate (this plan); `Partitioned` (CHIPS) already set; header-token / per-reopen re-auth fallback documented (A3, D-03) |
| T-01-BOOT | (auth availability) | first-open redirect loop / cookieless lockout | mitigate | offline first-open-bootstrap test (plan 02) + real-device step 1 confirm the `(boot)` surface boots and forwards without looping |
| T-01-DEP | Information Disclosure | secrets in deployed bundle | mitigate | Vercel env server-only; README + grep assert no `NEXT_PUBLIC_` secret; dev-mock dead in prod (D-12, proven in plan 02) |
| T-01-SC | Tampering | npm installs | mitigate | no new packages this plan (deploy + verify only) |
</threat_model>

<verification>
- Deployed dev URL opens: root `/` boots → forwarded to `/home` shell (no loop)
- Real-device: no-signup identify → home shell → session persists across reopen (AUTH-04) OR fallback confirmed
- `share/*` opens externally with no auth (AUTH-05)
- README documents run/deploy/verify; no `NEXT_PUBLIC_` secret
</verification>

<success_criteria>
- Walking skeleton runs on a dev environment end-to-end (boot → home, no redirect loop)
- AUTH-04 real-device session persistence verified (or fallback path confirmed working) — resolves Open Question 2
- AUTH-05 public share boundary verified on the deployed app
- Phase 1 visible payoff (D-10) observed on a real device
</success_criteria>

<output>
Create `.planning/phases/01-db/01-04-SUMMARY.md` when done.
</output>
</content>

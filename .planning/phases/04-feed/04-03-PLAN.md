---
phase: 04-feed
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - app/api/posts/[id]/like/route.ts
  - tests/api/like.test.ts
autonomous: true
requirements: [FEED-03]

must_haves:
  truths:
    - "POST /api/posts/[id]/like toggles the viewer's like and returns server-authoritative {liked, count} (D-09)"
    - "Repeated like (double-tap/retry) is idempotent — the count converges, never inflates (D-05)"
    - "Self-like is allowed (D-08)"
    - "A hidden or deleted post cannot be liked (visibility gate)"
    - "The endpoint gates requireSession first; unauthenticated → 401"
  artifacts:
    - path: "app/api/posts/[id]/like/route.ts"
      provides: "idempotent like toggle returning authoritative {liked, count}"
      exports: ["POST"]
  key_links:
    - from: "app/api/posts/[id]/like/route.ts"
      to: "likes table"
      via: "onConflictDoNothing({target:[likes.postId, likes.tgId]}) then DELETE in a transaction"
      pattern: "onConflictDoNothing"
    - from: "app/(mini)/feed/_components/LikeButton.tsx"
      to: "POST /api/posts/[id]/like"
      via: "fetch + reconcile to {liked,count}"
      pattern: "/like"
---

<objective>
Wire the like toggle endpoint that the Plan 02 `LikeButton` already posts to: `POST /api/posts/[id]/like` performs an idempotent toggle against the `likes` table inside a transaction and returns the server-authoritative `{liked, count}`. This completes the like vertical slice — the user can now toggle a heart on any visible feed card and the count converges under double-tap/retry.

Purpose: FEED-03. The DB UNIQUE (composite PK from Plan 01) makes the toggle race-free; returning the authoritative `{liked,count}` is what makes the client reconcile (D-09) rather than drift.
Output: `app/api/posts/[id]/like/route.ts` + its node-env API test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-feed/04-CONTEXT.md
@.planning/phases/04-feed/04-RESEARCH.md
@.planning/phases/04-feed/04-PATTERNS.md
@db/schema.ts
@app/api/posts/route.ts
@app/api/wait/[id]/arrive/route.ts
@lib/auth.ts
@lib/db.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/posts/[id]/like — idempotent toggle returning {liked, count}</name>
  <files>app/api/posts/[id]/like/route.ts, tests/api/like.test.ts</files>
  <read_first>
    - app/api/wait/[id]/arrive/route.ts (Next 16 dynamic-param handler: `{params}: {params: Promise<{id: string}>}`, `await params`, Number/isInteger param guard, owner-scope idea, sql update pattern, authError/badRequest helpers)
    - app/api/posts/route.ts (onConflictDoNothing({target}) + .returning() idempotency pattern at 110-132; the gate-ordering doc comment at 12-24; the JSON error helpers at 46-56)
    - db/schema.ts (likes composite PK [postId, tgId] + posts.hiddenAt/deletedAt from Plan 01)
    - lib/db.ts (the db client; confirm db.transaction availability for neon-http per RESEARCH A1)
    - .planning/phases/04-feed/04-RESEARCH.md (Pattern 3 lines 244-264 — toggle-in-transaction + recount + authoritative return; visible-post precheck)
  </read_first>
  <behavior>
    - like.test.ts (node env): first like of a post by a user inserts → returns {liked: true, count: prior+1}; a second call by the SAME user unlikes → {liked: false, count: prior}; a third call re-likes → {liked: true} (toggle).
    - Two rapid likes by the same user (simulating double-tap on the insert path) never produce count > 1 for that (post,user) — idempotent via composite PK.
    - Liking a post whose hiddenAt or deletedAt is set → 404 (cannot act on a non-visible post).
    - Unauthenticated (no session) → 401; non-integer id → 400.
    - Self-like (post.tgId === viewer tgId) succeeds (D-08).
  </behavior>
  <action>
    Create app/api/posts/[id]/like/route.ts. `export async function POST(req: Request, { params }: { params: Promise<{ id: string }> })`. Gate order (mirror arrive/route.ts + posts/route.ts): (1) `const tgId = await requireSession(); if (!tgId) return authError();` (2) `const { id } = await params; const postId = Number(id); if (!Number.isInteger(postId)) return badRequest();` (3) visibility precheck — SELECT the target post's tgId/hiddenAt/deletedAt by id; if missing OR hiddenAt set OR deletedAt set → notFoundJson() (cannot like a non-visible post). (4) `db.transaction(async (tx) => {...})`: try `tx.insert(likes).values({postId, tgId}).onConflictDoNothing({target:[likes.postId, likes.tgId]}).returning({postId: likes.postId})`; `const liked = ins.length > 0`; if `!liked` then `tx.delete(likes).where(and(eq(likes.postId, postId), eq(likes.tgId, tgId)))` (this tap unlikes); recount `tx.select({c: sql\`count(*)::int\`}).from(likes).where(eq(likes.postId, postId))`; return `{liked, count}` where liked reflects the post-action state (inserted ⇒ true; was-present-then-deleted ⇒ false). (5) `return Response.json(result)`. Copy authError()/badRequest()/notFoundJson() verbatim from posts/route.ts. NO money/owner/count field accepted from the body — only the route param postId + session tgId cross the boundary. Write tests/api/like.test.ts (@vitest-environment node) per the behavior block, modeling Request/Response mocking on the existing tests/api posts/order tests; use a live-DB skipIf(!DATABASE_URL) smoke for the real toggle+recount, plus pure-logic assertions where feasible.
  </action>
  <verify>
    <automated>npm test -- tests/api/like.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/posts/[id]/like returns {liked, count} server-authoritative
    - Toggle is idempotent — repeated like by same user never yields count > 1 for that (post,user) (composite PK)
    - Hidden/deleted post → 404; unauthenticated → 401; non-integer id → 400
    - The handler accepts NO body field other than the route param (no owner/count/money field)
    - Self-like allowed (D-08)
    - `npm test -- tests/api/like.test.ts` exits 0
  </acceptance_criteria>
  <done>Idempotent like toggle endpoint returns authoritative {liked,count}; like API test green; LikeButton (Plan 02) now functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/posts/[id]/like | Only the route param postId + session tgId are trusted; the body carries no authoritative field |
| DB → like action | The like target must be a currently-visible post |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | like body mass-assignment | mitigate | Handler reads only postId (route param) + tgId (session); no count/owner/liked field accepted — mirrors posts/route.ts no-money rule |
| T-04-09 | Tampering | double-tap/retry count inflation | mitigate | Composite PK (postId, tgId) makes the insert idempotent; client reconciles to authoritative {liked,count} (D-05/09) |
| T-04-10 | Information Disclosure | liking a hidden/deleted post | mitigate | Visibility precheck (hiddenAt IS NULL AND deletedAt IS NULL) before the toggle → 404 otherwise |
| T-04-11 | Spoofing | unauthenticated like | mitigate | requireSession() first; 401 before any DB work |
| T-04-SC | Tampering | npm installs | accept | No package installs this phase |
</threat_model>

<verification>
- `npm test -- tests/api/like.test.ts` green; full `npm test` green
- `npm run build` + `npm run lint` clean
- Manual (post-deploy): tap heart on /feed, double-tap, confirm count converges and persists on refresh
</verification>

<success_criteria>
- Like toggle idempotent + authoritative {liked,count} (FEED-03)
- Hidden/deleted posts cannot be liked; unauthenticated blocked
- LikeButton from Plan 02 is now end-to-end functional
</success_criteria>

<output>
Create `.planning/phases/04-feed/04-03-SUMMARY.md` when done
</output>

---
phase: 04-feed
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - app/api/posts/[id]/report/route.ts
  - tests/api/report.test.ts
autonomous: true
requirements: [FEED-05]

must_haves:
  truths:
    - "POST /api/posts/[id]/report records a report and immediately hides the post globally on the first report (D-10)"
    - "A duplicate report by the same user is an idempotent no-op (D-11)"
    - "Reporting one's own post is blocked server-side (D-13)"
    - "The reason is constrained to the enum spam|inappropriate|hate|other (D-12)"
    - "Hidden posts vanish from the feed because lib/feed.ts gates on hiddenAt IS NULL"
    - "The endpoint gates requireSession first; unauthenticated → 401"
  artifacts:
    - path: "app/api/posts/[id]/report/route.ts"
      provides: "report → instant global hide, self-report blocked, idempotent"
      exports: ["POST"]
  key_links:
    - from: "app/api/posts/[id]/report/route.ts"
      to: "reports table + posts.hiddenAt"
      via: "transaction: insert report onConflictDoNothing + set posts.hiddenAt = now() if null"
      pattern: "hiddenAt"
    - from: "app/(mini)/feed/_components/ReportMenu.tsx"
      to: "POST /api/posts/[id]/report"
      via: "fetch {reason} then onHide(postId)"
      pattern: "/report"
---

<objective>
Wire the report endpoint that the Plan 02 `ReportMenu` posts to: `POST /api/posts/[id]/report` validates the reason enum, blocks self-report, records the report idempotently, and on the first report sets `posts.hiddenAt = now()` — instantly removing the post from every viewer's feed (the visibility gate in `lib/feed.ts` enforces the disappearance). This completes the report→hide vertical slice.

Purpose: FEED-05. 1-report-immediate-hide (D-10) is the launch-safety moderation primitive; its abuse risk is accepted v1 and offset by operator restore (Plan 05, D-16).
Output: `app/api/posts/[id]/report/route.ts` + its node-env API test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-feed/04-CONTEXT.md
@.planning/phases/04-feed/04-RESEARCH.md
@.planning/phases/04-feed/04-PATTERNS.md
@db/schema.ts
@app/api/posts/route.ts
@app/api/wait/[id]/arrive/route.ts
@lib/auth.ts
@lib/db.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/posts/[id]/report — self-report block + idempotent report + instant hide</name>
  <files>app/api/posts/[id]/report/route.ts, tests/api/report.test.ts</files>
  <read_first>
    - app/api/posts/route.ts (zod body → generic 400 in try/catch at 38-44/79-85; the owner-scoped SELECT idea at 88-92 — here it is INVERTED to reject self-report; onConflictDoNothing target; JSON error helpers)
    - app/api/wait/[id]/arrive/route.ts (Next 16 params Promise + Number/isInteger guard; the `sql\`now()\`` update pattern at 76-79)
    - db/schema.ts (reports composite PK [postId, tgId] + reason enum + posts.hiddenAt from Plan 01)
    - lib/db.ts (db.transaction)
    - .planning/phases/04-feed/04-RESEARCH.md (Pattern 5 lines 279-298 — owner lookup self-report block, transaction insert report + set hiddenAt if null; reason enum block at 405-411)
  </read_first>
  <behavior>
    - report.test.ts (node env): a reporter (not the author) reporting a visible post → sets posts.hiddenAt (was null) and inserts a reports row; returns success (e.g. {hidden: true} or 204).
    - A second report of the same post by the SAME reporter → no-op insert (composite PK), hiddenAt unchanged (already set) — still success, idempotent.
    - Author reporting their OWN post (post.tgId === reporter tgId) → 403/forbidden, no report row, hiddenAt untouched (D-13).
    - An invalid/missing reason (not in spam|inappropriate|hate|other) → 400 (generic).
    - A report of an already-deleted post (deletedAt set) → 404.
    - Unauthenticated → 401; non-integer id → 400.
  </behavior>
  <action>
    Create app/api/posts/[id]/report/route.ts. `export async function POST(req: Request, { params }: { params: Promise<{ id: string }> })`. Gate order: (1) `requireSession()` → authError() if none. (2) parse body with `z.object({ reason: z.enum(['spam','inappropriate','hate','other']) })` in try/catch → badRequest() on failure (D-12). (3) `const { id } = await params; const postId = Number(id); if (!Number.isInteger(postId)) return badRequest();`. (4) owner lookup: SELECT `{tgId, hiddenAt, deletedAt}` from posts where id = postId; if missing OR deletedAt set → notFoundJson(); if `target.tgId === reporterTgId` → return forbidden (403, e.g. Response.json({error:'self_report'}, {status:403})) (D-13 self-report block). (5) `db.transaction(async (tx) => {...})`: `tx.insert(reports).values({postId, tgId: reporterTgId, reason}).onConflictDoNothing({target:[reports.postId, reports.tgId]})` (D-11 dup idempotent); if `!target.hiddenAt` then `tx.update(posts).set({ hiddenAt: sql\`now()\` }).where(eq(posts.id, postId))` (D-10 first report hides). (6) return success ({hidden: true}). Add a forbidden() helper alongside the copied authError/badRequest/notFoundJson. Only postId (route param) + reason (enum) cross the boundary — no other body field trusted. Write tests/api/report.test.ts (@vitest-environment node) per the behavior block; live-DB skipIf(!DATABASE_URL) smoke for the report+hide path + the self-report reject + the duplicate-idempotent path.
  </action>
  <verify>
    <automated>npm test -- tests/api/report.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - POST /api/posts/[id]/report sets posts.hiddenAt on the first report (D-10)
    - Self-report (post.tgId === reporter tgId) → 403, no hiddenAt change (D-13)
    - Duplicate report by same user → idempotent no-op via reports composite PK (D-11)
    - reason constrained to enum; invalid reason → 400
    - Only postId + reason cross the boundary (no other body field)
    - `npm test -- tests/api/report.test.ts` exits 0
  </acceptance_criteria>
  <done>Report→instant-hide endpoint with self-report block + idempotency; report API test green; ReportMenu (Plan 02) now functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/posts/[id]/report | Only postId (route param) + reason (enum) trusted; no other body field |
| report → global hide | Hiding is a mutation on shared state — never client-trusted beyond the report act |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Elevation of Privilege | self-report to hide own/others via forged owner | mitigate | Owner lookup inverted — reject when target.tgId === reporter tgId (D-13); reporter identity comes from session, not body |
| T-04-13 | Tampering | report body mass-assignment | mitigate | zod accepts only reason enum; postId is the route param; no hiddenAt/owner field accepted |
| T-04-14 | Denial of Service | 1-report-instant-hide weaponized | accept | Accepted v1 risk per D-10; offset by operator restore (Plan 05, D-16); self-report blocked; per-user dedup via composite PK. N-threshold deferred (residual risk documented) |
| T-04-15 | Tampering | duplicate report inflation | mitigate | reports composite PK (postId, tgId) → onConflictDoNothing makes repeat reports a no-op |
| T-04-16 | Spoofing | unauthenticated report | mitigate | requireSession() first; 401 before any DB work |
| T-04-SC | Tampering | npm installs | accept | No package installs this phase |
</threat_model>

<verification>
- `npm test -- tests/api/report.test.ts` green; full `npm test` green
- `npm run build` + `npm run lint` clean
- Manual (post-deploy): report a post via the ⋯ sheet, confirm it vanishes from the feed for all viewers
</verification>

<success_criteria>
- Report instantly hides on first report; duplicate idempotent; self-report blocked (FEED-05)
- Hidden post excluded from the feed via lib/feed.ts visibility gate
- ReportMenu from Plan 02 is now end-to-end functional
</success_criteria>

<output>
Create `.planning/phases/04-feed/04-04-SUMMARY.md` when done
</output>

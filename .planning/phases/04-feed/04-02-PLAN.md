---
phase: 04-feed
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/feed.ts
  - app/api/feed/route.ts
  - app/(mini)/feed/page.tsx
  - app/(mini)/feed/_components/FeedList.tsx
  - app/(mini)/feed/_components/FeedCard.tsx
  - app/(mini)/feed/_components/LikeButton.tsx
  - app/(mini)/feed/_components/ReportMenu.tsx
  - tests/lib/feed-cursor.test.ts
  - tests/ui/feed-card.test.tsx
autonomous: true
requirements: [FEED-01, FEED-02, FEED-04]

must_haves:
  truths:
    - "User opens /feed and sees real cross-user (+own) 인증 posts from the shared Neon DB (FEED-01, D-04)"
    - "Each card shows dual photos, receipt summary, 아낌 돈/kcal payoff, caption, diet, streak (FEED-04)"
    - "Author shown as anonymous deterministic handle via handleFor(post.tgId) — NO users join (D-01/02/03)"
    - "Hidden (hiddenAt) and deleted (deletedAt) posts never appear in the feed (D-10/16, visibility gate)"
    - "더 보기 loads the next keyset page with no duplicate or gap, including tied createdAt (FEED-02)"
    - "RSC first page and GET /api/feed run the IDENTICAL query via lib/feed.ts (no seam divergence)"
    - "Empty feed shows the + 나도 참고 인증하기 CTA"
  artifacts:
    - path: "lib/feed.ts"
      provides: "shared keyset feedPage(cursor, viewerTgId) query + cursor codec + PAGE_SIZE + visibility gate"
      exports: ["feedPage", "encodeCursor", "decodeCursor", "PAGE_SIZE"]
    - path: "app/api/feed/route.ts"
      provides: "GET cursor pages — same query as the RSC page"
      exports: ["GET"]
    - path: "app/(mini)/feed/page.tsx"
      provides: "RSC first page + auth guard + FeedList mount"
    - path: "app/(mini)/feed/_components/FeedCard.tsx"
      provides: "PostCard port — dual photo/receipt/payoff/caption/diet/streak + like+report action bar"
      min_lines: 60
  key_links:
    - from: "app/(mini)/feed/page.tsx"
      to: "lib/feed.ts feedPage"
      via: "server fetch of first page"
      pattern: "feedPage"
    - from: "app/api/feed/route.ts"
      to: "lib/feed.ts feedPage"
      via: "same shared query (no divergence)"
      pattern: "feedPage"
    - from: "app/(mini)/feed/_components/FeedCard.tsx"
      to: "lib/handle.ts handleFor"
      via: "handle computed from post.tgId for Avatar name"
      pattern: "handleFor"
    - from: "lib/feed.ts"
      to: "visibility gate"
      via: "WHERE isNull(hiddenAt) AND isNull(deletedAt)"
      pattern: "isNull"
---

<objective>
Deliver the feed read vertical slice end-to-end: a user opens `/feed` (명예의 전당), sees real cross-user 인증 posts from the shared Neon DB rendered as `FeedCard`s (dual photo, receipt, payoff, caption, diet, streak, anonymous handle), and loads more pages via a keyset cursor with no duplicates or gaps. Build the `FeedCard` with its like + report action bar (the islands `LikeButton`/`ReportMenu` post to endpoints wired in Wave 3 plans 03/04).

Purpose: This is the core FEED-01/02/04 slice — the visible 명예의 전당. The shared `lib/feed.ts` is the single source of truth so the RSC first page and the `GET /api/feed` load-more island can never diverge (Pitfall 1).
Output: `lib/feed.ts`, `GET /api/feed`, `/feed` RSC page, and the feed component island set (FeedList/FeedCard/LikeButton/ReportMenu) + cursor and card tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-feed/04-CONTEXT.md
@.planning/phases/04-feed/04-RESEARCH.md
@.planning/phases/04-feed/04-PATTERNS.md
@.planning/phases/04-feed/04-UI-SPEC.md
@.planning/phases/04-feed/04-VALIDATION.md
@db/schema.ts
@lib/handle.ts
@app/api/posts/route.ts
@app/(mini)/order/[id]/page.tsx
@app/(mini)/post/[id]/_components/PostClient.tsx
@design-reference/screens-social.jsx
@components/Avatar.tsx
@components/Money.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/feed.ts shared keyset query + cursor codec + GET /api/feed</name>
  <files>lib/feed.ts, app/api/feed/route.ts, tests/lib/feed-cursor.test.ts</files>
  <read_first>
    - lib/feed.ts (does not exist yet — this task creates it)
    - app/api/posts/route.ts (gate ordering: requireSession → parse(try/catch→400) → DB; the authError/badRequest helpers to copy verbatim; the and/eq/desc query block at lines 63-72)
    - db/schema.ts (posts/likes columns + the new visibility columns + composite index from Plan 01)
    - .planning/phases/04-feed/04-RESEARCH.md (Patterns 1-4 lines 184-277 — grouped likeCount subquery, viewer-liked LEFT JOIN, composite (createdAt,id) keyset predicate, N+1 hasMore, base64url cursor codec)
    - tests/lib/feed-cursor.test.ts (the skipped scaffold authored in Plan 01 — un-skip it now)
  </read_first>
  <behavior>
    - feed-cursor.test.ts (un-skipped): encodeCursor({createdAt, id}) → base64url string; decodeCursor(str) round-trips to {createdAt: Date, id: number}; decodeCursor of a malformed/garbage string returns null (no throw); decodeCursor(null/undefined) returns null.
    - feedPage(null, viewerTgId) returns the first PAGE_SIZE posts ordered by (createdAt DESC, id DESC), each row carrying likeCount (int, coalesced 0) and liked (boolean for viewer), plus a nextCursor (string|null) — null when no further pages (N+1 probe).
    - feedPage(cursor, viewerTgId) returns the next page strictly after the cursor tuple with no overlap; rows sharing a createdAt are disambiguated by id (no dup/gap).
    - The query WHERE includes isNull(posts.hiddenAt) AND isNull(posts.deletedAt) (visibility gate) — hidden/deleted rows excluded.
  </behavior>
  <action>
    Create lib/feed.ts. Import `and, desc, eq, isNull, lt, or, sql` from 'drizzle-orm', `db` from '@/lib/db', `posts, likes` from '@/db/schema'. Export `PAGE_SIZE = 10` (discretion, D-05 cursor note). Export `encodeCursor(r: {createdAt: Date; id: number})` → `Buffer.from(JSON.stringify({c: r.createdAt.toISOString(), i: r.id})).toString('base64url')` and `decodeCursor(s: string | null)` → defensive try/catch parse returning `{createdAt: Date, id: number} | null`. Build the grouped `likeCount` subquery (`.select({postId, c: count(*)::int}).from(likes).groupBy(likes.postId).as('like_count')`, D-06) and the viewer `viewerLike` subquery (`.from(likes).where(eq(likes.tgId, viewerTgId)).as('viewer_like')`, D-07). Export `async function feedPage(cursor, viewerTgId)`: SELECT the post display columns (id, tgId, restName, items, total, kcal, savedAmount, foodPhotoUrl, dietPhotoUrl, caption, diet, streakDay, createdAt) plus `likeCount: coalesce(like_count.c, 0)` and `liked: (viewer_like.postId is not null)`; leftJoin both subqueries on postId; WHERE `and(isNull(posts.hiddenAt), isNull(posts.deletedAt), keyset)` where keyset (when cursor present) = `or(lt(posts.createdAt, cursor.createdAt), and(eq(posts.createdAt, cursor.createdAt), lt(posts.id, cursor.id)))`; orderBy `desc(posts.createdAt), desc(posts.id)`; limit PAGE_SIZE + 1. Slice to PAGE_SIZE, compute nextCursor from the last kept row when hasMore, return `{posts: page, nextCursor}`. Do NOT join users (D-03) — the handle is computed in the card from tgId. Create app/api/feed/route.ts: `export async function GET(req: Request)` — requireSession → authError() if none; read `?cursor=` from new URL(req.url).searchParams; decodeCursor in/around try/catch (malformed → badRequest 400 OR treat as no cursor per RESEARCH Open Q2 — choose badRequest); `return Response.json(await feedPage(cursor, tgId))`. Copy authError()/badRequest() verbatim from app/api/posts/route.ts. Un-skip and finalize tests/lib/feed-cursor.test.ts against the exported codec.
  </action>
  <verify>
    <automated>npm test -- tests/lib/feed-cursor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/feed.ts exports feedPage, encodeCursor, decodeCursor, PAGE_SIZE
    - feedPage query WHERE includes isNull(posts.hiddenAt) AND isNull(posts.deletedAt)
    - keyset predicate uses composite (createdAt, id) tuple comparison — not createdAt-only
    - decodeCursor of a malformed string returns null (no throw)
    - GET /api/feed gates requireSession first, returns 401 when unauthenticated
    - lib/feed.ts contains NO join to the users table
    - `npm test -- tests/lib/feed-cursor.test.ts` exits 0
  </acceptance_criteria>
  <done>Shared keyset feed query + cursor codec + GET /api/feed handler complete; cursor test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FeedCard + LikeButton + ReportMenu islands + card render test</name>
  <files>app/(mini)/feed/_components/FeedCard.tsx, app/(mini)/feed/_components/LikeButton.tsx, app/(mini)/feed/_components/ReportMenu.tsx, tests/ui/feed-card.test.tsx</files>
  <read_first>
    - design-reference/screens-social.jsx (PostCard at lines 23-70 — the markup source of truth: header/avatar/streak badge → dual PostPhoto grid → receipt chip → StatBadge payoff → caption + diet pill → action bar)
    - app/(mini)/post/[id]/_components/PostClient.tsx (port conventions: 'use client', var(--color-*) token names, StatBadge usage at 288-301, fetch+reconcile pattern at 90-122, Won/Num usage)
    - components/Money.tsx (the Won/Num HARD RULE wrappers — every ₩/kcal/count routes through these, never inline span)
    - lib/handle.ts (handleFor — pass to Avatar name)
    - .planning/phases/04-feed/04-UI-SPEC.md (Component Inventory + Interaction Contracts + Copywriting Contract — locked spacing/typography/color, like reconcile rule D-09, report ⋯ glyph trigger, self-owned cards hide report, error copy)
  </read_first>
  <behavior>
    - feed-card.test.tsx: rendering FeedCard with a post fixture shows the anonymous handle (handleFor(tgId), NOT a raw username), both photo URLs, the receipt text "{restName} · {items summary}", the 아낌 ₩ value and the −kcal value (via Won/Num), the caption, the diet pill, and the 🔥 N일째 streak badge.
    - LikeButton initial render reflects the post's `liked`/`likeCount`; the heart fill is coral when liked, neutral when not; aria-pressed reflects liked.
    - When the post's tgId === the current viewer's tgId, the ReportMenu trigger is NOT rendered (D-13 self-report hidden client-side).
  </behavior>
  <action>
    Create FeedCard.tsx (server-or-client per composition; the card itself can be a server component receiving a `viewerTgId` prop, with LikeButton/ReportMenu as client islands). Port the PostCard markup from screens-social.jsx using the ported `--color-*`/`--shadow-card` token names (NOT the prototype raw vars). Structure: `<Card>` (pad 14) → header (`<Avatar name={handleFor(post.tgId)} size={42} />` + handle 14.5/800 display + "{relative time} · 인증" meta + 🔥 {streakDay}일째 badge) → dual-photo grid (2 cols, 128px height, 14px radius, corner labels "시킨 척 🤤"/"실제 식단 🥗") → receipt chip (`<Icon name="receipt"/>` + restName + items summary) → payoff row using `<StatBadge icon="won" label="아낌 " value={<Won value={post.savedAmount}/>} tint={TINT.save}/>` and `<StatBadge icon="fire" label="" value={<><Num value={post.kcal}/>kcal</>} tint={TINT.kcal}/>` → caption (13.5/500 lh 1.55) → diet pill (🥗 green) → action bar (borderTop, gap 18) holding `<LikeButton>` and, only when `post.tgId !== viewerTgId`, `<ReportMenu postId={post.id}/>`. DROP 응원/북마크 (CONTEXT discretion). Create LikeButton.tsx ('use client'): props {postId, initialLiked, initialCount}; useState for liked/count; on tap optimistically flip then `POST /api/posts/[id]/like` and RECONCILE to the returned `{liked,count}` (set, never +1/-1 — D-09/Pitfall 7); on network failure revert the optimistic flip and surface "좋아요를 반영하지 못했어요. 다시 눌러주세요."; ≥44px tap area; aria-pressed=liked, aria-label "좋아요"/"좋아요 취소"; coral heart fill when liked. Create ReportMenu.tsx ('use client'): a ⋯ text-glyph trigger (no flag/overflow icon exists — 18px Pretendard --color-ink3, ≥44px tap, aria-label "더보기") opening a bottom sheet (scrim rgba(20,12,8,.55)) titled "신고 사유" with the warning line "신고하면 바로 숨겨져요" (--color-primary-ink) and four single-select reason chips 스팸·부적절·혐오·기타 (enum spam/inappropriate/hate/other); tapping a chip `POST /api/posts/[id]/report` with {reason}; on success call an onHide(postId) callback (card removed) + toast "신고했어요. 검토 후 처리돼요."; word-break: keep-all on Korean chips. Write tests/ui/feed-card.test.tsx per the behavior block.
  </action>
  <verify>
    <automated>npm test -- tests/ui/feed-card.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - FeedCard renders handleFor(post.tgId), never a raw Telegram username/firstName
    - 아낌 ₩ and −kcal payoff render via Won/Num (no inline BM-font/fmtWon span)
    - ReportMenu trigger absent when post.tgId === viewerTgId (D-13)
    - LikeButton sets liked/count from server response, never increments locally (source: no `+ 1`/`- 1` on count state)
    - Card uses ported `--color-*` tokens, not prototype `var(--primary)`/`var(--bg)`
    - `npm test -- tests/ui/feed-card.test.tsx` exits 0
  </acceptance_criteria>
  <done>FeedCard + LikeButton + ReportMenu built to UI-SPEC; card render test green.</done>
</task>

<task type="auto">
  <name>Task 3: /feed RSC page + FeedList load-more island + empty/end states</name>
  <files>app/(mini)/feed/page.tsx, app/(mini)/feed/_components/FeedList.tsx</files>
  <read_first>
    - app/(mini)/order/[id]/page.tsx (RSC pattern: async, params Promise, requireSession, db fetch, render; imports of Body/Card/Won/Num/requireSession/notFound)
    - app/(mini)/layout.tsx (the (mini) guard already gates session — the feed page still calls requireSession() for the viewer id)
    - app/(mini)/post/[id]/_components/PostClient.tsx ('use client' island + fetch convention for FeedList)
    - lib/feed.ts (feedPage signature from Task 1)
    - .planning/phases/04-feed/04-UI-SPEC.md (FeedScreen header copy 명예의 전당 🏆 + subtitle; LoadMoreSentinel — prefer explicit 더 보기 button; end state 여기까지예요 🙌; empty state + CTA; feed list 14px gap)
  </read_first>
  <action>
    Create app/(mini)/feed/page.tsx as an async RSC: `const tgId = await requireSession();` (viewer id; the (mini) layout already redirected cookieless users). Render the header block ("명예의 전당 🏆" 24/800 chunky + subtitle "오늘도 시켜놓고 참아낸 사람들" 13/600 --color-ink2). Call `feedPage(null, tgId)` for the first page (public read — NO owner scope). If the first page is empty, render the FeedEmptyState (centered, 48px breathing, 🏆, heading "아직 명예의 전당이 비어 있어요", body "첫 번째로 시켜놓고 참아낸 기록을 남겨보세요 🏆", + the "+ 나도 참고 인증하기" dashed coral CTA navigating to the order/참기 entry). Otherwise mount `<FeedList initialPosts={...} initialCursor={nextCursor} viewerTgId={tgId} />`. Create FeedList.tsx ('use client'): holds the appended posts array + opaque cursor in useState; maps posts to `<FeedCard post viewerTgId onHide={removeById} />`; renders the tail affordance — prefer an explicit "더 보기" button (avoids backgrounded-webview observer flakiness) that `GET /api/feed?cursor=<cursor>`, appends the returned posts, and updates the cursor; while fetching show a coral spinner + "불러오는 중…"; when nextCursor is null show "여기까지예요 🙌" (--color-ink3, NOT an error); on fetch failure show "피드를 불러오지 못했어요. 잠시 후 다시 시도해 주세요." with a retry. onHide removes a reported card from the local list. Use plain fetch — NO react-query. Feed list column gap 14px. Mount under the (mini) guard (file path already inside (mini)).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npm test -- tests/ui/feed-card.test.tsx tests/lib/feed-cursor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - app/(mini)/feed/page.tsx calls feedPage(null, tgId) with no owner-scope filter (public read)
    - Empty feed branch renders the "+ 나도 참고 인증하기" CTA
    - FeedList appends pages via GET /api/feed?cursor= and stops at "여기까지예요 🙌" when nextCursor null
    - FeedList uses plain fetch (no @tanstack/react-query import)
    - `npm run build` succeeds; feed-card + feed-cursor tests green
  </acceptance_criteria>
  <done>/feed page renders the first server page + empty state; FeedList appends keyset pages with end/error states; build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/feed | Only the opaque cursor crosses; decoded defensively, used only as a parameterized WHERE bound |
| DB → public feed render | Every row must pass the visibility gate before reaching any viewer |
| post.tgId → rendered author | Must surface only the anonymous handle, never the underlying Telegram identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | feed read (hidden/deleted leak) | mitigate | lib/feed.ts centralizes WHERE isNull(hiddenAt) AND isNull(deletedAt); both the RSC page and GET /api/feed call feedPage — no second un-gated query (Pitfall 3) |
| T-04-05 | Information Disclosure | author identity | mitigate | Handle computed from tgId via handleFor; no users join; Telegram username/firstName never read or rendered (D-01/02/03) |
| T-04-06 | Tampering | cursor query param | mitigate | Opaque base64url; decodeCursor in try/catch → null/400; only used as a Drizzle-parameterized WHERE bound (no injection) |
| T-04-07 | Spoofing | GET /api/feed | mitigate | requireSession() gates before any DB work; unauthenticated → 401 |
| T-04-SC | Tampering | npm installs | accept | No package installs this phase (RESEARCH: zero new packages) |
</threat_model>

<verification>
- `npm test` green (feed-cursor + feed-card + carried Plan 01 tests)
- `npm run build` + `npm run lint` clean
- Manual (post-deploy, see 04-VALIDATION): /feed shows cross-user posts from live Neon; 더 보기 paginates without dup/gap
</verification>

<success_criteria>
- /feed renders real shared-DB posts as FeedCards with anonymous handles + full payoff fields (FEED-01/04)
- Keyset 더 보기 paginates with no duplicate/gap including tied createdAt (FEED-02)
- Hidden/deleted posts excluded; empty feed shows the CTA
- RSC page and GET /api/feed share lib/feed.ts (no seam divergence)
</success_criteria>

<output>
Create `.planning/phases/04-feed/04-02-SUMMARY.md` when done
</output>

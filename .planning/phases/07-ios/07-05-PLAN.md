---
phase: 07-ios
plan: 05
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/(mini)/_components/Skeleton.tsx
  - app/(mini)/feed/loading.tsx
  - app/(mini)/stats/loading.tsx
  - app/(mini)/my/loading.tsx
  - app/(mini)/store/[id]/loading.tsx
  - app/(mini)/post/[id]/loading.tsx
  - app/(mini)/order/[id]/loading.tsx
  - app/(mini)/wait/[id]/loading.tsx
  - tests/ui/loading-skeletons.test.tsx
autonomous: true
requirements: [NATIVE-05]
must_haves:
  truths:
    - "7개 무거운 RSC 라우트 세그먼트가 전환 즉시 코랄-소프트 펄스 스켈레톤을 표시한다 (끊김 제거)"
    - "각 스켈레톤은 타깃 화면의 셸 레이아웃을 형상-에코해 콘텐츠 스왑이 위치 안정적이다"
  artifacts:
    - path: "app/(mini)/_components/Skeleton.tsx"
      provides: "재사용 스켈레톤 블록 프리미티브 (pulse, coral-soft)"
      contains: "color-primary-soft"
    - path: "app/(mini)/feed/loading.tsx"
      provides: "피드 세그먼트 Suspense fallback"
    - path: "tests/ui/loading-skeletons.test.tsx"
      provides: "7개 loading.tsx default-export 렌더 회귀"
  key_links:
    - from: "loading.tsx (7)"
      to: "Skeleton 프리미티브"
      via: "default-export RSC가 Skeleton 블록 조합 렌더"
      pattern: "Skeleton"
---

<objective>
NATIVE-05 슬라이스: 무거운 RSC 라우트 세그먼트(feed, stats, my, store/[id], post/[id], order/[id], wait/[id])에 `loading.tsx`를 추가해, RSC 네비게이션 중 즉시 코랄-소프트 펄스 스켈레톤을 표시함으로써 "뚝뚝 끊김"을 제거한다 (D-10). 단일 재사용 `Skeleton` 프리미티브(pulse 1.2s, `--color-primary-soft`, Plan 01이 추가한 pulse 키프레임 소비)를 만들고, 각 `loading.tsx`는 타깃 화면의 셸을 형상-에코하도록 그 프리미티브를 조합한다 — 7개 bespoke 스켈레톤이 아니라 1개 프리미티브의 파라미터화.

Purpose: 사용자가 탭/상세 전환 시 빈 화면 대신 즉각적 시각 피드백을 받아 앱이 빠르게 느껴진다. loading.tsx는 순수 서버 파일(SDK·데이터·param 없음)로 segment async RSC가 resolve되는 동안 Suspense fallback으로 렌더.
Output: `Skeleton` 프리미티브 + 7개 `loading.tsx` + Wave-0 렌더 회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ios/07-RESEARCH.md
@.planning/phases/07-ios/07-PATTERNS.md
@.planning/phases/07-ios/07-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): 7개 loading.tsx 렌더 회귀 테스트 작성</name>
  <files>tests/ui/loading-skeletons.test.tsx</files>
  <behavior>
    - 7개 loading.tsx 각각이 default-export 컴포넌트이고 RTL render에서 throw 없이 마운트
    - 렌더 결과에 pulse 애니메이션 블록(>=1)이 존재 (코랄-소프트 fill)
    - loading.tsx는 SDK import·param·async 없음 (순수 서버 컴포넌트)
  </behavior>
  <read_first>
    - 07-RESEARCH.md § Pattern 5 (loading.tsx 본문 L363-381) + Pitfall 4(static segment면 no-op 허용 L436-440) + § Validation(loading-skeletons row L526)
    - 07-UI-SPEC.md § Loading Skeleton Contract(1개 재사용 프리미티브, per-segment shape echo 표 L110-141)
    - tests/ui/home-shell.test.tsx — RTL 컴포넌트 렌더 하네스(서버 컴포넌트 직접 조합 렌더)
  </read_first>
  <action>
    tests/ui/loading-skeletons.test.tsx를 작성한다. 7개 loading default-export를 import하고 각각 RTL render → unmount가 throw하지 않는지(`expect(()=>render(<Loading/>)).not.toThrow()`) + 컨테이너에 펄스 블록(예: animation에 'pulse' 또는 background에 var(--color-primary-soft)를 쓰는 요소)이 1개 이상 존재하는지 단언한다. 각 loading.tsx가 'use client'/SDK import를 갖지 않음을 fs 소스 읽기로 보조 단언(SDK·useEffect 부재). 구현 전 RED(파일 미존재). 7개 import를 모두 포함하되, 파일이 아직 없으므로 처음엔 import 실패 = RED.
  </action>
  <acceptance_criteria>
    - tests/ui/loading-skeletons.test.tsx 존재, 7개 loading default-export render not.toThrow + 펄스 블록 존재 단언
    - SDK/'use client' 부재 보조 단언
    - 구현 전 RED
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/ui/loading-skeletons.test.tsx 2>&1 | grep -qE "fail|FAIL|✗|×|Cannot find module" && echo "RED-as-expected"</automated>
  </verify>
  <done>테스트 파일 존재, 7개 렌더+펄스 단언, 구현 전 RED.</done>
</task>

<task type="auto">
  <name>Task 2: Skeleton 프리미티브 + 7개 loading.tsx 형상-에코 구현</name>
  <files>app/(mini)/_components/Skeleton.tsx, app/(mini)/feed/loading.tsx, app/(mini)/stats/loading.tsx, app/(mini)/my/loading.tsx, app/(mini)/store/[id]/loading.tsx, app/(mini)/post/[id]/loading.tsx, app/(mini)/order/[id]/loading.tsx, app/(mini)/wait/[id]/loading.tsx</files>
  <read_first>
    - 07-UI-SPEC.md § Loading Skeleton Contract(블록 프리미티브 속성 L114-122 + per-segment shape echo 표 L126-137: feed=4×카드, stats=hero+3타일+차트, my=프로필+요약+레코드, store=hero+메뉴행, post=영수증+듀얼사진+캡션, order=영수증+CTA높이블록, wait=스텝퍼+맵+게이지)
    - 07-PATTERNS.md § 7×loading.tsx(L234-255, 순수 서버 파일, --color-primary-soft L22 + pulse 키프레임 + 인라인-스타일 토큰 컨벤션)
    - app/globals.css — Plan 01이 추가한 @keyframes pulse + --color-primary-soft 토큰
    - order/[id]/page.tsx 하단 CTA(var(--safe-b) — order 스켈레톤이 CTA-높이 블록을 --safe-b 위에 배치)
  </read_first>
  <action>
    먼저 app/(mini)/_components/Skeleton.tsx를 만든다 — 재사용 블록 프리미티브: props {height, radius, width?}를 받아 div를 렌더하되 background `var(--color-primary-soft)`, `animation: 'pulse 1.2s ease-in-out infinite'`, borderRadius=radius, height/width 인라인 스타일. 텍스트·이모지·SDK 없음. 'use client' 불필요(순수 표현). 이어서 7개 loading.tsx를 각각 순수 서버 컴포넌트(default export, async 아님, SDK import 없음)로 작성하고 Skeleton 프리미티브를 조합해 UI-SPEC per-segment shape echo를 형상-에코한다: feed=16px 패딩 컨테이너에 4× full-width 카드 블록(~120px, radius 18, gap 12); stats=hero 블록(~96px)+3타일 행+wide 차트 블록; my=프로필 헤더(아바타 원+2 텍스트 바)+누적요약 블록+2~3 레코드 카드; store=store-hero 블록+3~4 메뉴-행; post=영수증 블록+2× 듀얼-사진 정사각 나란히+캡션 필드; order=영수증 요약+--safe-b 위 CTA-높이 블록; wait=스텝퍼 행+큰 맵 블록+게이지 블록. 잠긴 radii/16px 패딩 토큰을 재사용(새 측정 발명 금지). 그레이·full --color-primary 금지(스켈레톤은 --color-primary-soft만). static으로 판명된 세그먼트의 스켈레톤은 무해한 no-op(Pitfall 4) — 제거하지 말고 SUMMARY에 기록.
  </action>
  <acceptance_criteria>
    - app/(mini)/_components/Skeleton.tsx 존재, --color-primary-soft + pulse 애니메이션 블록 프리미티브
    - 7개 loading.tsx 모두 존재, default-export 순수 서버 컴포넌트, Skeleton 조합, SDK/'use client' 없음
    - per-segment shape echo가 UI-SPEC 표와 일치(feed 4카드, stats hero+3타일+차트 등)
    - 그레이/full --color-primary 미사용, 텍스트/이모지 없음
    - Task 1 테스트 GREEN
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/ui/loading-skeletons.test.tsx && ls 'app/(mini)/feed/loading.tsx' 'app/(mini)/stats/loading.tsx' 'app/(mini)/my/loading.tsx' 'app/(mini)/store/[id]/loading.tsx' 'app/(mini)/post/[id]/loading.tsx' 'app/(mini)/order/[id]/loading.tsx' 'app/(mini)/wait/[id]/loading.tsx' && grep -q 'color-primary-soft' 'app/(mini)/_components/Skeleton.tsx' && echo OK</automated>
  </verify>
  <done>Skeleton 프리미티브 + 7개 loading.tsx 형상-에코, 순수 서버, coral-soft only, 테스트 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

새 신뢰 경계·입력·엔드포인트·스키마 없음 (07-RESEARCH § Security Domain V4: loading.tsx는 데이터 렌더 안 하고 param 안 받음). 순수 표현 서버 파일.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Info Disclosure | loading.tsx가 데이터를 노출 | accept | loading.tsx는 정적 셸 형상만 렌더 — 데이터·param·SDK 없음(07-RESEARCH § Security V4). 누출 표면 없음 |

high-severity 없음. SDK 호출 없음(서버 파일) → 가용성 위협도 무관.
</threat_model>

<verification>
- npm test 그린 (loading-skeletons 테스트 포함)
- 7개 loading.tsx + Skeleton.tsx 존재, 순수 서버(SDK/'use client' 없음)
- npm run build 클린
</verification>

<success_criteria>
- 7개 무거운 RSC 세그먼트에 loading.tsx 존재, 즉시 코랄-소프트 펄스 스켈레톤
- 단일 Skeleton 프리미티브 재사용, per-segment shape echo로 위치 안정적 스왑
- 그레이/full coral 미사용, 텍스트/이모지 없음
- Wave-0 렌더 테스트 그린
</success_criteria>

<output>
Create `.planning/phases/07-ios/07-05-SUMMARY.md` when done
</output>

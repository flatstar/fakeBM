---
phase: 07-ios
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - lib/haptics.ts
  - tests/lib/haptics.test.ts
autonomous: true
requirements: [NATIVE-03]
must_haves:
  truths:
    - "lib/haptics.haptic.impact/notify/selection이 텔레그램 안에서는 SDK 햅틱을 호출한다"
    - "SDK 미가용(SSR/비텔레그램/미초기화/미지원)에서는 no-op이며 throw하지 않는다"
  artifacts:
    - path: "lib/haptics.ts"
      provides: "ifAvailable 가드 햅틱 래퍼 (impact/notify/selection)"
      contains: "ifAvailable"
    - path: "tests/lib/haptics.test.ts"
      provides: "가용/미가용 토글 → 호출 vs no-op-no-throw 단언"
  key_links:
    - from: "lib/haptics.ts"
      to: "@telegram-apps/sdk-react haptic fns"
      via: ".ifAvailable() 가드 호출"
      pattern: "ifAvailable"
---

<objective>
NATIVE-03 코어 슬라이스: 전역에서 재사용할 `lib/haptics.ts` 안전 모듈을 만든다 (D-05). SDK `hapticFeedbackImpactOccurred`/`...NotificationOccurred`/`...SelectionChanged`를 `.ifAvailable(...)`로 감싸 SSR·비텔레그램·미초기화·미지원에서 자동 no-op(throw 없음)으로 동작하게 한다. D-06 매핑(탭→selection, CTA→impact 'medium', 성공→notification 'success', 에러→notification 'error')을 호출자가 쓸 수 있도록 얇은 API를 노출한다.

Purpose: 이후 모든 슬라이스(FAB·네이티브 버튼·좋아요·CTA)가 단일 안전 헬퍼를 호출해 햅틱을 발생. `lib/streak.ts`류 import-경량 안전 모듈 컨벤션을 따라 trivially 테스트 가능.
Output: `lib/haptics.ts` + Wave-0 단위 테스트(가용/미가용 양쪽).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ios/07-RESEARCH.md
@.planning/phases/07-ios/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): haptics 가용/미가용 테스트 작성</name>
  <files>tests/lib/haptics.test.ts</files>
  <behavior>
    - SDK 가용(isAvailable→true): `haptic.impact('medium')`이 `hapticFeedbackImpactOccurred`를 'medium'으로 호출
    - 가용: `haptic.notify('success')`가 `hapticFeedbackNotificationOccurred`를 'success'로 호출
    - 가용: `haptic.selection()`이 `hapticFeedbackSelectionChanged` 호출
    - 미가용(ifAvailable→[false]): 세 호출 모두 no-op, throw 없음 (expect(() => ...).not.toThrow())
    - `haptic.impact()` 인자 생략 시 기본 'medium' 디스패치
  </behavior>
  <read_first>
    - tests/ui/share-sheet.test.tsx L26-40 — `vi.hoisted` + `Object.assign(fn, { isAvailable / ifAvailable })` + `vi.mock('@telegram-apps/sdk')` 패턴(07-PATTERNS § SDK-mock test pattern L277-279)
    - 07-RESEARCH.md § Pattern 1 (lib/haptics 본문 L281-299) + § Haptics 검증 enum(impact: light|medium|heavy|rigid|soft; notification: error|success|warning) L116-131
  </read_first>
  <action>
    `tests/lib/haptics.test.ts`를 작성한다. `@telegram-apps/sdk-react`를 mock하되 re-export되는 코어 이름(`hapticFeedbackImpactOccurred`/`hapticFeedbackNotificationOccurred`/`hapticFeedbackSelectionChanged`)을 `Object.assign(spyFn, { ifAvailable: (...a) => state.available ? (spyFn(...a), [true]) : [false] })` 형태로 감싼다(share-sheet 패턴). 토글 가능한 `state.available`로 가용/미가용 두 케이스를 구동한다. 가용 시 각 spy가 올바른 enum 인자로 호출됨을, 미가용 시 호출 0회 + `.not.toThrow()`를 단언한다. impact 기본값 'medium' 케이스 포함. 구현(Task 2) 전 RED.
  </action>
  <acceptance_criteria>
    - `tests/lib/haptics.test.ts` 존재, SDK ifAvailable mock 토글 패턴 사용
    - 가용 3종 호출 + enum 인자 단언, 미가용 no-op-no-throw 단언, impact 기본값 단언
    - 구현 전 RED
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/lib/haptics.test.ts 2>&1 | grep -qE "fail|FAIL|✗|×|Cannot find module" && echo "RED-as-expected"</automated>
  </verify>
  <done>테스트 파일 존재, 가용/미가용 양쪽 단언, 구현 전 RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/haptics.ts 구현</name>
  <files>lib/haptics.ts</files>
  <behavior>
    - `haptic.impact(style='medium')` → `hapticFeedbackImpactOccurred.ifAvailable(style)`
    - `haptic.notify(type)` → `hapticFeedbackNotificationOccurred.ifAvailable(type)`
    - `haptic.selection()` → `hapticFeedbackSelectionChanged.ifAvailable()`
    - 어떤 경로에서도 throw하지 않음(ifAvailable가 미가용을 삼킴)
  </behavior>
  <read_first>
    - 07-RESEARCH.md § Pattern 1 (정확한 본문 + import alias L281-299) + § SafeWrapped 계약(ifAvailable이 SSR/비TMA/미초기화/미지원 모두 no-op L107-114)
    - lib/streak.ts L1-12 — import-0 안전 모듈 doc-header + named-export 컨벤션(07-PATTERNS L98-107)
    - 07-RESEARCH.md 타입: ImpactHapticFeedbackStyle, NotificationHapticFeedbackType
  </read_first>
  <action>
    `lib/haptics.ts`를 작성한다. `@telegram-apps/sdk-react`에서 `hapticFeedbackImpactOccurred as impact`, `hapticFeedbackNotificationOccurred as notify`, `hapticFeedbackSelectionChanged as selection` + 타입 `ImpactHapticFeedbackStyle`/`NotificationHapticFeedbackType`을 import한다. `haptic` 객체로 `impact(s='medium')`/`notify(t)`/`selection()`을 named-export하며 각각 `<fn>.ifAvailable(...)`만 호출한다. `lib/streak.ts`류 doc-header로 no-op 안전 계약(SSR/비텔레그램에서 no-op, manual window/try-catch 불필요 — RESEARCH Anti-Pattern)을 명시한다. window 가드·try-catch를 추가하지 않는다(ifAvailable가 이미 커버). 모듈 스코프 import는 이 파일이 클라이언트 컴포넌트에서만 호출되므로 안전하나, 'use client' 마킹은 호출자 책임 — 본 모듈은 순수 wrapper.
  </action>
  <acceptance_criteria>
    - `lib/haptics.ts` 존재, `haptic.impact/notify/selection` named-export
    - 세 메서드 모두 `.ifAvailable(...)`만 호출, raw 호출/window 가드/try-catch 없음
    - impact 기본 인자 'medium'
    - Task 1 테스트 GREEN
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/lib/haptics.test.ts && grep -q 'ifAvailable' lib/haptics.ts && ! grep -q 'try {' lib/haptics.ts && echo OK</automated>
  </verify>
  <done>lib/haptics.ts 구현, ifAvailable-only, 테스트 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

새 신뢰 경계·입력·엔드포인트·스키마 없음 (07-RESEARCH § Security Domain). 클라이언트 SDK 햅틱 호출만 추가.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | DoS (availability) | lib/haptics.ts SDK 호출 | mitigate | `.ifAvailable()`이 SSR/비텔레그램/미초기화/미지원을 모두 no-op으로 삼킴 — throw로 인한 크래시/SSR 누출 없음 (07-RESEARCH § SafeWrapped, Pitfall 3) |

high-severity 없음. 햅틱은 무상태 bridge 호출로 데이터 경계 없음.
</threat_model>

<verification>
- `npm test` 그린 (haptics 테스트 포함)
- `lib/haptics.ts`에 window/try-catch 가드 부재 (ifAvailable-only)
- `npm run build` 클린
</verification>

<success_criteria>
- `lib/haptics.ts`가 impact/notify/selection을 `.ifAvailable()`로 노출
- 가용 시 SDK 햅틱 디스패치, 미가용 시 no-op-no-throw
- Wave-0 테스트 그린
</success_criteria>

<output>
Create `.planning/phases/07-ios/07-02-SUMMARY.md` when done
</output>

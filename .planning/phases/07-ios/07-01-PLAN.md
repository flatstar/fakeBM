---
phase: 07-ios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - lib/telegram.ts
  - components/BottomNav.tsx
  - components/TgMainButton.tsx
  - app/(mini)/order/[id]/page.tsx
  - app/(mini)/_components/ShareEntryButton.tsx
  - app/(mini)/feed/_components/ReportMenu.tsx
  - app/share/[id]/_components/ShareSheet.tsx
  - tests/ui/safe-area-token.test.ts
autonomous: true
requirements: [NATIVE-01]
must_haves:
  truths:
    - "iOS Telegram의 하단 고정 요소가 content-safe-area inset을 반영해 홈 인디케이터에 안 가린다"
    - "단일 --safe-b 토큰이 6개 하단 고정 파일에서 공유되어 env() 직접 사용이 0이다"
    - "부팅 시 expandViewport()가 호출되어 iOS 부분-높이 진입이 풀 높이로 확장된다"
  artifacts:
    - path: "app/globals.css"
      provides: "--safe-b 토큰 + pulse 키프레임"
      contains: "--safe-b"
    - path: "lib/telegram.ts"
      provides: "boot에 expandViewport() 추가"
      contains: "expandViewport"
    - path: "tests/ui/safe-area-token.test.ts"
      provides: "토큰 존재 + 6파일 env() 부재 회귀 가드"
  key_links:
    - from: "6 bottom-fixed surfaces"
      to: "var(--safe-b)"
      via: "calc(Xpx + var(--safe-b))"
      pattern: "var\\(--safe-b\\)"
    - from: "lib/telegram.ts boot"
      to: "expandViewport"
      via: "isAvailable() 가드 호출"
      pattern: "expandViewport"
---

<objective>
NATIVE-01 슬라이스: iOS 텔레그램 WebView에서 `env(safe-area-inset-bottom)`이 0을 반환해 하단 네비/CTA가 홈 인디케이터에 가리는 근본 버그를, SDK가 이미 바인딩한 `--tg-viewport-content-safe-area-inset-bottom` 우선 토큰(`--safe-b`)으로 중앙화해 해소한다 (D-01/D-02). 동시에 부팅 시 `expandViewport()`를 호출해 부분-높이 진입을 풀 높이로 확장한다 (D-03).

Purpose: 라이브 iOS 테스트에서 드러난 1순위 결함(하단 탭 불안정) 제거. 단일 토큰 중앙화로 6개 파일이 일괄 보정되고, 향후 추가 하단 요소도 같은 토큰만 쓰면 안전.
Output: `--safe-b` 토큰 + `pulse` 키프레임(globals.css), 6개 파일의 `env()`→`var(--safe-b)` 스왑, boot `expandViewport()`, Wave-0 회귀 테스트.

참고(doc drift): 실제 하단(`env(safe-area-inset-bottom)`) 콜사이트는 **6곳**이다 — BottomNav, TgMainButton(CSS L34 + 추가로 L6 doc-comment의 동일 참조), order/[id]/page, ShareEntryButton, ReportMenu(2 사이트), ShareSheet. 07-UI-SPEC의 "8"은 top inset 1곳 + 추정치를 합산한 값이라 하단 스왑 대상과 다르다(이 차이를 executor phantom-miss로 오인하지 말 것).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ios/07-RESEARCH.md
@.planning/phases/07-ios/07-PATTERNS.md
@.planning/phases/07-ios/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): safe-area 토큰 회귀 테스트 작성</name>
  <files>tests/ui/safe-area-token.test.ts</files>
  <read_first>
    - tests/ui/share-sheet.test.tsx — 기존 SDK-mock + 문자열/소스 단언 컨벤션 (이 파일은 SDK mock 불필요, 순수 소스 읽기 단언)
    - app/globals.css — `--safe-b`가 추가될 `:root` 블록과 `@keyframes` 그룹 (07-PATTERNS L33-71)
    - 07-RESEARCH.md § Validation Architecture (NATIVE-01 row) — 토큰 존재 + 6파일 env() 부재 단언 명세
  </read_first>
  <action>
    `tests/ui/safe-area-token.test.ts`를 작성한다 (`@vitest-environment node` — 파일시스템 소스 읽기). fs로 `app/globals.css`를 읽어 `--safe-b`가 정의되어 있고 그 정의가 `--tg-viewport-content-safe-area-inset-bottom`와 `env(safe-area-inset-bottom`을 `max(...)`로 결합하는지 정규식 단언한다. 이어서 6개 하단 고정 파일(components/BottomNav.tsx, components/TgMainButton.tsx, app/(mini)/order/[id]/page.tsx, app/(mini)/_components/ShareEntryButton.tsx, app/(mini)/feed/_components/ReportMenu.tsx, app/share/[id]/_components/ShareSheet.tsx)을 각각 읽어 `env(safe-area-inset-bottom`이 **파일 전체에서 0회** 등장하고(주석 포함 — TgMainButton의 L6 doc-comment까지 모두 스왑되어야 하므로 카운트는 0) `var(--safe-b)`가 1회 이상 등장하는지 단언한다. ReportMenu는 2개 사이트이므로 `var(--safe-b)` 2회 이상. 이 테스트는 Task 2/3 전에 RED로 실패해야 한다 (토큰·스왑 아직 없음).
  </action>
  <acceptance_criteria>
    - `tests/ui/safe-area-token.test.ts` 파일이 존재한다
    - 테스트가 globals.css에서 `--safe-b` 정의 + `max(var(--tg-viewport-content-safe-area-inset-bottom...), env(safe-area-inset-bottom...))` 구조를 단언한다
    - 테스트가 6개 파일에서 `env(safe-area-inset-bottom` == 0(주석 포함), `var(--safe-b)` >= 1을 단언한다
    - 작성 직후 실행 시 RED (토큰/스왑 미존재로 실패)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/ui/safe-area-token.test.ts 2>&1 | grep -qE "fail|FAIL|✗|×" && echo "RED-as-expected"</automated>
  </verify>
  <done>회귀 테스트 파일 존재, 토큰+6파일 단언 포함, 구현 전 RED.</done>
</task>

<task type="auto">
  <name>Task 2: --safe-b 토큰 + pulse 키프레임 정의 + boot expandViewport()</name>
  <files>app/globals.css, lib/telegram.ts</files>
  <read_first>
    - app/globals.css L20-25(--color-primary-soft 등 토큰) + `:root` 브리지 패턴(07-PATTERNS L37-55, 기존 `:root { --font-pretendard }` L75-77) + `@keyframes` 그룹(07-PATTERNS L57-71, 기존 fadeUp L51-54)
    - lib/telegram.ts boot 블록 L54-67 (07-PATTERNS L128-147): initSDK()→backButton.mount()→miniApp.mount()→themeParams.bindCssVars()→initData.restore()→viewport.mount().then(bindCssVars). L32-41 동적 import 디스트럭처.
    - 07-RESEARCH.md § Safe-Area (정확한 토큰 정의 L199-206) + § Boot edit (expandViewport 가드 L458-463) + Pitfall 2 (두 번째 bindCssVars 금지)
  </read_first>
  <action>
    globals.css의 `:root`에 `--safe-b: max(var(--tg-viewport-content-safe-area-inset-bottom, 0px), env(safe-area-inset-bottom, 0px))` 토큰을 추가한다 — `@theme` 안이 아니라 `:root`(use-time에 var()+env()를 합성하므로, 기존 `:root { --font-pretendard }` 브리지와 동일 위치). 기존 `@keyframes fadeUp` 그룹 옆에 `@keyframes pulse { 0%,100% { opacity: .55 } 50% { opacity: 1 } }`를 추가한다(NATIVE-05가 소비, Plan 05). lib/telegram.ts: 동적 import 디스트럭처(L32-41)에 `expandViewport`를 추가하고, boot try 블록 내 `initSDK()` 직후에 `expandViewport.isAvailable() && expandViewport();`를 호출한다(D-03). 두 번째 `viewport.bindCssVars()`는 절대 추가하지 않는다(Pitfall 2: CSSVarsBoundError). 기존 boot 순서/가드는 보존.
  </action>
  <acceptance_criteria>
    - globals.css `:root`에 `--safe-b` 토큰 존재, `--tg-viewport-content-safe-area-inset-bottom` + `env(safe-area-inset-bottom)`를 `max()`로 결합
    - globals.css에 `@keyframes pulse` 존재 (opacity .55↔1)
    - lib/telegram.ts boot에 `expandViewport`가 import되고 `isAvailable()` 가드로 호출됨
    - boot에 두 번째 `bindCssVars()` 호출 없음 (기존 1회만)
  </acceptance_criteria>
  <verify>
    <automated>grep -q -- '--safe-b' app/globals.css && grep -q 'expandViewport' lib/telegram.ts && grep -q '@keyframes pulse' app/globals.css && echo OK</automated>
  </verify>
  <done>--safe-b·pulse 토큰 정의, boot에 가드된 expandViewport(), 재바인딩 없음.</done>
</task>

<task type="auto">
  <name>Task 3: 6개 하단 고정 파일 env()→var(--safe-b) 스왑</name>
  <files>components/BottomNav.tsx, components/TgMainButton.tsx, app/(mini)/order/[id]/page.tsx, app/(mini)/_components/ShareEntryButton.tsx, app/(mini)/feed/_components/ReportMenu.tsx, app/share/[id]/_components/ShareSheet.tsx</files>
  <read_first>
    - 07-PATTERNS.md § "6 safe-area swap files" 정확한 스왑 표(L79-90): 각 파일·라인·현재 텍스트·교체 텍스트
    - 07-UI-SPEC.md § Spacing Scale 8 call-site 표(L56-68) — 상수 Xpx 보존 명세
    - 각 파일의 해당 라인: BottomNav L49, TgMainButton **L6(doc-comment) + L34(CSS)**, order/[id]/page L164, ShareEntryButton L118, ReportMenu L109+L177, ShareSheet L209
  </read_first>
  <action>
    각 파일에서 리터럴 `env(safe-area-inset-bottom)`을 `var(--safe-b)`로 교체한다 — `calc()` 안의 상수 Xpx는 정확히 보존(시각 패딩 변화 없음, inset 보정만). 교체 매핑: BottomNav L49 `calc(22px + env(...))`→`calc(22px + var(--safe-b))`; TgMainButton L34 `calc(14px + env(...))`→`calc(14px + var(--safe-b))`; order/[id]/page L164 `calc(14px + env(...))`→`calc(14px + var(--safe-b))`; ShareEntryButton L118 `calc(90px + env(...))`→`calc(90px + var(--safe-b))`; ReportMenu L109 `calc(20px + env(...))` + L177 `calc(90px + env(...))` 둘 다 `var(--safe-b)`; ShareSheet L209 `calc(28px + env(...))`→`calc(28px + var(--safe-b))`. **TgMainButton은 L34의 실제 CSS뿐 아니라 L6의 doc-comment(`safe-area bottom padding calc(14px + env(safe-area-inset-bottom))`)도 함께 수정해 `calc(14px + var(--safe-b))`로 바꾼다** — 그래야 파일 전체에서 `env(safe-area-inset-bottom)` 등장이 0이 되어 Task 1의 `count == 0` 단언과 이 태스크의 grep verify가 둘 다 통과한다(주석이 살아남아 자가-무효화되는 BLOCKER 회피). `env()` fallback은 이제 `--safe-b` 토큰 내부에 있으므로 소비처는 raw `env(`를 완전히 제거한다(RESEARCH Anti-Pattern: bare env() 금지). app/layout.tsx의 `env(safe-area-inset-top)`은 top inset이라 범위 밖 — 건드리지 않는다.
  </action>
  <acceptance_criteria>
    - 6개 파일 전체에서 `env(safe-area-inset-bottom` grep 카운트 0 (TgMainButton은 L6 doc-comment + L34 CSS 모두 스왑되어 0)
    - 6개 파일이 각각 `var(--safe-b)` 사용 (ReportMenu는 2회)
    - calc 상수(22/14/14/90/20+90/28px)가 보존됨
    - app/layout.tsx의 `env(safe-area-inset-top)`은 변경되지 않음
    - Task 1 회귀 테스트가 GREEN으로 전환
  </acceptance_criteria>
  <verify>
    <automated>! grep -rl 'env(safe-area-inset-bottom' components/BottomNav.tsx components/TgMainButton.tsx 'app/(mini)/order/[id]/page.tsx' 'app/(mini)/_components/ShareEntryButton.tsx' 'app/(mini)/feed/_components/ReportMenu.tsx' 'app/share/[id]/_components/ShareSheet.tsx' && npx vitest run tests/ui/safe-area-token.test.ts</automated>
  </verify>
  <done>6개 파일(TgMainButton은 doc-comment+CSS 둘 다) raw env() 제거 + var(--safe-b) 적용, top inset 보존, 회귀 테스트 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

이 플랜은 **새 신뢰 경계·입력·엔드포인트·스키마를 추가하지 않는다** (07-RESEARCH § Security Domain 확인: V2/V3/V4/V5/V6 모두 no). CSS 토큰 정의와 boot SDK 호출만 변경한다.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | DoS (availability) | lib/telegram.ts boot expandViewport() | mitigate | `expandViewport.isAvailable() && expandViewport()` 가드 — SSR/비텔레그램/미초기화에서 no-op, throw 없음 (07-RESEARCH Pitfall 3) |
| T-07-02 | DoS (availability) | viewport.bindCssVars 재호출 | mitigate | 두 번째 bindCssVars() 추가 금지 — boot 1회만(CSSVarsBoundError 회피, Pitfall 2) |

high-severity 항목 없음 (security_block_on: high 미발동). 이 슬라이스는 클라이언트 CSS/boot 폴리시로 데이터 누출/크래시 표면 없음.
</threat_model>

<verification>
- `npm test` 그린 (safe-area-token 테스트 포함)
- `grep -r 'env(safe-area-inset-bottom' app components` → 0건 (top inset 제외)
- `npm run build` 클린
</verification>

<success_criteria>
- `--safe-b` 토큰이 globals.css `:root`에 정의되고 content-safe-area inset 우선 + env() fallback
- 6개 하단 고정 파일이 `var(--safe-b)` 사용, raw `env(safe-area-inset-bottom)` 0건 (주석 포함)
- boot에 가드된 `expandViewport()` 추가, 재바인딩 없음
- `pulse` 키프레임 정의 (Plan 05 소비 대비)
- Wave-0 회귀 테스트 그린
</success_criteria>

<output>
Create `.planning/phases/07-ios/07-01-SUMMARY.md` when done
</output>

---
phase: 07-ios
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - components/BottomNav.tsx
  - app/(mini)/_components/WelcomeIntro.tsx
  - app/(mini)/post/[id]/_components/PostClient.tsx
  - app/(mini)/wait/[id]/_components/DeliveryClient.tsx
  - app/(mini)/wait/[id]/_components/CancelModal.tsx
  - app/(mini)/_components/ShareEntryButton.tsx
  - app/(mini)/store/[id]/_components/StoreMenu.tsx
  - app/(mini)/cart/page.tsx
  - app/(mini)/order/[id]/page.tsx
  - app/(mini)/order/[id]/_components/OrderBackButton.tsx
  - app/(mini)/feed/_components/LikeButton.tsx
  - tests/ui/bottom-nav-fab.test.tsx
autonomous: true
requirements: [NATIVE-02, NATIVE-03, NATIVE-04]
must_haves:
  truths:
    - "중앙 참기 FAB 탭이 haptic.impact('medium') + router.push('/home')로 주문 플로우에 진입한다 (죽은 FAB 해소)"
    - "하단 네비 탭이 haptic.selection()을 발생시킨다"
    - "label-only 1차 CTA가 useNativeMainButton으로 네이티브화되고, sub/커스텀색 CTA는 DOM TgMainButton + haptic으로 유지된다"
    - "D-09의 5개 라우트(store/[id], post/[id], order/[id], wait/[id], cart) 전부에서 useNativeBackButton이 BackButton을 노출한다"
    - "좋아요 토글이 haptic.selection()을 발생시킨다 (D-06 like→selection 매핑)"
  artifacts:
    - path: "components/BottomNav.tsx"
      provides: "FAB onCenter 기본값 + 탭 haptic"
      contains: "router.push('/home')"
    - path: "app/(mini)/order/[id]/_components/OrderBackButton.tsx"
      provides: "order/[id] 서버 페이지용 BackButton client island"
      contains: "useNativeBackButton"
    - path: "tests/ui/bottom-nav-fab.test.tsx"
      provides: "FAB 기본 push+haptic 회귀"
  key_links:
    - from: "BottomNav FAB"
      to: "router.push('/home') + haptic"
      via: "onCenter ?? default"
      pattern: "push\\('/home'\\)"
    - from: "label-only CTA 화면"
      to: "useNativeMainButton"
      via: "훅 채택 + DOM fallback 억제"
      pattern: "useNativeMainButton"
    - from: "5개 상세/서브 라우트 (cart·order 포함)"
      to: "useNativeBackButton"
      via: "client island 훅 호출"
      pattern: "useNativeBackButton"
    - from: "LikeButton 토글"
      to: "haptic.selection()"
      via: "onTap 진입 시 호출"
      pattern: "haptic\\.selection"
---

<objective>
NATIVE-02 + NATIVE-03(적용) + NATIVE-04(채택) 통합 슬라이스: (1) `BottomNav` 중앙 "참기" FAB의 `onCenter` 기본값을 `haptic.impact('medium')` + `router.push('/home')`로 배선해 죽은 FAB 버그를 해소하고(D-04), 네비 탭에 `haptic.selection()`을 추가한다(D-06). (2) Plan 03의 `useNativeMainButton`/`useNativeBackButton` 훅을 실제 화면에 채택한다 — label-only 1차 CTA는 네이티브화(DOM fallback 억제), sub/커스텀색 CTA(CancelModal #8a5a3a, ShareEntryButton sub)는 DOM 유지 + 햅틱 보강(D-07/D-08). D-09의 **5개 라우트 전부**(store/[id], post/[id], order/[id], wait/[id], cart)에 BackButton을 노출한다(D-09 — 부분 적용·후속 메모 deferral 금지). (3) 좋아요 토글에 `haptic.selection()`을 배선한다(D-06 like→selection 매핑).

Purpose: 앞 세 플랜이 만든 토큰·헬퍼·훅을 실제 사용자 동작에 연결 — 이 슬라이스 후 사용자가 FAB로 주문에 진입하고, 탭/CTA/좋아요에서 햅틱을 느끼며, 5개 라우트 전부에서 네이티브 BackButton을 본다.
Output: FAB 배선 + 탭 햅틱(BottomNav), CTA 콜사이트의 네이티브/DOM 결정 적용, 5개 라우트 BackButton 채택(cart는 직접 훅, order는 client island), 좋아요 selection 햅틱, Wave-0 FAB 테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ios/07-RESEARCH.md
@.planning/phases/07-ios/07-PATTERNS.md
@.planning/phases/07-ios/07-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): FAB 기본 동작 테스트 + FAB/탭 햅틱 배선</name>
  <files>tests/ui/bottom-nav-fab.test.tsx, components/BottomNav.tsx</files>
  <behavior>
    - FAB 클릭(onCenter prop 없음): router.push('/home') 호출 + haptic.impact('medium') 호출
    - onCenter prop이 주어지면 기본값 대신 그 prop이 호출됨
    - 네비 탭 Link 클릭: haptic.selection() 호출
  </behavior>
  <read_first>
    - components/BottomNav.tsx — FAB onClick L62(onCenter? 미설정 L37/L40), 탭 Link L92-117, 이미 'use client' L14 (07-PATTERNS L215-231)
    - 07-RESEARCH.md § Pattern 4 (FAB onCenter 기본값 L352-361) + § Validation(bottom-nav-fab row L524)
    - tests/ui/share-sheet.test.tsx — vi.mock('next/navigation') + vi.mock('@/lib/haptics') mock 패턴; tests/ui/home-shell.test.tsx — BottomNav 렌더 하네스
  </read_first>
  <action>
    먼저 tests/ui/bottom-nav-fab.test.tsx를 작성한다: next/navigation useRouter를 push 스파이로, @/lib/haptics를 impact/selection 스파이 객체로 mock한다. RTL로 BottomNav(onCenter 없이) 렌더 → FAB(aria-label="참기") 클릭 → push('/home') + impact('medium') 단언. 탭 Link 클릭 → selection() 단언. onCenter prop 전달 케이스도 단언(기본값 미사용). 구현 전 RED. 이어서 components/BottomNav.tsx를 수정한다: useRouter(next/navigation) + haptic(@/lib/haptics) import 추가, const handleCenter = onCenter ?? (() => { haptic.impact('medium'); router.push('/home'); }) 로 기본값 부여하고 FAB onClick에 handleCenter 배선. 각 네비 Link에 onClick으로 haptic.selection() 추가(D-06 nav-select). FAB 시각·aria-label·✋(U+270B)는 변경하지 않는다(UI-SPEC: 픽셀 불변, U+1FAF7 금지). (mini)/layout.tsx는 변경 없음(prop 없이 렌더, 기본값이 처리).
  </action>
  <acceptance_criteria>
    - tests/ui/bottom-nav-fab.test.tsx 존재, FAB 기본 push('/home')+impact('medium') + 탭 selection + onCenter override 단언
    - BottomNav가 router.push('/home') + haptic.impact('medium') 기본 FAB 동작, 탭에 haptic.selection()
    - FAB 시각/✋ U+270B/aria-label 불변, U+1FAF7 미사용
    - 테스트 GREEN
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/ui/bottom-nav-fab.test.tsx && grep -q "push('/home')" components/BottomNav.tsx && grep -q 'haptic.selection' components/BottomNav.tsx && echo OK</automated>
  </verify>
  <done>FAB 테스트 + FAB/탭 햅틱 배선, 시각 불변, 테스트 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: label-only CTA 네이티브화 (useNativeMainButton 채택, DOM fallback 억제)</name>
  <files>app/(mini)/_components/WelcomeIntro.tsx, app/(mini)/post/[id]/_components/PostClient.tsx, app/(mini)/store/[id]/_components/StoreMenu.tsx</files>
  <read_first>
    - 07-PATTERNS.md § "CTA call-site adoption" 표(L200-211): WelcomeIntro "시작하기"=NATIVE, PostClient L311 인증 CTA=NATIVE, store 1차 CTA=NATIVE; PostClient.tsx L21-29 client-island import shape
    - 07-UI-SPEC.md § Progressive enhancement contract(네이티브 지원 시 DOM TgMainButton 억제 — 정확히 1개 CTA만 보임 L160-162) + Native MainButton Params(text=기존 라벨 verbatim L150-157)
    - hooks/useNativeMainButton.ts (Plan 03 산출 — 시그니처 {text,onClick,disabled,loading})
    - 각 파일의 현재 TgMainButton 렌더 위치 + 라벨 텍스트
  </read_first>
  <action>
    label-only 1차 CTA를 가진 3개 client island에서 useNativeMainButton({text, onClick, disabled, loading})을 채택한다. text는 기존 DOM TgMainButton 라벨을 verbatim으로 전달(copy 불변 — UI-SPEC). onClick/disabled/loading은 기존 핸들러·상태를 그대로 매핑. 점진적 향상: setMainButtonParams.isAvailable()이 true면 네이티브가 보이므로 그 CTA의 DOM TgMainButton을 억제(렌더 조건에 !nativeActive 게이트, 또는 훅이 가용일 때 DOM CTA를 숨김) — 정확히 1개 1차 CTA만 보이게 한다. 비텔레그램/미지원에서는 훅이 no-op이고 DOM TgMainButton이 픽셀-동일 fallback으로 남는다. WelcomeIntro "시작하기", PostClient(L311 인증 CTA), StoreMenu 1차 CTA에 적용. sub/커스텀색이 있는 CTA는 이 태스크에서 다루지 않는다(Task 3). 가용 여부는 setMainButtonParams.isAvailable()을 한 번 읽어 boolean state로 보관(useState+useEffect 또는 useSignal); SSR 안전을 위해 초기값 false.
  </action>
  <acceptance_criteria>
    - 3개 파일이 useNativeMainButton을 import·호출
    - text가 기존 라벨 verbatim, onClick/disabled/loading 매핑
    - 네이티브 가용 시 해당 DOM TgMainButton 억제(1개 CTA만), 미가용 시 DOM fallback 유지
    - 라벨 copy·이모지 불변
    - npm test·npm run build 그린
  </acceptance_criteria>
  <verify>
    <automated>grep -lq 'useNativeMainButton' 'app/(mini)/_components/WelcomeIntro.tsx' 'app/(mini)/post/[id]/_components/PostClient.tsx' 'app/(mini)/store/[id]/_components/StoreMenu.tsx' && npx vitest run && echo OK</automated>
  </verify>
  <done>3개 label-only CTA 네이티브화 + DOM fallback 억제, copy 불변, 테스트 그린.</done>
</task>

<task type="auto">
  <name>Task 3: DOM-유지 CTA 햅틱 보강 + 5개 라우트 BackButton 채택 + 좋아요 selection 햅틱</name>
  <files>app/(mini)/wait/[id]/_components/CancelModal.tsx, app/(mini)/_components/ShareEntryButton.tsx, app/(mini)/wait/[id]/_components/DeliveryClient.tsx, app/(mini)/post/[id]/_components/PostClient.tsx, app/(mini)/store/[id]/_components/StoreMenu.tsx, app/(mini)/cart/page.tsx, app/(mini)/order/[id]/page.tsx, app/(mini)/order/[id]/_components/OrderBackButton.tsx, app/(mini)/feed/_components/LikeButton.tsx</files>
  <read_first>
    - 07-PATTERNS.md § CTA call-site 표(L200-211): CancelModal "그만 참을래요" #8a5a3a=DOM+haptic, ShareEntryButton sub=DOM+haptic; DeliveryClient primary=NATIVE if label-only else DOM+haptic
    - 07-UI-SPEC.md § Stay-DOM CTAs(D-08 — 커스텀색/sub은 DOM + haptic.impact('medium') on press L163-166)
    - 07-CONTEXT.md D-09 BackButton 라우트 목록(store/[id], post/[id], order/[id], wait/[id], **cart** — 5개 전부) + 루트 탭 hide; D-06 좋아요 토글→selection 매핑
    - hooks/useNativeBackButton.ts (Plan 03 산출); 07-RESEARCH.md § D-06 매핑(성공→notify 'success', 에러→notify 'error', 좋아요→selection)
    - app/(mini)/cart/page.tsx — 이미 'use client' + useRouter 사용(L19/L22/L35) → 훅을 직접 호스팅 가능
    - app/(mini)/order/[id]/page.tsx — async 서버 컴포넌트(client island 없음) → 별도 client island 마운트 필요
    - app/(mini)/feed/_components/LikeButton.tsx — onTap L38(토글 진입점), 이미 'use client'
  </read_first>
  <action>
    (A) DOM 유지 CTA 햅틱 보강: CancelModal "그만 참을래요"(커스텀 #8a5a3a, DOM 유지) press에 haptic.impact('medium'), ShareEntryButton "공유 카드 만들기"(sub 보유, DOM 유지) press에 haptic.impact('medium') 추가. DeliveryClient 1차 CTA가 label-only면 useNativeMainButton 채택(Task 2 패턴), 아니면 DOM+haptic. D-06 성공/에러 매핑 적용: 참기 성공·인증 업로드·공유 생성 성공 경로에 haptic.notify('success'), 에러/거부 경로에 haptic.notify('error')(해당 핸들러가 이 파일들에 있을 때만).

    (B) D-09 5개 라우트 BackButton 전부 채택 (부분 적용·후속 메모 금지):
      - store/[id]·post/[id]·wait/[id]: client island(StoreMenu/PostClient/DeliveryClient)에서 useNativeBackButton()을 호출.
      - cart: app/(mini)/cart/page.tsx는 이미 'use client' + useRouter이므로 CartPage 본문 최상위에서 useNativeBackButton()을 직접 호출(별도 island 불필요).
      - order/[id]: app/(mini)/order/[id]/page.tsx는 async 서버 컴포넌트라 훅을 직접 호출할 수 없으므로, 새 client island `app/(mini)/order/[id]/_components/OrderBackButton.tsx`를 만든다 — `'use client'`, useNativeBackButton()만 호출하고 `return null` 하는 최소 컴포넌트. 그 다음 order/[id]/page.tsx의 반환 JSX(예: 최상위 fragment 안, SubBar 근처)에 `<OrderBackButton />`을 마운트한다. IDOR 가드/owner-scoped SELECT/receipt 렌더 로직은 일절 변경하지 않는다(T-03 보존). 루트 탭(home/feed/stats/my)에서는 훅을 호출하지 않는다(BackButton 숨김 유지).

    (C) 좋아요 토글 selection 햅틱(D-06): app/(mini)/feed/_components/LikeButton.tsx의 onTap(L38) 진입부(optimistic flip 직전/직후)에 `haptic.selection()`을 호출한다(@/lib/haptics import 추가). optimistic-then-reconcile 로직·엔드포인트·revert 동작은 변경하지 않는다.

    새 destructive 액션·copy를 추가하지 않는다.
  </action>
  <acceptance_criteria>
    - CancelModal·ShareEntryButton의 DOM CTA press에 haptic.impact('medium') 추가, DOM 유지(커스텀색/sub 보존)
    - 성공/에러 경로에 haptic.notify('success')/notify('error') 적용(존재 시)
    - D-09 5개 라우트 전부 BackButton: StoreMenu·PostClient·DeliveryClient + cart/page(직접 호출) + OrderBackButton island(order/[id]/page에 마운트)에서 useNativeBackButton() 호출 — 부분 적용/SUMMARY deferral 없음
    - order/[id]/page.tsx의 IDOR 가드/owner-scoped SELECT 변경 없음
    - LikeButton.onTap이 haptic.selection() 발생(D-06 like→selection)
    - 루트 탭(home/feed/stats/my)에서 BackButton 미호출
    - 새 copy/destructive 없음
    - npm test·npm run build 그린
  </acceptance_criteria>
  <verify>
    <automated>grep -lq 'haptic' 'app/(mini)/wait/[id]/_components/CancelModal.tsx' 'app/(mini)/_components/ShareEntryButton.tsx' && grep -lq 'useNativeBackButton' 'app/(mini)/store/[id]/_components/StoreMenu.tsx' 'app/(mini)/post/[id]/_components/PostClient.tsx' 'app/(mini)/wait/[id]/_components/DeliveryClient.tsx' 'app/(mini)/cart/page.tsx' 'app/(mini)/order/[id]/_components/OrderBackButton.tsx' && grep -q 'OrderBackButton' 'app/(mini)/order/[id]/page.tsx' && grep -q 'haptic.selection' 'app/(mini)/feed/_components/LikeButton.tsx' && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>DOM CTA 햅틱 보강 + 5개 라우트(cart 직접·order island 포함) BackButton 채택 + 좋아요 selection 햅틱, copy 불변, build 그린.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

새 신뢰 경계·입력·엔드포인트·스키마 없음 (07-RESEARCH § Security Domain). FAB는 기존 (mini) 보호 경계 안의 /home으로 진입(requireSession이 AUTH-05 이미 강제). 훅/햅틱은 로컬 props만 받음. order/[id]의 OrderBackButton island는 UI-only(useNativeBackButton만 호출, 데이터 미접근) — IDOR 가드(T-03)는 서버 페이지에 그대로 보존.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Elevation | FAB가 비보호 표면으로 내비게이트 | accept | 타깃은 (mini) 안의 /home — requireSession 가드가 이미 AUTH-05 강제(07-RESEARCH § Known Threat Patterns). 새 경계 없음 |
| T-07-07 | Tampering (logic) | 라우트 전환 시 stale MainButton 클릭 | mitigate | useNativeMainButton cleanup이 onClick VoidFunction off()+isVisible:false (Plan 03 훅이 보장, Pitfall 1) |
| T-07-08 | DoS (availability) | 콜사이트 SDK 호출이 SSR/비텔레그램에서 throw | mitigate | haptic은 ifAvailable, 훅은 isAvailable 가드 — 미가용 no-op (Pitfall 3) |
| T-07-10 | Info Disclosure | order/[id] BackButton island가 주문 데이터 노출 | accept | OrderBackButton은 useNativeBackButton만 호출하고 null 렌더 — 데이터·param 미접근. owner-scoped IDOR SELECT는 서버 페이지에 불변(T-03) |

high-severity 없음. 데이터 표면 없음, FAB는 비파괴.
</threat_model>

<verification>
- npm test 그린 (bottom-nav-fab + 기존 스위트)
- BottomNav가 push('/home')+haptic, 3개 label-only CTA가 useNativeMainButton, DOM CTA가 haptic 보강, D-09 5개 라우트 전부 useNativeBackButton, LikeButton이 haptic.selection
- npm run build 클린
</verification>

<success_criteria>
- FAB 탭이 /home 진입 + impact('medium') (죽은 FAB 해소)
- 네비 탭 selection() 햅틱
- label-only 1차 CTA 네이티브화 + DOM fallback 억제(1개 CTA만), sub/커스텀색 CTA는 DOM+haptic
- D-09 5개 라우트(store/post/wait/cart/order) 전부에서 BackButton 노출, 루트 탭에서 숨김
- 좋아요 토글이 selection 햅틱 발생(D-06)
- Wave-0 FAB 테스트 그린
</success_criteria>

<output>
Create `.planning/phases/07-ios/07-04-SUMMARY.md` when done
</output>

---
phase: 05-my
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/(mini)/stats/page.tsx
  - app/(mini)/stats/_components/WeeklyChart.tsx
  - app/(mini)/stats/_components/ConversionCards.tsx
autonomous: true
requirements: [STATS-01, STATS-02, STATS-03, STATS-04]
must_haves:
  truths:
    - "/stats RSC 화면이 이번 달·누적 아낀 돈, 덜 먹은 kcal, 번 참음, 연속일을 lib/stats 실시간 집계로 보여준다 (STATS-01/02)"
    - "주간 차트가 이번 주 월~일(KST) 요일별 아낀 돈을 순수 SVG/CSS 막대로 보여주며 미래 요일은 빈 막대 (STATS-03, D-05/06)"
    - "환산 비유 3종(공깃밥 N개·영화 N편·최다 메뉴) '절약/선택' 톤으로 보여준다 (STATS-04, D-07/08)"
    - "0 인증 사용자는 NaN 없이 all-zero 대시보드 + 격려 CTA를 본다 (empty state)"
  artifacts:
    - path: "app/(mini)/stats/page.tsx"
      provides: "통계 대시보드 RSC: requireSession 게이트 + lib/stats reads + hero + 3타일 + 차트 + 환산 + empty CTA"
      min_lines: 40
    - path: "app/(mini)/stats/_components/WeeklyChart.tsx"
      provides: "pure SVG/CSS 월~일 7칸 바차트 (max-day coral, rest soft, future 라벨 숨김)"
    - path: "app/(mini)/stats/_components/ConversionCards.tsx"
      provides: "공깃밥/영화/최다 메뉴 환산 카드 (Won/Num, topMenu null 플레이스홀더)"
  key_links:
    - from: "app/(mini)/stats/page.tsx"
      to: "lib/stats.ts userTotals/bucketWeekByKstWeekday/topMenuName/currentStreak"
      via: "RSC server fetch"
      pattern: "from '@/lib/stats'"
    - from: "app/(mini)/stats/page.tsx"
      to: "requireSession"
      via: "auth gate (owner-scope)"
      pattern: "requireSession"
    - from: "stats components"
      to: "components/Money Won/Num"
      via: "₩/number HARD RULE"
      pattern: "from '@/components/Money'"
---

<objective>
Build the `/stats` dashboard — a thin end-to-end vertical slice that turns the plan 05-01 aggregation core into the pixel-defined StatsScreen output. After this plan, a user with ≥1 인증 opening /stats sees their real, server-aggregated 절제력 통계: hero (이번 달 아낀 돈 + 🔥 스트릭 + 누적), 3 tiles (kcal·번 참음·연속일), a Mon~Sun weekly chart, and 3 conversion cards — and a 0-인증 user sees a graceful all-zero dashboard + encouraging CTA.

Port the prototype StatsScreen (`design-reference/screens-social.jsx` L101–164) with the Phase-1 token rename (`var(--primary)` → `var(--color-primary)` etc). Charts are pure SVG/CSS (CLAUDE.md — no chart libs). Every ₩/number routes through `<Won>/<Num>` (Money HARD RULE). OMIT the "공유 카드 만들기" button (D-12, Phase 6).

Purpose: STATS-01/02/03/04 — the 통계 대시보드 정본. Activates the already-wired `/stats` BottomNav tab.
Output: `app/(mini)/stats/page.tsx` + `WeeklyChart.tsx` + `ConversionCards.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-my/05-CONTEXT.md
@.planning/phases/05-my/05-RESEARCH.md
@.planning/phases/05-my/05-PATTERNS.md
@.planning/phases/05-my/05-UI-SPEC.md
@.planning/phases/05-my/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WeeklyChart + ConversionCards (pure SVG/CSS presentational components)</name>
  <files>app/(mini)/stats/_components/WeeklyChart.tsx, app/(mini)/stats/_components/ConversionCards.tsx</files>
  <read_first>
    - design-reference/screens-social.jsx StatsScreen L135–146 (weekly bars) + L148–162 (conversion cards) — the pixel source to port
    - app/(mini)/feed/_components/FeedCard.tsx doc L1–10 — the token-rename convention (prototype var(--primary)/var(--bg) → ported var(--color-primary)/var(--color-bg))
    - components/Money.tsx — <Won>/<Num> signatures (₩/number HARD RULE — never inline fmtWon in a BM-font span)
    - components/Card.tsx — Card primitive (18px radius, --shadow-card) used by both
    - .planning/phases/05-my/05-UI-SPEC.md §Component Inventory (WeeklyChart, ConversionCards rows) + §Color (max-day coral / rest soft) + §Copywriting Contract
  </read_first>
  <action>Port two presentational components. WeeklyChart: props `byDay: number[]` (length-7 from bucketWeekByKstWeekday, index0=월). Render `DAYS=['월','화','수','목','금','토','일']`, plot height 110, `maxDay = Math.max(...byDay, 1)` (guards empty, Pitfall 6), bar height `Math.max(4, (v/maxDay)*84)` (4px floor = D-06 future/empty stub), borderRadius 7, bar color `v === maxDay ? 'var(--color-primary)' : 'var(--color-primary-soft)'`, amount label `${Math.round(v/1000)}k` shown ONLY when `v > 0` (hide future/empty labels, D-06), 요일 label below (11/600, tabular-nums where numeric). Heading "이번 주 아낀 돈" (14/800 display). Pure flex `<div>` bars — NO chart lib, NO client island. ConversionCards: props `kcalTotal`, `savedTotal`, `topMenu: string | null`. Heading "그래서 이만큼이에요" (16/800 display). Three Card rows (emoji + text flex:1 + trailing value): 🍚 "안 먹은 칼로리 = 공깃밥 {rice}개" where `rice = Math.round(kcalTotal/RICE_KCAL)` (import constant from @/lib/stats); 🎬 "아낀 돈 = 영화 {movies}편" where `movies = Math.floor(savedTotal/MOVIE_WON)`; 🍗 "가장 많이 참은 메뉴 = {topMenu}" with trailing "명예의 적" wit. Values/amounts route through <Won>/<Num>. When `topMenu === null` render placeholder "아직 없어요" and suppress/replace "명예의 적" with "—" (Pitfall 6, never blank/NaN). Use D-08 menu name — do NOT use any prototype category/topCat logic. Apply the token rename to every ported `var(--*)`. No fenced code in implementation — match the StatsScreen layout via inline style flex.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -L "var(--primary)\|var(--bg)\|topCat" app/\(mini\)/stats/_components/WeeklyChart.tsx app/\(mini\)/stats/_components/ConversionCards.tsx</automated>
  </verify>
  <acceptance_criteria>
    - WeeklyChart renders 7 bars, max-day = var(--color-primary), rest = var(--color-primary-soft); amount label hidden when v==0 (D-06)
    - WeeklyChart uses pure flex divs, no chart library import, no client directive
    - ConversionCards imports RICE_KCAL/MOVIE_WON from @/lib/stats; rice=round, movies=floor; topMenu null → "아직 없어요" placeholder, no NaN
    - both route ₩/number through <Won>/<Num> from @/components/Money (no inline fmtWon span)
    - no raw prototype token (`var(--primary)`/`var(--bg)`) and no `topCat`/category logic remains
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>Both pure-SVG/CSS components compile, use ported tokens + Money HARD RULE, handle empty/null gracefully, and never reference category.</done>
</task>

<task type="auto">
  <name>Task 2: /stats RSC page (auth gate + lib/stats reads + hero + tiles + chart + conversions + empty state)</name>
  <files>app/(mini)/stats/page.tsx</files>
  <read_first>
    - app/(mini)/feed/page.tsx — RSC + requireSession + redirect('/?reauth=1') shell pattern and the FeedEmptyState (centered column, dashed coral CTA, href) to mirror for the 0-인증 state
    - lib/stats.ts (plan 05-01) — userTotals/weekRows/allItemsRows/bucketWeekByKstWeekday/topMenuName/currentStreak signatures to call
    - design-reference/screens-social.jsx StatsScreen L105–134 (title/sub + hero L112–117 + 3 tiles L119–132) — hero gradient, 🔥 watermark, custom shadow, tile colors
    - .planning/phases/05-my/05-UI-SPEC.md §Component Inventory (StatsView/Hero/3-tile) + §Typography (hero via <Won> Pretendard, NOT raw BMDohyeon) + §Empty States + §Color (tile semantic colors)
    - components/Money.tsx, components/Body.tsx, components/Card.tsx — primitives
  </read_first>
  <action>Build the `/stats` RSC page as the StatsScreen 정본 (minus the share button). Gate: `const tgId = await requireSession(); if (!tgId) redirect('/?reauth=1');` (belt-and-braces; (mini) layout also guards). Owner-scoped reads via lib/stats: `userTotals(tgId)` → {savedTotal, kcalTotal, resisted, savedMonth}; `bucketWeekByKstWeekday(weekRows(tgId), new Date())` → byDay[7]; `topMenuName(allItemsRows(tgId))`; `currentStreak(tgId)`. Render: screen title "내 절제력 통계" (24/800 chunky) + sub "참을수록 숫자가 커져요" (13/600). Hero card — coral gradient `linear-gradient(135deg,#FF6A3D,#E8431F)`, white text, custom shadow `0 16px 32px -12px rgba(232,67,31,.6)`, 🔥 watermark (96px/.16 absolute right:-10 top:-10); label "이번 달 아낀 돈" → hero amount via `<Won value={savedMonth} />` (Pretendard hero scale per UI-SPEC Typography, NOT raw BMDohyeon for the ₩ value) → "🏆 {streak}일 연속 참는 중 · 누적 <Won value={savedTotal} />" (12.5/600 .92). 3-tile grid (gap 8–12): 🔥 `<Num value={kcalTotal}/>` "kcal 덜 먹음" (--color-primary) · ✋ `<Num value={resisted}/>` "번 참음" (--color-green) · 📅 `<Num value={streak}/>` "일 연속" (--color-amber-ink). Then `<WeeklyChart byDay={byDay}/>` and `<ConversionCards kcalTotal={kcalTotal} savedTotal={savedTotal} topMenu={topMenu}/>`. Empty state (resisted === 0): still render the dashboard chrome (hero ₩0, 0 tiles, 4px soft bars) PLUS an encouraging CTA card — heading "아직 참은 기록이 없어요", body "지금 첫 주문을 참아보면 여기 숫자가 쌓여요", coral action "첫 인증하러 가기" linking into the order/참기 flow (mirror FeedEmptyState dashed coral CTA). OMIT the TgMainButton "공유 카드 만들기" (D-12). Apply token rename to every ported var(--*). Wrap in `<Body style={{ background: 'var(--color-bg)' }}>`; screen padding 16px 16px 0.</action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/(mini)/stats/page.tsx is an async RSC calling requireSession; redirects on null (no cross-user param)
    - all stats come from lib/stats owner-scoped reads (tgId from session only) — STATS-02 server-authority
    - renders hero (이번 달 via <Won>) + 3 tiles + <WeeklyChart> + <ConversionCards>; hero/tile numbers via <Won>/<Num> (HARD RULE)
    - 0-인증 path renders all-zero chrome + "아직 참은 기록이 없어요 · 첫 인증하러 가기" CTA, no NaN
    - NO "공유 카드 만들기" button (D-12); no raw prototype token remains
    - npm run build succeeds; /stats BottomNav tab now active
  </acceptance_criteria>
  <done>/stats renders the real server-aggregated dashboard for users with posts and a graceful empty-state CTA for 0-인증, all money via Money HARD RULE, share button omitted, build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram WebView → /stats RSC | uid from requireSession() only; no id param |
| /stats RSC → lib/stats reads | owner-scoped on tgId (inherited from 05-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | /stats page reads | mitigate | tgId derived ONLY from requireSession(); every lib/stats call passes that tgId; no user-supplied id selects another user's aggregates (IDOR). Dominant threat. |
| T-05-04 | Information Disclosure | anonymity consistency | mitigate | /stats shows ONLY the viewer's own aggregate numbers — no other user's identity or data surfaces anywhere on this screen. |
| T-05-05 | Spoofing / Access | unauth access to /stats | mitigate | requireSession() gate + (mini) layout guard; null → redirect('/?reauth=1') (AUTH-05). |
| T-05-SC | Tampering | package installs | accept | Zero packages installed this phase (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npm run build` succeeds (/stats route compiles as RSC)
- `npm test` full suite stays green
- Manual (post-deploy, per 05-VALIDATION Manual-Only): open /stats with ≥1 인증 → hero/3타일/주간차트/환산비유 reflect real data; open as 0-인증 → all-zero + CTA
- grep confirms no `var(--primary)`/`var(--bg)`/`topCat` and no "공유 카드 만들기" button
</verification>

<success_criteria>
/stats is the pixel-faithful StatsScreen 정본 (minus share button): real-time server-aggregated hero + 3 tiles + Mon~Sun pure-SVG weekly chart + 3 conversion cards, owner-scoped, Money HARD RULE honored, 0-인증 empty state graceful, BottomNav tab active, build clean.
</success_criteria>

<output>
Create `.planning/phases/05-my/05-02-SUMMARY.md` when done
</output>

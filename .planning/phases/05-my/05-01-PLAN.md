---
phase: 05-my
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/stats.ts
  - app/api/posts/route.ts
  - tests/lib/stats.test.ts
  - tests/api/stats-live.test.ts
autonomous: true
requirements: [STATS-01, STATS-02, STATS-03, STATS-04]
must_haves:
  truths:
    - "lib/stats.ts computes 누적/이번달 아낀 돈, 덜 먹은 kcal, 번 참음(post count), live streak owner-scoped on tgId (D-01/02/03/04)"
    - "이번 달 경계는 KST 달력 월(off-by-9h-safe), 주간 차트는 월~일(KST) 7칸·미래 요일=0 (D-03/05/06)"
    - "최다 메뉴는 items[].name 빈도 1위 (NOT category) (D-08); 환산상수 RICE_KCAL=300, MOVIE_WON=15000 (D-07)"
    - "모든 통계 read가 where eq(posts.tgId, uid) 소유 스코프 (IDOR 차단)"
  artifacts:
    - path: "lib/stats.ts"
      provides: "pure aggregation fns (kstMonthBounds, bucketWeekByKstWeekday, topMenuName, currentStreak) + DB scalar totals read + conversion constants"
      contains: "kstMonthBounds"
      exports: ["kstMonthBounds", "bucketWeekByKstWeekday", "topMenuName", "RICE_KCAL", "MOVIE_WON"]
    - path: "tests/lib/stats.test.ts"
      provides: "pure-fn unit suite (KST month boundary, Mon-first weekday bucket, future=0, topMenu freq, conversions, current streak)"
    - path: "tests/api/stats-live.test.ts"
      provides: "live-Neon server-authority aggregate smoke (skipIf no DATABASE_URL)"
  key_links:
    - from: "lib/stats.ts"
      to: "lib/streak.ts kstDateKey/nextStreak"
      via: "import — single-sourced KST math + streak recompute"
      pattern: "from '@/lib/streak'"
    - from: "lib/stats.ts"
      to: "posts table"
      via: "owner-scoped Drizzle aggregate read"
      pattern: "eq\\(posts\\.tgId"
---

<objective>
Build `lib/stats.ts` — the single tested aggregation core for Phase 5. Thin owner-scoped DB reads over the frozen `posts` columns plus PURE exported functions for every date/JSON computation (KST month bounds, Mon-first weekday bucketing, topMenu frequency, current-streak recompute). This is the Nyquist test seam: every aggregation is a pure function mapped 1:1 to a Vitest case.

This is a TDD plan — the pure functions get their tests written first (RED), then implemented (GREEN). Lift `computeStreak` out of `app/api/posts/route.ts` into `lib/stats.ts` so the route and stats share ONE streak definition (D-04).

Purpose: Server-authority real-time stats (D-02) that `/stats` (plan 05-02) and `/my` (plan 05-03) both consume. Getting KST boundaries and visibility exactly right here means both pages are correct by construction.
Output: `lib/stats.ts`, refactored `app/api/posts/route.ts` (imports shared streak), `tests/lib/stats.test.ts`, `tests/api/stats-live.test.ts`.

NO schema change, NO db:push, NO mutation — pure read aggregation over existing columns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-my/05-CONTEXT.md
@.planning/phases/05-my/05-RESEARCH.md
@.planning/phases/05-my/05-PATTERNS.md
@.planning/phases/05-my/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Wave-0 pure-function test suite for lib/stats.ts</name>
  <files>tests/lib/stats.test.ts</files>
  <read_first>
    - tests/lib/streak.test.ts — vitest describe/it/expect style + KST boundary-instant comments (UTC↔KST) to mirror for kstMonthBounds
    - lib/streak.ts — kstDateKey/nextStreak signatures the new fns reuse (the fns under test import these)
    - .planning/phases/05-my/05-RESEARCH.md §Validation Architecture → Test Map + §Common Pitfalls 1/2/6 — exact boundary cases to assert
  </read_first>
  <behavior>
    - kstMonthBounds: instant at 2026-06-01 00:30 KST (= 2026-05-31 15:30 UTC) yields June bounds → that instant counts as June, not May (Pitfall 1 off-by-9h)
    - kstMonthBounds: returns { startUtc, endUtc } half-open [start, end); a post at the last KST millisecond of the month is inside, first ms of next month is outside
    - bucketWeekByKstWeekday: length-7 array, index 0 = 월(Mon); a row's savedAmount lands in the bucket for its KST weekday; sums multiple rows in same bucket
    - bucketWeekByKstWeekday: future weekdays (after KST today) stay 0 (D-06); rows outside this KST week are excluded
    - topMenuName: most-frequent items[].name across all rows wins (NOT category); empty rows → null; deterministic tie-break
    - currentStreak recompute: latest endured post on yesterday(KST) → streak holds; 2+ KST days stale → 0; no posts → 0 (D-04, extends streak cases)
    - conversion math: rice = Math.round(kcalTotal/RICE_KCAL=300), movies = Math.floor(savedTotal/MOVIE_WON=15000), both 0 on empty (D-07)
  </behavior>
  <action>Author the pure-fn suite FIRST (RED — functions do not exist yet, suite must fail to import/compile). Mirror tests/lib/streak.test.ts: vitest `describe`/`it`/`expect`, no DB, no live clock — pass `now` explicitly into kstMonthBounds/bucketWeekByKstWeekday. Group cases by named export: `describe("kstMonthBounds")`, `"bucketWeek"`, `"topMenu"`, `"currentStreak"`, `"conversions"` so the RESEARCH Test Map `-t` filters (`vitest run ... -t "kstMonthBounds"`) resolve. Each KST case carries an explicit `// 2026-06-01 00:30 KST = 2026-05-31 15:30 UTC` style comment. Do NOT write implementations in this task. Per D-08 assert topMenu reads `items[].name` and explicitly add a case proving it does NOT count a `category` field. This task delivers the failing spec the next task implements against.</action>
  <verify>
    <automated>npx vitest run tests/lib/stats.test.ts 2>&1 | grep -qi "fail\|error\|cannot find" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - tests/lib/stats.test.ts exists with describe blocks named kstMonthBounds/bucketWeek/topMenu/currentStreak/conversions
    - suite currently FAILS (imports of unimplemented lib/stats exports) — RED state confirmed
    - a topMenu case asserts name-frequency and explicitly rejects category logic (D-08)
    - a kstMonthBounds case uses a sub-9h-of-midnight KST boundary instant (Pitfall 1)
  </acceptance_criteria>
  <done>Failing pure-fn spec authored covering every D-03/05/06/08 + conversions + D-04 streak behavior; running it shows RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement lib/stats.ts (pure fns + owner-scoped totals read) and lift computeStreak</name>
  <files>lib/stats.ts, app/api/posts/route.ts</files>
  <read_first>
    - lib/streak.ts (full) — kstDateKey + KST_OFFSET_MS fixed +09:00 convention + nextStreak signature (reuse, do NOT reintroduce date-fns-tz)
    - lib/feed.ts L23-25 + L100-162 — drizzle import style (`@/` alias), the verified `sql<number>\`...::int\`` + `coalesce` idiom, and the centralized visibility-gate WHERE to mirror
    - app/api/posts/route.ts — the existing `computeStreak` (select latest endured post → nextStreak) to LIFT into lib/stats.ts and re-import here
    - lib/db.ts + db/schema.ts (posts: savedAmount, kcal, streakDay, endured, items jsonb, tgId, createdAt, hiddenAt, deletedAt) — column names for the aggregate
    - tests/lib/stats.test.ts (from Task 1) — the contract these fns must satisfy
  </read_first>
  <action>Implement lib/stats.ts to make Task 1 GREEN. Export PURE fns: `kstMonthBounds(now)` (KST calendar month [startUtc,endUtc) using the +09:00 offset-add convention from lib/streak.ts — NEVER raw Date.getMonth() on UTC, Pitfall 1), `bucketWeekByKstWeekday(rows, now)` (length-7 index0=월, weekday derived via kstDateKey, future days stay 0 per D-06, Pitfall 2), `topMenuName(itemsRows)` (most-frequent `items[].name` per D-08, returns string|null, deterministic tie-break, NEVER category). Export conversion constants `RICE_KCAL = 300` and `MOVIE_WON = 15000` (D-07). LIFT `computeStreak` from app/api/posts/route.ts into lib/stats.ts as the shared definition and add a `currentStreak(tgId)` variant for the live display (select latest endured=true post for tgId, orderBy desc createdAt limit 1, apply `nextStreak(new Date(), prev ?? null, true)`, D-04); then update app/api/posts/route.ts to IMPORT computeStreak from lib/stats.ts instead of its local copy (single source). Add the owner-scoped scalar totals read `userTotals(uid)`: `db.select` with `savedTotal/kcalTotal/resisted/savedMonth` using the verified `sql<number>\`coalesce(...)::int\`` form; resisted = `count(*)::int` (D-01); savedMonth uses the kstMonthBounds startUtc/endUtc as Drizzle params (D-03). Centralize ONE visibility predicate constant in this module — exclude `deletedAt IS NOT NULL`, INCLUDE `hiddenAt` (own resistance record per CONTEXT discretion / Pitfall 5) — and apply it to EVERY read. Every read WHERE is `and(eq(posts.tgId, uid), <visibility>)` — owner-scoped, no user-supplied id (IDOR control). Also export a `weekRows(uid)`/`allItemsRows(uid)` (or one combined fetch) reader returning the rows the pure bucketing/topMenu fns consume. If the `filter (where …)` month aggregate errors against Postgres, fall back to two `.select()`s (one with the month-bound WHERE) — both trivially correct (RESEARCH A1).</action>
  <verify>
    <automated>npx vitest run tests/lib/stats.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/stats.ts exports kstMonthBounds, bucketWeekByKstWeekday, topMenuName, RICE_KCAL, MOVIE_WON, currentStreak, userTotals
    - tests/lib/stats.test.ts exits 0 (GREEN)
    - topMenu uses items[].name not category (verified by Task-1 case)
    - every DB read is owner-scoped: `grep -c "eq(posts.tgId" lib/stats.ts` ≥ number of reads, and no read selects another user's id
    - all KST math routes through kstDateKey/KST offset — `grep -nE "getMonth\(\)|getDay\(\)" lib/stats.ts` (excluding comments) returns nothing
    - app/api/posts/route.ts imports computeStreak from '@/lib/stats' (no local duplicate) and tsc/build stays clean
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>All pure-fn tests green; lib/stats.ts is the single owner-scoped aggregation source; route shares the lifted streak definition; no UTC date primitive leaks.</done>
</task>

<task type="auto">
  <name>Task 3: Live-Neon server-authority aggregate smoke</name>
  <files>tests/api/stats-live.test.ts</files>
  <read_first>
    - tests/api/like-live.test.ts (full) — `// @vitest-environment node` header, `describe.skipIf(!process.env.DATABASE_URL)`, seed users+orders+posts → drive real query → assert → cleanup pattern (separate file because unit files mock @/lib/db)
    - lib/stats.ts (from Task 2) — userTotals/currentStreak signatures to drive against live Neon
    - db/schema.ts — posts/users/orders insert shapes for seeding
  </read_first>
  <action>Author the live smoke mirroring tests/api/like-live.test.ts: `// @vitest-environment node`; `describe.skipIf(!process.env.DATABASE_URL)`. Seed a deterministic test user + a couple of orders + posts with known savedAmount/kcal/endured/items for a unique test tgId, call lib/stats.ts `userTotals(tgId)` (+ `currentStreak`) against REAL Neon, assert server-authority totals: savedTotal/kcalTotal = SUM of seeded values, resisted = COUNT of seeded posts (D-01/02), savedMonth reflects only this-KST-month rows (D-03), streak matches the seeded endured chain (D-04). Critically assert IDOR isolation: seed a SECOND user's posts and confirm `userTotals(firstTgId)` excludes them. Clean up all seeded rows in afterAll. This separate file proves the real read-only round-trip works (RESEARCH Open Q2 — validates the assumed `filter (where)`/aggregate SQL against live Postgres).</action>
  <verify>
    <automated>npx vitest run tests/api/stats-live.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/stats-live.test.ts exists with `@vitest-environment node` + skipIf(!DATABASE_URL)
    - drives real lib/stats.ts reads against live Neon; asserts savedTotal/kcalTotal/resisted/savedMonth/streak
    - includes a second-user IDOR-isolation assertion (other tgId's posts excluded)
    - exits 0 with DATABASE_URL present (live), skips cleanly without it
    - seeded rows cleaned up in afterAll
  </acceptance_criteria>
  <done>Live aggregate read validated against real Neon with owner-scope isolation; assumed aggregate SQL confirmed against Postgres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram WebView → RSC/lib | Session-derived uid is the ONLY identity input; no user-supplied id selects data |
| lib/stats.ts → posts (Neon) | Read-only aggregate; every WHERE owner-scoped on tgId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | lib/stats.ts every read | mitigate | uid comes ONLY from session (consumed by pages in 05-02/03); EVERY read WHERE `and(eq(posts.tgId, uid), <visibility>)`; no path/query id param. Live smoke asserts a second user's rows are excluded. This is the dominant phase threat. |
| T-05-02 | Tampering | client-supplied stat values | mitigate | Server recomputes from posts (D-02); client never sends totals — structurally absent. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Phase 5 installs ZERO packages (RESEARCH Package Legitimacy Audit: N/A); no install task exists. |
</threat_model>

<verification>
- `npx vitest run tests/lib/stats.test.ts` green (pure fns)
- `npx vitest run tests/api/stats-live.test.ts` green or skipped
- `npx vitest run` full suite stays green (route refactor did not regress posts API)
- `npx tsc --noEmit` + `npm run build` clean
- `grep -nE "getMonth\(\)|getDay\(\)" lib/stats.ts` (non-comment) empty — KST single-sourced
</verification>

<success_criteria>
lib/stats.ts is the single owner-scoped aggregation core: pure fns for KST month bounds / Mon-first weekday bucket / topMenu name-frequency / current-streak recompute + conversion constants, plus owner-scoped scalar totals reads. computeStreak shared with the posts route. Pure-fn suite + live smoke green. No schema change, no mutation, no UTC date primitive leak.
</success_criteria>

<output>
Create `.planning/phases/05-my/05-01-SUMMARY.md` when done
</output>

---
phase: 05-my
plan: 03
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - app/(mini)/feed/_components/FeedCard.tsx
  - lib/stats.ts
  - app/(mini)/my/page.tsx
  - tests/ui/my-records.test.tsx
autonomous: true
requirements: [STATS-05]
must_haves:
  truths:
    - "/my RSC 화면이 내 프로필(텔레그램 실명/아바타 + 피드 익명 핸들 병기)을 보여준다 (D-09)"
    - "/my가 누적 절약/kcal/현재 스트릭 요약 + '자세히 → /stats' 링크를 보여준다 (D-10)"
    - "/my가 내 인증 기록을 FeedCard(readOnly — 좋아요/신고 액션 숨김)로 owner-scoped 리스트로 보여준다 (D-11)"
    - "FeedCard readOnly prop은 기본 falsy라 기존 /feed 동작 불변; per-user 기록 쿼리는 where eq(posts.tgId, uid) owner-scoped"
  artifacts:
    - path: "app/(mini)/my/page.tsx"
      provides: "MY RSC: requireSession + users 프로필 + handleFor 병기 + 누적 요약 + per-user FeedCard[] readOnly + empty state"
      min_lines: 40
    - path: "app/(mini)/feed/_components/FeedCard.tsx"
      provides: "readOnly prop suppressing LikeButton + ReportMenu (default unchanged)"
      contains: "readOnly"
    - path: "lib/stats.ts"
      provides: "ownerRecordsPage(uid) — feedPage variant owner-scoped on posts_tg_created_idx"
      contains: "eq(posts.tgId"
    - path: "tests/ui/my-records.test.tsx"
      provides: "RTL shell-compose: own-records FeedCard renders with actions suppressed"
  key_links:
    - from: "app/(mini)/my/page.tsx"
      to: "lib/stats ownerRecordsPage + userTotals + currentStreak"
      via: "RSC owner-scoped reads"
      pattern: "from '@/lib/stats'"
    - from: "app/(mini)/my/page.tsx"
      to: "lib/handle handleFor"
      via: "anonymity note 병기"
      pattern: "handleFor"
    - from: "app/(mini)/my/page.tsx"
      to: "FeedCard readOnly"
      via: "own-records render with actions suppressed"
      pattern: "readOnly"
---

<objective>
Build the `/my` screen — the private self-view vertical slice. After this plan, a user opening /my sees: their profile (Telegram 실명/아바타 + "피드에선 {handle}로 보여요" anonymity note), a cumulative summary (누적 절약/kcal/현재 스트릭) with a "자세히 → /stats" link, and their own 인증 기록 rendered through the existing `FeedCard` in read-only mode (LikeButton + ReportMenu suppressed). 0-기록 users see profile + summary + an encouraging empty CTA.

Three moves: (1) add a `readOnly` prop to `FeedCard` (defaults to current behavior — feed unaffected); (2) add an owner-scoped `ownerRecordsPage(uid)` feedPage-variant to `lib/stats.ts` (keyset on `posts_tg_created_idx`); (3) the `/my` RSC page composing profile + summary + records.

Purpose: STATS-05 — MY 화면. Activates the already-wired `/my` BottomNav tab.
Output: modified `FeedCard.tsx`, extended `lib/stats.ts`, new `app/(mini)/my/page.tsx`, `tests/ui/my-records.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-my/05-CONTEXT.md
@.planning/phases/05-my/05-RESEARCH.md
@.planning/phases/05-my/05-PATTERNS.md
@.planning/phases/05-my/05-UI-SPEC.md
@.planning/phases/05-my/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add readOnly prop to FeedCard + owner-scoped ownerRecordsPage to lib/stats</name>
  <files>app/(mini)/feed/_components/FeedCard.tsx, lib/stats.ts</files>
  <read_first>
    - app/(mini)/feed/_components/FeedCard.tsx (full) — current FeedCardProps + action bar L175–192 where LikeButton always renders and ReportMenu renders only when !isOwn (L191)
    - lib/feed.ts L100–162 — feedPage keyset query (createdAt,id desc), FeedPost shape, decodeCursor codec, centralized visibility gate to MIRROR owner-scoped
    - db/schema.ts — posts columns + posts_tg_created_idx (the index this read targets) + users (firstName/username/theme)
    - .planning/phases/05-my/05-RESEARCH.md §Pitfall 4 (readOnly prop recommendation) + Pattern 3 (per-user feedPage variant)
  </read_first>
  <action>Two changes. (A) FeedCard: add an OPTIONAL `readOnly?: boolean` to FeedCardProps (default falsy → current behavior, A3 low-risk). In the action bar, gate the LikeButton with `!readOnly` and the ReportMenu with `!readOnly && !isOwn` (currently only `!isOwn`). When readOnly, BOTH actions are suppressed (D-11 "좋아요/신고 액션 숨김"). The per-user query must still supply likeCount/liked to satisfy FeedPost (or stub to 0/false) so the type holds. The default-falsy prop means /feed and /api/feed surfaces are unchanged. (B) lib/stats.ts: add `ownerRecordsPage(uid, cursor?)` — a feedPage VARIANT mirroring lib/feed.ts feedPage but ADD `eq(posts.tgId, uid)` to the WHERE; keep the `(createdAt, id)` keyset desc + the SAME centralized visibility gate (exclude deletedAt, include hiddenAt — consistent with userTotals per Pitfall 5) + reuse the decodeCursor codec from lib/feed.ts (import it; do not duplicate). Returns the same row/FeedPost shape feedPage returns (with likeCount/liked stubbed or computed) so FeedCard can render it. This read targets posts_tg_created_idx (schema) and is owner-scoped (IDOR control).</action>
  <verify>
    <automated>npx tsc --noEmit && grep -n "readOnly" app/\(mini\)/feed/_components/FeedCard.tsx && grep -n "eq(posts.tgId" lib/stats.ts</automated>
  </verify>
  <acceptance_criteria>
    - FeedCardProps has optional readOnly; readOnly suppresses BOTH LikeButton and ReportMenu; default falsy leaves feed behavior unchanged
    - lib/stats.ts exports ownerRecordsPage with `eq(posts.tgId, uid)` in the WHERE (owner-scoped) + keyset (createdAt,id) desc + same visibility gate as userTotals
    - ownerRecordsPage reuses decodeCursor from lib/feed (no duplicate codec)
    - existing feed tests still green (no regression from the prop addition)
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>FeedCard gains a backward-compatible readOnly prop; lib/stats exposes an owner-scoped per-user records page reusing the feed keyset + visibility gate.</done>
</task>

<task type="auto">
  <name>Task 2: /my RSC page (profile + handle 병기 + cumulative summary + readOnly records + empty state)</name>
  <files>app/(mini)/my/page.tsx</files>
  <read_first>
    - app/(mini)/feed/page.tsx — RSC + requireSession + redirect shell + FeedEmptyState (dashed coral CTA) to mirror
    - lib/stats.ts (extended) — userTotals/currentStreak/ownerRecordsPage signatures
    - lib/handle.ts — handleFor(tgId) pure deterministic handle (same handle the feed card shows)
    - db/schema.ts users (firstName/username/theme) — profile row read by tgId
    - app/(mini)/feed/_components/FeedCard.tsx — FeedCard props incl new readOnly + viewerTgId
    - .planning/phases/05-my/05-UI-SPEC.md §/my Screen Layout + §Component Inventory (My profile header / summary / records list) + §Empty States + §Copywriting Contract
    - components/Avatar.tsx, components/Card.tsx, components/Body.tsx, components/Icon.tsx (chevron) — primitives
  </read_first>
  <action>Build `/my` RSC. Gate: `const tgId = await requireSession(); if (!tgId) redirect('/?reauth=1');`. Read profile: `const [u] = await db.select().from(users).where(eq(users.tgId, tgId))` (owner-scoped). Vertical stack on `<Body style={{background:'var(--color-bg)'}}>`, padding 16px 16px 0, Card-to-Card gap 14. (1) Optional screen title "MY"/"내 정보" (24/800 chunky). (2) Profile header Card (D-09): Avatar (Telegram photo if present else initials from u.firstName warm gradient) + real name `u.firstName` (15/700) + anonymity note "피드에선 {handleFor(tgId)}로 보여요" (12.5/600, handle in coral/ink-emphasis). (3) Cumulative summary Card (D-10): 누적 아낀 돈 `<Won value={savedTotal}/>` + 덜 먹은 kcal `<Num value={kcalTotal}/>` + 현재 스트릭 `<Num value={streak}/>` (same stat colors as /stats tiles; reuse userTotals + currentStreak — cheap, RESEARCH Open Q3) + a coral "자세히 →" row (chevron Icon) linking `<Link href="/stats">`. Teaser only — does NOT duplicate the full dashboard. (4) "내 인증 기록" heading (14–16/800 display) + own-records: `const page = await ownerRecordsPage(tgId)`; map rows through `<FeedCard post={row} viewerTgId={tgId} readOnly />` (viewerTgId === ownerTgId so isOwn also hides report; readOnly suppresses both actions). If 0 records → empty state: heading "아직 참은 기록이 없어요", body "첫 인증을 올리면 여기에 모여요", coral "첫 인증하러 가기" CTA (profile + summary still render, summary ₩0/0). (5) MY footer "배달의 만족 · 시켜놓고, 참는다" (small ink3 centered). For v1 per-user volume a single server-rendered page is sufficient — no load-more client island required (RESEARCH Open Q1). Money HARD RULE on every number; token-rename any ported var(--*).</action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/(mini)/my/page.tsx async RSC: requireSession → redirect on null; users read owner-scoped on tgId
    - profile shows u.firstName + "피드에선 {handleFor(tgId)}로 보여요" (D-09); handle matches feed card
    - cumulative summary shows 누적 절약/kcal/스트릭 via <Won>/<Num> + coral "자세히 → /stats" Link (D-10)
    - records rendered via <FeedCard ... readOnly /> from ownerRecordsPage(tgId) — owner-scoped, actions suppressed (D-11)
    - 0-기록 path renders profile + summary + "아직 참은 기록이 없어요 · 첫 인증하러 가기" CTA, no NaN
    - npm run build succeeds; /my BottomNav tab active
  </acceptance_criteria>
  <done>/my shows owner-scoped profile (실명 + 익명 핸들 병기), cumulative teaser linking to /stats, and read-only own-records FeedCards with a graceful empty state; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: RTL test — own-records FeedCard renders read-only (actions suppressed)</name>
  <files>tests/ui/my-records.test.tsx</files>
  <read_first>
    - .planning/STATE.md decision [01-03] — RTL shell-compose pattern (render component composition directly, NOT async RSC layout) to work around async RSC
    - app/(mini)/feed/_components/FeedCard.tsx (with readOnly) — props to drive; LikeButton/ReportMenu testids or accessible labels to assert absent
    - tests/lib/streak.test.ts / existing tests/ui (if any) — vitest + @testing-library/react conventions + tests/setup.ts
    - lib/feed.ts FeedPost shape — to build a fixture post
  </read_first>
  <action>Author an RTL test composing FeedCard directly (shell-compose per 01-03, avoid async RSC). Render `<FeedCard post={fixture} viewerTgId={fixture.tgId} readOnly />` and assert the LikeButton and ReportMenu are NOT in the document (query by their accessible label/role/testid and expect null), proving D-11 actions are suppressed. Add a contrast case: render the SAME card WITHOUT readOnly and assert the LikeButton IS present (proves the prop default leaves feed behavior intact). Assert the card still renders its core content (dual photos / 아낌 ₩ / 캡션) so readOnly suppresses only actions, not the record. Build the fixture from the FeedPost shape (likeCount/liked stubbed). This is the STATS-05 Wave-0 UI check.</action>
  <verify>
    <automated>npx vitest run tests/ui/my-records.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tests/ui/my-records.test.tsx composes FeedCard directly (shell-compose, no async RSC)
    - asserts readOnly suppresses LikeButton AND ReportMenu (both queried, expected absent)
    - contrast case: without readOnly, LikeButton present (default behavior intact)
    - asserts core card content still renders under readOnly
    - exits 0
  </acceptance_criteria>
  <done>RTL proof that FeedCard readOnly hides both actions while preserving the record render, and that the default leaves the feed unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram WebView → /my RSC | uid from requireSession() only; no id param selects another user |
| /my RSC → users/posts reads | every read owner-scoped on tgId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | /my records + profile reads | mitigate | ownerRecordsPage WHERE includes `eq(posts.tgId, uid)`; users read `eq(users.tgId, uid)`; uid from requireSession() only — no cross-user leak (IDOR). Dominant threat. |
| T-05-07 | Information Disclosure | anonymity consistency | mitigate | /my shows the VIEWER'S OWN real identity (D-09 — their private screen, fine); records are the viewer's OWN posts; FeedCard still renders the anonymous handleFor for the author = the viewer, so no OTHER user's real identity is ever exposed. |
| T-05-08 | Tampering / DoS | forged cursor on /my records | mitigate | ownerRecordsPage reuses decodeCursor (try/catch→null, never throws) + Drizzle-parameterized bound — same defense as feed. |
| T-05-09 | Spoofing / Access | unauth access to /my | mitigate | requireSession() gate + (mini) layout guard; null → redirect (AUTH-05). |
| T-05-SC | Tampering | package installs | accept | Zero packages installed this phase (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx vitest run tests/ui/my-records.test.tsx` green
- `npm test` full suite green (no feed regression from readOnly prop)
- `npx tsc --noEmit` + `npm run build` clean
- Manual (post-deploy, per 05-VALIDATION): open /my → 실명+핸들 병기, 누적 요약 + 자세히 링크, FeedCard 내 기록 리스트(액션 없음); 0-기록 → empty CTA
- grep confirms ownerRecordsPage is owner-scoped (`eq(posts.tgId`)
</verification>

<success_criteria>
/my is the private self-view: owner-scoped profile (실명 + handleFor 병기, D-09), cumulative teaser → /stats (D-10), read-only own-records FeedCards (D-11), graceful 0-기록 empty state. FeedCard readOnly prop is backward-compatible (feed unaffected). BottomNav /my tab active, build + tests clean.
</success_criteria>

<output>
Create `.planning/phases/05-my/05-03-SUMMARY.md` when done
</output>
